# Build ledger — plan: docs/plans/2026-09-09T112557-PLAN--durable-delivery-history.md

F1: Ruling: the readback assumed `docs/evidence/` is implicitly scoped to the plan being closed; it is not — the directory holds files from several plans, matched by an id prefix (`0070-RED-baseline.txt`) — so the convention states the id-prefix match explicitly and reports rather than guesses when a file cannot be attributed — costs a wrong-plan evidence copy if the prefix rule is later broken by a producer that does not follow it
F1: Ruling: the readback found no written rule for multiple legacy `--review-vNN.md`; built the highest-version reading, matching what `/add-framework--done` STEP 1.3 already did before the review companion was retired — costs losing an earlier review version if a legacy plan ever needs its full review history
F1: Ruling: the plan attributed the new `uses:` declarations to F2 and F4 only; F1 needed one too, because its prose names /add-framework--done — declared as `mention:`, matching the existing `mention: /add-framework--build` row, since the skill describes the command rather than loading it — costs a wrong edge kind if a later reader treats the skill as depending on the command
F1: complete (commits d314fe3..6a345b1, build.js exit 0, 0 warnings vs 0 baseline, 710 edges, framwork/ lane clean)
F2: Ruling: the readback asked whether STEP 3 or STEP 6 writes `origin`; STEP 3's entry-field list held the literal `docs/plans/<id>.md`, so F2 edited both — STEP 3 for the value, STEP 6 for the assembly — rather than STEP 6 alone as the Current State table implied — costs a stale literal in STEP 3 if a later edit reverts one half
F2: Ruling: L1.2 asserts the file no longer contains `origin: docs/plans/<id>.md`; the string survives inside the new prohibition that forbids that value — read the assertion as "no longer the authorised VALUE", which a grep alone cannot express — costs a false green if a future reader greps for the string instead of the field
F2: complete (commits 6a345b1..199b066, build.js exit 0, 0 warnings vs 0 baseline, 711 edges, framwork/ lane clean, STEP map still integer 1-9)
F3: Ruling: the readback asked what happens to branch removal when the worktree removal is skipped; built "both skipped together", because `git branch -d` fails while the branch is checked out in a live worktree and attempting it adds a second failure that says nothing new — costs a manual `git branch -d` from the primary checkout, named in the report
F3: Ruling: the new STEP 8 contradicted two top-of-file prohibitions the plan did not list — the untracked-file rm ban and the blanket ban on deleting under docs/plans/ and docs/brainstorming/; rewrote both in this block rather than leaving a same-file contradiction for the review pass — costs nothing found; leaving them would have made the command refuse its own new step
F3: Ruling: removed `mention: @framework-discovery-agent` from the uses: block after the build warned it was stale — the only prose naming it was the Kept cell this block deleted, and F5 gives the agent its own reason to read docs/deliveries/ — costs re-adding the edge if a later block names the agent in this file again
F3: Ruling: L1.3 asserts docs/plans/ no longer appears in the Kept column; it appears there qualified as "files belonging to OTHER plans", which is true and is the honest statement — read the assertion as "this plan's files are no longer claimed as kept" — costs a false red if a future reader greps the column for the bare string
F3: complete (commits 199b066..c09b7e1, build.js exit 0, 0 warnings vs 0 baseline, 710 edges, framwork/ lane clean, STEP map still integer 1-9)
F4: complete (commits c09b7e1..45eb0d3, build.js exit 0, 0 warnings vs 0 baseline, 711 edges, framwork/ lane clean)
F5: Ruling: the readback found no format for "marked as delivered"; built a ` [delivered]` suffix on the plan's row in the existing Ranked Plans table, changing no structure — costs a caller parsing the column by exact filename, which nothing does today
F5: complete (commits 45eb0d3..027cc0e, build.js exit 0, 0 warnings vs 0 baseline, 711 edges, framwork/ lane clean)
F6: complete (commits 027cc0e..2ca4794, build.js exit 0, 0 warnings vs 0 baseline, 711 edges, framwork/ lane clean)
F7: complete (commits 2ca4794..f5d2a6c, build.js exit 0, 0 warnings vs 0 baseline, 711 edges, framwork/ lane clean, L1.1 all 7 files, L1.8 ignore rules intact)
L3.0: measured — `git worktree remove` without `--force`, run from outside, exits 0 and silently deletes every gitignored file in the worktree. Problem 1's premise holds exactly as the plan states it. No correction owed.
L3.1: pass — the STEP 6 assembly produces plan.md, ledger.md, design.md and evidences/ with original names; `git status --porcelain docs/deliveries/` reports `??`, so the directory is untracked and NOT ignored; the id-prefix match left another plan's evidence file out.
L3.2: pass — after the archive commit, removing the worktree deleted every gitignored original while `git show <branch>:docs/deliveries/<id>/plan.md` still resolved.
L3.4: measured — from inside, `git worktree remove .` unregisters the worktree and then fails to delete the directory (Permission denied, exit 255), leaving an orphan; a second call answers `is not a working tree`. `git branch -d` on a MERGED branch checked out in a live worktree fails with `cannot delete branch used by worktree` (exit 1).
F3: Ruling: L3.4 measured a worse failure than F3's text assumed — attempting the self-removal half-succeeds and leaves an orphan directory, rather than failing cleanly; corrected the prose in a follow-up commit (30d9a49) rather than re-executing F3, which the resume rule forbids — costs a reader seeing the fix in a commit that carries no F-block id, which the trailer `Follows F3` answers
L2.1: pass — ADD_GRAPH_WARNINGS=1 node scripts/build.js exit 0 and 0 warnings on every F-block, against a 0-warning baseline measured at d314fe3.
L2.2: pass — git status --porcelain framwork/ empty after every F-block, artefact-graph.json excluded.
L2.3: pass — delivered.sh verify byte-identical before and after planting docs/deliveries/FAKE-PLAN/plan.md; the corpus excludes docs/ in fact, not only in documentation.
L2.4: pass — impact add-plan-authoring --depth 1 now returns 2, add-framework--done and add-framework--plan; it returned 1 at the branch point.
L3.3: FAILED on the first probe, then passed. Run 1 against a real docs/deliveries/ fixture suffixed [delivered] onto a plan sitting in docs/plans/, reading the mark off the document's Status line, and did not surface the fixture. Run 2, after the fix, correctly left the docs/plans/ plan unmarked but still did not surface the fixture — inconclusive, because the fixture's slug overlapped the real plan's topic. Run 3 used a fixture slug matching no plan on disk: it was found, ranked 1, and suffixed [delivered]. The glob runs and the mark comes from the directory.
F5: Ruling: L3.3 found the delivered mark being inferred from the plan's Status line rather than from the glob that found it; corrected in a follow-up commit (bda959f) rather than re-executing F5, which the resume rule forbids — costs a reader seeing the fix in a commit with no F-block id, which the trailer `Follows F5` answers
F5: Ruling: the same edit had left the two slug examples orphaned under the wrong bullet; restored them under the strip-the-leading-token rule they illustrate, in the same follow-up commit — costs nothing; leaving them would have read as examples of the scoring rule they do not illustrate
F3: Ruling: rejected the side-effect auditor's finding that add-framework--sync.md and the product consistency-agent still cite docs/plans/<id>.md paths for plans 0022 and 0074 — both predate this branch, neither plan was ever closed out by this command, and neither has a docs/deliveries/ directory to repoint at; migrating them needs a decision about pre-index plans that the plan never made — costs those two citations staying fragile, which they already were
F3: Ruling: the product schema add-doc-schemas/references/delivery-index.md still describes `origin` as a directory OR a plan path and claims it survives pruning, which is now false for the internal layer; applied the in-scope half only — done.md STEP 3 states the narrowing where origin is set — and did NOT open a [product] F-block, because "Camada: internal apenas" is a Validated Decision and reversing it is the user's call — costs a reader who starts from the schema rather than from done.md seeing the permissive wording; raised at the publish gate
REVIEW: complete (7 findings, 4 applied, 3 rejected)
F3: Ruling: the REVIEW: line above miscounted; the true tally is 7 findings — 5 applied (4 in commit 8cf862b, plus the rehearsal fixture the diff auditor found, removed from the working tree), 1 rejected (the pre-existing docs/plans/ citations in add-framework--sync and the product consistency-agent), and 1 whose product-layer half is deferred to the user at the publish gate — appended rather than edited, because the ledger is append-only — costs a reader trusting the first count; this line is the correction
F8: Ruling: opened an F-block the plan does not have, tagged [product], to narrow the delivery-index schema's `origin` field — the plan's Validated Decision was "internal only" and its Does NOT Include named the index schema, so this reverses both; the user was presented the finding alone at the publish gate and chose to fix it in this branch, which is the decision path the build command prescribes rather than one I invented — costs a delivery that now spans both layers, which the index entry records as one line carrying its dominant layer
F8: complete (commits 23ef96c..a823c91, build.js exit 0, 0 graph warnings and 0 lint warnings vs 0 baseline, 711 edges, no write under .claude/ or CLAUDE.md)
F2: Ruling: the operator found that "copy, never move" constrains the deletion timing and not the production of the archived file, leaving an agent free to Write a fresh plan.md or summarise the ledger; added an explicit Write/Edit ban, a cp requirement and a cmp proof in STEP 6.1, and the same rule in add-plan-authoring which owns the convention (commit e397e94) — costs a close-out that stops on a cmp mismatch caused by something harmless, which is the safe direction
CLOSE-OUT: gate 2.3 failed on test-cli (20) and (22) — cli/tests/review-no-loops.test.js L3.6 required every line naming `--review-v` to contain "excluding", and F3 added a line naming it as a local original STEP 8 deletes. Ruling: widened the assertion to the property its own comment states — ruled out OR removed, never read or required — and added the negative ban, rather than dropping the companion from STEP 8's removal list, which would have restored the inconsistency the side-effect auditor found. Fourth narrowing of this kind in this file, after L1.3 and L2.9. Costs a line that carries the word "archived" for an unrelated reason slipping past the whitelist; the new negative assertion is what still catches it.
