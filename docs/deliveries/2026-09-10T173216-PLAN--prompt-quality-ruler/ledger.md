# Build ledger — plan: docs/plans/2026-09-10T173216-PLAN--prompt-quality-ruler.md

Ruling: STEP 2 Design [STOP] treated as approved by the invocation — "implemente tudo... quero o PR aberto com o pipeline green" pre-authorises execution and the STEP 9 push — costs an unwanted delivery if the user meant to review the design first, recoverable by closing the PR
Ruling: branch `feat/prompt-quality-ruler` in the main working tree, no worktree — a new agent created inside a worktree is not dispatchable by name until merge (project memory, plan-readback-agent 2026-09-09) — costs a dirtier main clone if the build is abandoned, recoverable by branch delete
Ruling: readback gaps 1-3 (ruler item text, "Family A", what a ruling is) are not plan defects — `add-plan-authoring` forbids restating the design doc, and the ledger/ruling mechanics belong to `add-build-ledger` — costs nothing; the reader had neither document in hand, which is the format working
Ruling: readback gap 4 — a "revision" for the third reader is one dispatch per artefact per command run; a second read over the same build's fix-then-ok edits is NOT a new revision — the alternative caps nothing and reintroduces the loop `add-review-discipline` exists to remove — costs an unreviewed fix inside one build if wrong, caught by the next build's delivery read
Ruling: readback gap 5 — a `blocked` verdict at plan time (F6, audit mode) does NOT halt STEP 3; it becomes an F-block like any other failed item and its question is surfaced in STEP 4's questionnaire, which already stops — a second stop inside STEP 3 would make the audit cost a command round-trip — costs a user seeing an item-4/6 question one step later than they might expect
Ruling: readback gap 6 — F1 writes the matrix AND runs it to observe RED, recording the RED list in this ledger — an assertion nobody ran is not RED-first — costs one extra suite run

F1: Ruling: L3.3 reclassified from guard to RED — building-commands line 341 carries "Maximum ~15 words per item", and F2 already forbids any numeric limit in that file — costs nothing; the line is removed by the F-block that owns the file
F1: complete (commits 9527068..ae81d3e, matrix observed RED 12/15 with 3 guards green, build.js clean, framwork/ empty)
F2: Ruling: "3+ gates explicitly block wrong actions" keeps the property and drops the number — a minimum count is a threshold an author satisfies rather than a property an artefact holds, and it is the same failure mode as a word budget — costs a command with one thin gate passing item 3, caught by item 8
F2: Ruling: building-commands declares `mention:` and not `skill:` for add-framework-development and add-review-discipline — the ruler cites them as owners of neighbouring rules and loads neither — costs a missing dependency edge if one is ever actually loaded from here
F2: Ruling: the prose names @prompt-review-agent before F3 creates it; the declaration is added in F3, not here — the dangling gate rejects an edge to a file that does not exist, so a forward reference must be declared by the block that lands the target — costs a build.js failure in F3 if forgotten, which is exactly what F3's own gate run catches
F2: complete (commits ae81d3e..HEAD, L3.1-L3.4 green, 7/15 matrix green, build.js exit 0 with 739 edges vs 737 baseline and no warning, framwork/ empty)
F3: Ruling: `mode` is carried in the report but changes no step of the method — the two callers differ in what they do with the report, not in what the reviewer reads, and an invented per-mode difference would be behaviour nobody specified — costs a caller expecting mode-specific depth
F3: Ruling: a mention of an agent needs the `@` sigil — build.js `usesTargetId` resolves a bare mention to a skill, so `- mention: prompt-review-agent` dangled as internal/skill/... — costs nothing, caught by the gate in this same block
F3: complete (commits 6caac37..HEAD, L3.5-L3.6 green, 9/15 matrix green, L2.2 confirmed USES_SKILL to building-commands, build.js exit 0 with 216 nodes 745 edges and no warning, framwork/ empty, agent orphaned as the plan expects until F4)
F4: Ruling: the `blocked`-in-audit-mode exception is written HERE and not in add-framework--plan — this skill owns how a verdict is acted on, and a second copy in the command is the single-owner failure the ruler's item 5 exists to catch — costs a reader of the plan command having to follow one pointer
F4: Ruling: four "neither/either" phrasings rewritten for three readers, beyond the plan's F4 list — a count in prose is wrong the moment a third reader lands, and the ruler's item 6 would fail the file I was editing — costs a slightly wider diff than the plan named
F4: complete (commits 040b38f..HEAD, L3.10-L3.11 green, 10/15 matrix green, build.js exit 0 with 746 edges and no warning, framwork/ empty, agent no longer orphaned)
F5: Ruling: "The fourth is one dispatch per ..." stays inside STEP 7 — it indexes the scope, not the report count, and the count it replaces is stated as `3 + N` two lines above — costs a reader confusing scope 4 with a fixed four
F5: complete (commits 730a7c8..HEAD, L3.7-L3.8 green, 11/15 matrix green, build.js exit 0 with 747 edges and no warning, framwork/ empty)
F6: Ruling: the STEP map line for STEP 3 gains "audit the subject" — the map is the first thing an executor reads and a sub-step it never names is a sub-step it skips — costs one more line in the map
F6: complete (commits e1e1682..HEAD, L3.9 green, 12/15 matrix green, build.js exit 0 with 748 edges and no warning, framwork/ empty)
F7: complete (commits a77516a..5bae815, L3.12 green, build.js exit 0 and no warning, framwork/ empty)
F8: complete (commits 5bae815..a8ed055, L3.13 green, no "N words" hit left in the file, build.js exit 0 and no warning, framwork/ empty)
F10: Ruling: the cli/ snapshot update is its own [product] F-block, F10, not part of F9 — cli/ is the product layer and CLAUDE.md the internal one, and the build's layer gate forbids one block writing both — costs an F-block the plan did not name, recorded here and in the completion report
F10: Ruling: the snapshot broke when F3 landed and was caught only at the end-of-build serial run — the plan's per-F-block validation named L3 items and L1, and I ran the ruler matrix per block but the whole suite once — costs a later block discovering an earlier block's breakage, which is what happened and cost one extra commit
F10: complete (commits a8ed055..7ee8eba, build-artefact-graph 50/50 green, full serial suite 969 passed 1 skipped across 42 files)
F9: complete (commits 7ee8eba..8649e05, L3.14 green, matrix 15/15, build.js exit 0 with 748 edges and no warning)

REVIEW: complete (14 findings, 8 applied, 6 rejected)

Ruling: the named dispatch of @prompt-review-agent failed with "Agent type not found", so all four audit scopes ran as generic read-only subagents told to read the agent file and follow it as their own instructions — the F5 gate covers exactly this and the memory predicted it — costs nothing observable; the reports came back in the agent's own format
Ruling: each of the nine ruler dispatches carried one substitution, allowing `node scripts/graph.js` where the agent said MCP-only — the CLI is the same engine, and without it family A would have been `not verified` nine times — costs a per-dispatch workaround, which finding F11/item-6 then removed from the workaround and put in the agent
Ruling: REJECTED add-framework--plan L169-172, the layer paths table duplicating add-plan-authoring (item 5, medium) — pre-existing, in a STEP this delivery never touched, and the plan excludes fixing existing artefacts — costs a real item-5 failure staying on disk; it is the first candidate for the follow-up audit plan
Ruling: REJECTED add-framework-development L649/L683, the two `## 8.` headings (item 2, medium) — the plan defers them by name, and the ruler's own item-2 Not-expected example quotes them as present, so fixing them would falsify the example in the same commit — costs the same defect staying on disk; a follow-up must fix the heading and the example together
Ruling: REJECTED add-framework-development L662-665, restating building-commands' gate and rules format (item 5, medium) — pre-existing, and the delegation at L667 already sits beside it — costs a real item-5 failure staying on disk, same follow-up
Ruling: REJECTED the conformance auditor's note that CLAUDE.md names @prompt-review-agent alongside building-commands where the plan named only the skill (low) — the row points a reader at both halves of the mechanism, and the plan's wording was a summary not a contract — costs nothing
Ruling: REJECTED the diff auditor's request to amend the plan for F10 as "cosmetic only" — it was applied, not rejected, because a plan whose Scope stops at F9 while ten files changed is unreadable to anyone without the ledger — recorded here for the count
Ruling: the line between applied and rejected is mechanical-versus-structural — a dead pointer costs the next reader a wrong path and its fix invents no decision, so it lands now; a duplication is a design change about who owns a rule, so it gets its own plan — costs four known defects staying on disk with no owner until that plan exists

Ruling: the local bats run was stopped rather than waited on — CI's test-scripts job passed in 56s and the diff carries zero `.sh` files, so the local run could only re-answer a question already answered — costs nothing; if a shell file ever enters this branch the local run becomes mandatory again
PUBLISH: PR #49 opened on feat/prompt-quality-ruler, six CI checks green (test-cli 20, test-cli 22, test-scripts, CodeQL, Analyze actions, Analyze javascript-typescript)

F13: Ruling: the second prompt pass is kept and narrowed to `confirm` rather than deleted — the user chose it over cutting either read, and a narrow confirmation answers a bounded question the full re-tick does not — costs one extra dispatch per fixed artefact
F13: Ruling: `confirm` re-ticks items 1 and 2 beyond the cited ones — an edit that adds a name or moves a section is the edit that breaks those two, and both are tool-answered so the cost is near zero — costs a slightly wider second pass than "only the cited items"
F13: Ruling: the cap formulation stays out of the agent after cli/tests/review-no-loops.test.js L4.3 flagged it — that guard owns the single-owner rule for "no third" and "at most twice", and the agent is a leaf that should not restate a count — costs a reader of the agent following one pointer to learn the cap
F13: Ruling: L3.10 was rewritten rather than deleted when its pinned phrase "per revision" went — the phrase described a count that allowed two full ticks, and L3.17 now pins the rule that replaced it — costs nothing; the comment records why
F13: complete (commits 1f41822..HEAD, matrix 18/18 with L3.16-L3.18 added, full serial suite 972 passed 1 skipped across 42 files, build.js exit 0 with no warning, framwork/ empty)

Ruling: F11 and F12 carry no `complete` line while F13 does — the first two landed as STEP 7.3 review-finding edits, which the build records under the `REVIEW:` line, and F13 landed as a normal block after the user's refinement request. Both are traceable through their commit trailers, but the ledger reads inconsistently — costs a reader of this archive having to join REVIEW: to two commits by hand. Gate 2.2 is unaffected: it asks whether F1-F10, the plan's own blocks, are complete, and all ten are
CLOSE-OUT: gate 2.2 passed on F1-F10; F11, F12 and F13 are extra blocks, reported and not required
CLOSE-OUT: gate 2.3 passed on CI evidence, six checks success on ce2dd6c (the archive commit); STEP 6 committed and pushed the archive and the index entry
CLOSE-OUT: STEP 7 merge REFUSED — "the base branch policy prohibits the merge". Cause verified, not assumed: main's ruleset sets require_last_push_approval: true with required_approving_review_count: 0, so the pusher cannot approve their own last push. mergeable=MERGEABLE, mergeStateStatus=BLOCKED, every check success. Not a gate failure
Ruling: STOPPED at STEP 7 instead of using `gh pr merge --admin` — project memory records that bypassing this ruleset is the owner's decision and never taken alone, and the command's own STEP 7 says report the refusal and stop — costs the close-out staying open until the owner picks --auto or --admin
CLOSE-OUT: STEP 8 NOT RUN, per STEP 7's instruction. The plan, the ledger and the design doc remain in docs/plans/ and docs/brainstorming/; the branch and its remote are intact
CLOSE-OUT: ON RESUME, DO NOT re-run STEP 3 or STEP 6. The entry and docs/deliveries/<id>/ are already committed on this branch. Gate 2.1's table has no row for "PR open AND entry present" — that state is this one, and the only work left is the merge and then STEP 8
