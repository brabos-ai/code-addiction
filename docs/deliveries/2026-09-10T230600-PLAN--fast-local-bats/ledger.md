# Build ledger — plan: docs/plans/2026-09-10T230600-PLAN--fast-local-bats.md

READBACK: complete (cold read matches the plan's scope, order and F-block set; six gaps marked, all implementation detail the plan left open, ruled below)
F1: Ruling: the module is CommonJS exporting pure decision functions, with a main guarded by require.main — matches scripts/inventory.js, which cli/tests/inventory.test.js already imports through createRequire — costs a rewrite of F1's imports if the wrapper is later made ESM
F1: Ruling: the Docker-absent message must literally contain the string `CODEADD_BATS_RUNNER=native` and the word CI, taking the readback's stronger reading of "names both the override and CI" — costs nothing if the weaker reading was meant, since a message that names them also defers to them
F3: Ruling: Docker reachability is probed with `docker version --format {{.Server.Version}}`, treating a non-zero exit or a spawn error as unreachable, with no custom timeout — it queries the daemon rather than only the client, which `docker --version` does not — costs a wrong verdict on a daemon that is starting but not yet serving, which reports unreachable and is the safe direction
F3: Ruling: image presence is checked with `docker image inspect <tag>` before each run, and orphaned tags from earlier Dockerfile revisions are never pruned — pruning is a destructive operation on a shared daemon and is out of this plan's scope — costs unbounded disk growth across many Dockerfile edits, which `docker image prune` handles by hand
F3: Ruling: both branches spawn through a shell with the command as a single string — the native branch reproduces byte-for-byte what npm runs today, glob expansion included, and the container branch needs a shell for the same glob inside the image — costs nothing that the current `test:scripts` does not already cost
F5: Ruling: kept the plan's strict F5 → F6 → F7 chain even though the readback correctly observes their Produces/Consumes show no dependency among them — three sequential single-file edits cost nothing to serialise and the ledger reads in one order — costs a little parallelism that was never available anyway
F2: Ruling: L1 is expected to remain fully RED after F2 and turn GREEN only at F3, confirming the readback's reading — the plan's per-block table re-runs L1 at F2 as a checkpoint, not as a pass expectation — costs a false stall if a future reader treats the F2 checkpoint as a gate
F1: Ruling: L3's five assertions live in the same file as L1 rather than in a file of their own — all ten are vitest checks over the same delivery and the plan assigned L3 to no block — costs one file to split if L3 later needs a different runner
F1: complete (commits 48eb87d..6ec4fcc, 14 of 14 RED for the right reason; full cli suite 984 passed unchanged from baseline; build.js exit 0, 0 warnings against a 0 baseline; framwork/ untouched)
F2: complete (commits 6ec4fcc..2884398, image builds; verified inside it: no apt bats on PATH, mounted ./node_modules/.bin/bats reports 1.13.0, git has no global user.name; L1 still fully RED as ruled; build.js exit 0, 0 warnings; framwork/ untouched)
F1b: Ruling: opened F1b [product] rather than editing cli/tests/ under F3's [internal] tag — running the wrapper showed buildCommand's single-string contract cannot survive cmd.exe, and the fix spans one product file and one internal file — costs one extra commit against a plan that named seven blocks
F1b: Ruling: the graph-mcp "exposes history" failure in the full serial run is pre-existing flakiness, not this block's — it timed out at 5029ms under serial load and passes 10 of 10 in isolation, and it passed in the F1 baseline run before either file of this block existed — costs a missed real regression if the isolation pass was itself luck
F1b: complete (commits 2884398..0da43ab, full cli suite 994 passed; 5 failed = 4 expected RED for F4-F7 plus the ruled pre-existing flake; build.js exit 0, 0 warnings)
F3: complete (commits 0da43ab..dc20b4b, L1 10 of 10 GREEN; L2 all five: 386 passing in 33s at four jobs, 386 passing in 2m16 at one, bats 1.13.0, qa-preflight green, exit 0 both runs; build.js exit 0, 0 warnings; framwork/ untouched)
F4: complete (commits dc20b4b..5ea3b67, npm run test:scripts announces the docker runner, 386 passing in 35s, exit 0; build.js exit 0, 0 warnings; framwork/ untouched)
F4b: Ruling: opened F4b [product] rather than editing cli/tests/ under F4's [internal] tag — same layer boundary as F1b — costs one extra commit
F4b: Ruling: L3.1's own regex was the defect, not package.json — `\bbats\b` matches inside "run-bats.js" because hyphen and dot are word boundaries, so the check flagged the wrapper that replaced the direct invocation — costs a missed second bats script if the tighter pattern is too narrow
F4b: complete (commits 5ea3b67..HEAD, full cli suite 996 passed; 3 failed = L3.3 L3.4 L3.5, the expected RED for F5 F6 F7; the graph-mcp flake did not recur, confirming the F1b ruling)
F5: complete (commits 67932fd..ffedcb2, L3.3 GREEN, serial-vitest gate byte-unchanged; build.js exit 0, 0 warnings; framwork/ untouched)
F6: complete (commits ffedcb2..620f30d, L3.4 GREEN; grep sweep over .claude/ and CLAUDE.md finds no surviving "over an hour" or "63 seconds"; build.js exit 0, 0 warnings; framwork/ untouched)
F7: complete (commits 620f30d..HEAD, L3.5 GREEN, 15 of 15 GREEN; inventory --check reports the block current; build.js exit 0, 0 warnings; framwork/ untouched)
F1b: Ruling: SUPERSEDES the F3 shell ruling above — running the wrapper showed a single shell string cannot cross cmd.exe, so the container branch spawns argv with no outer shell while the native branch keeps the string — the earlier line stands as written because this ledger is append-only — costs a reader who stops at the first ruling
F1b: Ruling: L1.9 as reshaped and L1.10 as written were authored AFTER the wrapper existed, not confirmed RED first — the defect was found by running the wrapper, which is the only way it could have been found — costs the RED guarantee on exactly those two assertions, and the plan's own Reviewer Handoff names this as the gap to hunt
F1b: Ruling: the F1b completion line's "5 failed" was measured against a working tree already holding the uncommitted wrapper — a clean checkout of 0da43ab fails 14, not 5 — costs nothing to the delivery and everything to that line as evidence
REVIEW: Ruling: rejected changing framwork/.codeadd/scripts/tests/run-tests.sh, which still calls npx bats directly and is a second live entry point — it ships to users' projects where scripts/run-bats.js does not exist, so pointing it at the wrapper would break it there — costs a slow path surviving for anyone who finds that file
REVIEW: Ruling: rejected the single-owner finding at add-framework--done.md L429/L431, where the delivered-home content is restated instead of delegated — it predates this delivery and the plan scoped one paragraph of that file — costs a real defect staying until something scopes it
REVIEW: Ruling: rejected widening the new .sh gate to cover scripts/smoke-test.sh, which nothing references — the gate codifies what the plan decided, and adopting an orphaned seam-test is a design decision no plan has taken — costs the gate grading unit tests only, which its own text does not overclaim
REVIEW: Ruling: rejected editing docs/deliveries/ and docs/changelog/ files that quote the retired figure — CLAUDE.md's durability split makes those dated narrative, and rewriting an archived delivery is the one thing add-plan-authoring forbids outright — costs a reader following an old pointer to a paragraph that changed
REVIEW: Ruling: rejected the forced-native cmd.exe finding — npm already ran that exact string through cmd.exe before this wrapper existed, so shell:true reproduces it rather than regressing it — costs a broken escape hatch if npm was in fact using a different shell
REVIEW: Ruling: rejected the finding that add-framework-internal-layer describes support scripts as scripts/*.js while scripts/ now holds a Dockerfile — the table was already approximate, holding three .sh files and a stray log — costs one more inaccuracy in a description nothing reads as a contract
REVIEW: Ruling: rejected adding coverage for main(), which leaves L1.3's exit code and L1.6's print-order untested — F1's own "must not shell out to Docker" forbids it and the plan wrote that constraint deliberately — costs the L1.6 mitigation in the risk register being weaker than the plan claims
REVIEW: Ruling: rejected automating L4.2, the simulated no-runner ledger path — it is behavioural about the build command's process, not about any file this delivery wrote — costs an unproven acceptance level, partly offset by L3.3 now asserting the three-part ruling form
REVIEW: Ruling: rejected removing the timing figures from run-bats.js, bats.Dockerfile and the test file header — each is rationale at the point of use, not a claim a gate rests on, and the gate-bearing copies are the ones now asserted absent — costs three comments that will age
REVIEW: Ruling: rejected treating the plan's Status: draft as a review finding — STEP 8 owns that transition — costs nothing
REVIEW: L2 re-run through the real entry point after F4, on win32 with the docker runner: npm run test:scripts 386 passing in 36s exit 0; CODEADD_BATS_JOBS=1 386 passing exit 0; npm run test:scripts -- <one file> 7 passing exit 0; CODEADD_BATS_RUNNER=Docker exits 2 with its message
REVIEW: complete (24 findings, 11 applied, 13 rejected)
