# Build ledger — plan: docs/plans/2026-09-11T014333-PLAN--product-close-out-parity.md

Baseline: graph warnings 0 (ADD_GRAPH_WARNINGS=1 node scripts/build.js, exit 0)
Baseline: cli suite 45 files, 1017 passed, 1 skipped, 0 failed, 133s serial
Baseline: bats 386 tests, exit 0, container runner resolved
Baseline: node scripts/graph.js orphans = 24
Ruling: graded every F-block against zero cli failures, not against the plan's "two expected graph-query failures" — vitest-fixture-scope merged as PR #52 between planning and execution and its F5 fixed both timeouts, so the baseline measured green — costs nothing if wrong, the criterion is strictly stricter than the plan's
Ruling: treated the plan's "land vitest-fixture-scope first" constraint as satisfied rather than pending — it is merged, indexed and archived — costs a stale test-count conflict if wrong, which the green baseline already disproves
Readback: complete — order independently derived from Produces/Consumes matched the stated Execution Order exactly, including the F5 non-dependency the plan itself flags
Ruling: built the recovery route so that MORE THAN ONE docs/features/[NNNN][L]-*/ directory in the merge commit STOPS and asks which, never picks one — the readback found the plan assumes exactly one match and an epic merge can touch several; every other resolver in this repo stops on ambiguity rather than guessing — costs one extra question on an epic recovery if wrong, against a delivery indexed under the wrong feature if it guessed
Ruling: recorded the three-document design set as unarchivable by the current close-out rather than reducing it — add-plan-authoring's Delivered Home maps exactly one design.md, sourced from "the docs/brainstorming/ file the plan's Context table names", and this plan's Context table names three — costs the umbrella and one subtopic never reaching main if nothing acts on it, so it is carried to STEP 7 as a finding rather than silently resolved here
Ruling: re-measured the cli baseline in the WORKTREE, not the primary checkout where I first took it — the build runs here and a baseline from another tree grades the wrong thing — costs nothing for F1, whose three failures I proved were caused by the deletion and not pre-existing, but every later block is graded against the worktree figure
Ruling: opened F1b [internal] for CLAUDE.md rather than writing it under F1's [product] tag — deleting a product script leaves the generated inventory block stale and three cli tests red, and the layer boundary forbids a product block from touching CLAUDE.md; the build's own rule says a path outside the current tag opens a new block carrying that path's tag — costs one extra commit per artefact-set change if wrong, against either a layer violation or seventeen blocks gated on a red suite
Ruling: the same F1b pattern will repeat at F15, which adds a skill to the inventory — deciding it once here rather than re-deriving it there — costs nothing; the alternative was deferring every sync to STEP 8 and running every remaining block against a knowingly red gate
Ruling: removed the whole `Reference scripts that do not exist in this repo` NEVER item instead of stripping its example — with the script deleted the rule protects nothing that two mechanisms do not already enforce, a call failing at runtime and build.js rejecting a dangling uses: target — costs a re-added call nobody can make if wrong
Ruling: updated the graph node snapshot in place (script 18->17, total 216->215, declares unchanged at 104) rather than treating the failure as a regression — the test's own comment mandates exactly this for an intended count change and states it lives in one place so one change does not scatter failures — costs a silently wrong count if the deletion was not intended, which the plan's F1 states it is
F1: complete (commits c430da5..1956ac8, build.js 0 warnings vs baseline 0, orphans 24 vs 24, bats 375 vs 386 with the 11-test drop fully accounted to the deleted suite, L1 RED then GREEN)
F1b: complete (commits 1956ac8..393847b, inventory.js --check exit 0, cli suite 46 files 1022 passed 1 skipped 0 failed vs worktree baseline)
Ruling: split L2 by mechanism — the seven behavioural assertions went to converge-gates.bats where a script's behaviour is testable, the five content assertions to the cli matrix — the plan listed all twelve under one level as if one mechanism could carry both — costs nothing; every assertion the plan named still exists, in the home that can run it
Ruling: scoped F2's sweep to five artefacts and moved add.done's two edits to F3 — F2 would otherwise have written "whether 4.0, 4.1, 4.2 and 4.3 pass" and "every one of them is read below" into a file where 4.3 does not exist and the fifth gate is not read, failing ruler item 2 — costs one more file in F3's diff if wrong, against a committed block leaving a pointer that resolves to nothing
Ruling: rewrote converge-gates.bats:744's historical comment to drop its denominator instead of updating it to 5/5 or exempting it — the note describes a past defect where 4/4 was correct, so updating falsifies the record and exempting lets the number go stale at the next gate — costs a slightly vaguer historical note if wrong
F2: complete (commits 393847b..39d531b, build.js 0 warnings vs baseline 0, bats 382 = 375 + the 7 new gate cases, cli 1027 passed 0 failed = 1022 + the 5 new content assertions, L2 RED then GREEN on all 12)
Ruling: extended F3 past the plan's wording to also fix add.done's STEP 4 summary line and its re-derive prohibition — the plan named only the 4.3 block and the parse list, but a gate whose file is absent from "DO NOT re-derive by reading X, Y, Z" reads as permission to re-derive it — costs two lines of diff if wrong
Ruling: skipped the bats gate on F3 — it touched one .md and the cli matrix, no .sh — costs nothing; the plan conditions that gate on framwork/.codeadd/scripts/*.sh and F3 matched none
Note: writing the two-character backslash-n sequence inside a python heredoc corrupts the file before python reads it. It bit converge-gates.sh silently (bash -n still passed on a quoted literal newline) and the cli matrix loudly (parse error). Build the backslash by concatenation instead.
F3: complete (commits 39d531b..9343699, build.js 0 warnings vs baseline 0, cli 1033 passed 0 failed = 1027 + the 6 new L3 assertions, L3 RED then GREEN)
Ruling: made do_cleanup switch to main conditionally rather than giving --cleanup a branch argument — the PR route calls it from the feature branch because gh merges server-side and the local HEAD never moves, while --merge calls it already on main; one conditional serves both and the plan specified neither — costs a wrong-branch checkout if wrong, which the bats case pins
Ruling: rewrote two of my own new bats cases that passed for the wrong reason — an unrecognised flag also errors on main, so asserting STATUS=ERROR proved nothing about the mode existing — replaced with the error that actually fires plus a case proving an unknown flag falls through to context mode — costs nothing; it turned two decorative assertions into two that bite
Finding for STEP 7: merge_guards' "Already on $MAIN_BRANCH" guard is near-unreachable. The branch-ID check above it fires first because main carries no [NNNN][L], and the pre-existing test names this in its own title ("fails when already on main (no ID)"). Not repaired here — out of this plan's scope, and removing a guard is a decision the plan never made.
F4: complete (commits 9343699..9f639c2, build.js 0 warnings vs baseline 0, bats 388 = 382 + 6 net new cases with 0 deletions in the suite, cli 1033 unchanged because no cli-visible file moved)
Ruling: gave INDEX_ENTRY three values instead of the plan's two — no-index means no docs/delivered.jsonl at all, absent means the file exists without this id — costs a first delivery reading as a lost one if collapsed, which is the opposite error from the duplicate this probe exists to catch
Ruling: parsed gh's JSON with node rather than sed — mergeCommit is nested and a regex over JSON survives only until a field moves; node is already required by converge-gates.sh, which the same command runs, so the dependency set does not grow — costs one process spawn per close-out
Ruling: declared node and gh in done.sh's dependency header, which previously named only get-main-branch.sh — a script whose contract omits a dependency is a script whose failure mode is unexplained — costs nothing
F5: complete (commits 9f639c2..7a6b4ef, build.js 0 warnings vs baseline 0, bats 402 = 388 + 14 new probe cases, cli 1033 unchanged because no cli-visible file moved)
Ruling: gave --cleanup the merge sha as an argument rather than deriving it — a squash creates a new commit so the branch tip is never an ancestor of main, and no ancestry walk can recover it; --merge passes its own, the PR route passes gh's, and absent it refuses — costs one argument at every call site if wrong, against deleting a branch whose work never landed
Ruling: corrected the plan's claim that the push dry-run catches branch protection. It does not — measured: git push --dry-run is client-side and never reaches the remote pre-receive hook where GitHub enforces it. Kept the dry-run for what it does catch (stale local main, unreachable remote, transport auth) and pinned the limit in a bats case so nobody later believes otherwise — costs a false sense of cover if left unstated, which is why it is stated
Ruling: updated one F4 bats case instead of leaving it red — F6 changed --cleanup's contract to require the merge sha, so the old call was asserting a rule that no longer exists; the product-layer skill's own line is "update it, and comment why" — costs nothing
F6: complete (commits 7a6b4ef..d83e6bd, build.js 0 warnings vs baseline 0, bats 408 = 402 + 6 new cases, cli 1033 unchanged, done.bats 54 cases 0 failing)
Ruling: renumbered two prose citations of "STEP 17" inside add.build.md that the plan did not name — both meant Completion, and left alone they would point a reader at the new publish step looking for the rulings table — costs two lines of diff if wrong
F7: complete (commits d83e6bd..e650194, build.js 0 warnings vs baseline 0, cli 1039 passed 0 failed = 1033 + the 6 new L4 assertions, L4 RED then GREEN, fragments verified to cite only STEP 15 and 11.3)
Ruling: routed MERGED_ON_MAIN=unknown as NOT merged — the plan crossed "merge state" against the entry without saying what the third value does, and treating unknown as merged would run the Recovery route, which writes on main, because origin/<main> could not be read — costs one extra Normal-route run on an unreachable remote if wrong, against writing to main on a guess
Ruling: added a prohibition block naming the two calls that would recompute the routing facts (gh pr view, reading delivered.jsonl) rather than trusting the prose "read the probe" — the failure mode is a second reader of one tree, which is the exact thing converge-gates.sh was created to stop — costs four lines
F8: complete (commits e650194..e592756, build.js 0 warnings vs baseline 0, cli 1044 passed 0 failed = 1039 + the 5 new L5 assertions, L5 RED then GREEN)
Ruling: asserted the ORDER of the SHA comparison against the verdict read, not merely that both exist — a comparison placed after the read does not undo the read, and the plan's wording ("compare the SHA before reading the verdict") is only checkable as an order — costs nothing
Ruling: named the four non-pass check conclusions individually instead of restating "only success is a pass" — the general form is unverifiable and the specific one is — costs four words
Ruling: repaired STEP 8's "the script owns the full sequence", which this block made false — done.sh owns every LOCAL git write on both routes, and gh pr merge is not one; stating the distinction is what stops a later reader moving the call back into the script — costs five lines
F9: complete (commits e592756..feb7195, build.js 0 warnings vs baseline 0, cli 1050 passed 0 failed = 1044 + the 6 new L6 assertions, L6 RED then GREEN)
Ruling: implemented the readback's multi-feature finding as a STOP-and-ask, as ruled before F1 — the diff naming more than one docs/features/[NNNN][L]-*/ directory prints every candidate rather than taking the first — costs one question on an epic recovery, against filing a delivery under the wrong feature
Ruling: named FOUR skipped steps on the resume route where the internal equivalent names three — STEP 5's QA promotion is product-specific and naming three would have re-run it silently — costs nothing
Ruling: stated that recovery cannot use done.sh at all rather than leaving it implied — it runs on main, where context mode needs a branch id and merge mode refuses — costs three lines, against a run that tries and fails confusingly
F10: complete (commits feb7195..f095464, build.js 0 warnings vs baseline 0, cli 1055 passed 0 failed = 1050 + the 5 new L7 assertions, L7 RED then GREEN)
F11: complete (commits f095464..83f1e71, build.js 0 warnings vs baseline 0, cli 1060 passed 0 failed = 1055 + the 5 new L8 assertions, L8 RED then GREEN)
Ruling: added "do not allocate a second CHG id" to F12's prohibition, which the plan did not name — complementing without it would keep the narrative and still mint a second identifier for one delivery, which is the same defect from the other side — costs one line
F12: complete (commits 83f1e71..5ea1673, build.js 0 warnings vs baseline 0, cli 1065 passed 0 failed = 1060 + the 5 new L9 assertions, L9 RED then GREEN)
Ruling: replaced add.pull-request's NEVER item about the idempotency guard rather than deleting it — the guard it named is now a complement, so the sentence was false, but the thing it protected (one CHG id per delivery) is still worth forbidding — costs one line
Note: THIRD vacuous assertion caught in this build. F4 had two (an unrecognised flag also errors on main, so STATUS=ERROR proved nothing) and F13 had one (looked for "Write to:" where the file says "Write to"). The pattern is the same every time: I wrote the expected string from memory instead of copying it out of the file. Switched to literal comparison over regex wherever possible — a regex mangled by heredoc escaping matches nothing and passes silently.
F13: complete (commits 5ea1673..224be43, build.js 0 warnings vs baseline 0, cli 1071 passed 0 failed = 1065 + the 6 new L10 assertions, L10 RED then GREEN)
Ruling: declared /add.plan as mention: rather than command: in add.build's uses block — the prose points AWAY from it, naming what it already did and forbidding a halt that returns there, and command: would assert a handoff that does not exist — costs a wrong edge in the graph if wrong
Ruling: opened F14b [internal] for web/public/artefact-graph.mmd, the second time the layer rule has forced a companion block (F1b was the first, for CLAUDE.md) — a product block changed the graph and the checked-in diagram lives at repo root — costs one extra commit per graph-changing product block
F14: complete (commits 224be43..F14, build.js 0 warnings AFTER two undeclared references were fixed — it failed first, exit 1 with 2 warnings, and the gate held, cli 1079 passed 0 failed = 1071 + the 8 new L11 assertions)
F14b: complete (commits F14..1b083fd, graph.js mermaid --write, cli 1079 passed 0 failed)
Ruling: named commands in the new skill's prose as /add.x rather than {{cmd:add.x}} — the graph sniffer requires the leading slash for a command node, and {{cmd:}} is the resource-path form for pointing at a file; add-plan-review, the closest sibling in subject, already writes it this way — costs nothing
Ruling: declared add-code-review, converge-gates.sh and qa-evidence.sh as mention: rather than skill:/script: — the skill neither loads nor runs them, it only points at them — costs three wrong edges in the graph if wrong
Ruling: deferred L13 (the internal sibling note) out of F15 and into F16 — it was written a block early and would have held F15's gate red on work F16 owns; RED-first means immediately before the block, not one before — costs nothing
Ruling: opened F15b [internal] for CLAUDE.md, the third companion block this layer rule has forced (F1b, F14b, F15b) — costs one extra commit per artefact-set change, and the pattern is now established rather than re-derived each time
F15: complete (commits 1b083fd..F15, build.js 0 warnings after four refusals all legitimate, cli 1086 passed 0 failed = 1079 + the 7 new L12 assertions, node snapshot skill 51->52 and declares 104->105)
F15b: complete (commits F15..3e7014b, inventory.js --check exit 0)
Ruling: wrote the sibling note to name WHERE the two skills disagree and why, not merely that a sibling exists — a bare acknowledgement is an invitation to reconcile them, which would break both — costs twenty lines of comment
F16: complete (commits 3e7014b..e4a466c, build.js 0 warnings vs baseline 0, cli 1090 passed 0 failed = 1086 + the 4 new L13 assertions, lane proof: git status --porcelain framwork/ empty)
Ruling: defined "restates the verdict table" as the three verdict names appearing in sequence within a text window, not as the presence of the word blocked — a word-level ban would have forced deleting legitimate single references, and a sequence test lets a reference survive while catching a copy — costs a slightly looser test if wrong
F17: complete (commits e4a466c..39293ab, build.js 0 warnings vs baseline 0, cli 1095 passed 0 failed = 1090 + the 5 new L14 assertions, L14 RED then GREEN)
Ruling: declared add-plan-authoring as mention: in the brainstorm command — the new text cites it as the owner of the parallel plan-set convention and never loads it — costs a wrong edge if wrong
F18: complete (commits 39293ab..37ee5ba, build.js 0 warnings after one legitimate refusal, cli 1100 passed 0 failed = 1095 + the 5 new L15 assertions, lane proof: git status --porcelain framwork/ empty, standalone form intact at 3 occurrences)
Ruling: merged some ruler dispatches — add.new+add.brainstorm, add-commit+add-ecosystem, and the three internal artefacts each went in one call instead of one per artefact — the command says one dispatch per .md artefact and 17 concurrent agents risked flooding; every artefact still got its own eight-item tick and its own report section — costs a shared context between two artefacts that might have been read independently
Ruling: REJECTED add.plan-to-ready's "declare /add.new in uses:" finding — the two prose mentions are one inside a fenced block and one in {{cmd:}} form without the leading slash, so the sniffer sees neither; declaring it would create a PHANTOM EDGE warning and fail the build. The add.pull-request auditor simulated the same gate and reached the opposite conclusion, and a simulated gate outranks an asserted one — costs a missing graph edge that the gate itself does not want
Ruling: REJECTED the same finding's twin on add.done (declare status.sh) for the identical reason — both its mentions are fenced — costs the same
Ruling: REJECTED both add-subagent-driven-development contract findings (fix-agent's and reviewer-agent's input/report shapes) and the add-qa-migration template quote — all three are pre-existing, in text this delivery never opened, and the verdict was `blocked` because they need a decision about which contract the fix loop should speak. That decision is not this plan's to make — costs three real contract mismatches left standing, recorded here so they are not lost
Ruling: REJECTED "add.build has no ## Rules section" and "add.build restates the Four Hard Stops" — both pre-existing, both real ruler failures, neither caused nor touched by this delivery. Adding a Rules section to a 1200-line command is its own piece of work — costs two ruler items staying red on that artefact
Ruling: REJECTED add-wiki-maintenance's stale "add.done STEP 4.9 / 4.5" pointers and the same drift in add-ecosystem and docs.astro — pre-existing since a7af8f6, in artefacts outside this plan — costs three dangling step references
Ruling: REJECTED add.pull-request's /add.release pointer (names no artefact anywhere) and its MODEL:-before-LANG: header order — both pre-existing and confirmed untouched by this delivery's diff
Ruling: REJECTED "docs.astro does not list the new skill" — that page is regenerated by /add-framework--sync, which owns it; writing it by hand here would be edited over
Ruling: REJECTED add-commit's "carries the result as a trailer" vs "body lines, not git trailers" contradiction, and add.plan-to-ready restating add-commit's receipt rule in full — both pre-existing; the half-edit I introduced in that area (five vs six) IS fixed
Ruling: REJECTED the three internal-layer nits (prior_deliveries not in framework-discovery-agent's contract, a Rules item restating step order, the ALSO ALWAYS third bucket) — all pre-existing in files this delivery opened for one line each
Ruling: corrected the plan's own L10.5 expectation. It claimed a cross-layer uses: on the internal sibling would fail build.js with a dangling target. It does NOT: a node of that name exists in that layer too, so the declaration resolves to the file itself and the sniffer skips self-reference. build.js stayed green; only the cli matrix caught it. Recorded because the plan asserted a second guard that does not exist
Ruling: ran the plan's L10 mutation level, which the build had skipped entirely. Seven mutations, seven caught — but M4 (restoring the 6.3 skip) went GREEN first, because L9.1 forbade two old phrasings instead of requiring the new behaviour, and the mutation wrote the same defect in different words. Strengthened, then caught — costs nothing; it turned a decorative level into a real one
Ruling: the ledger's per-block evidence lines say "the N new L9/L10 assertions" meaning the TEST FILE's levels, not the PLAN's L9 and L10. The renumber was never ruled on. The plan's L9 (behavioural acceptance) and L10 (mutation) are: L10 now run in full, L9.2 (a second close-out writes no second line) is NOT asserted and is recorded as an accepted gap — costs an unproven behavioural claim, named here rather than left to read as covered
REVIEW: complete (43 findings across 13 auditors plus 2 from the graph, 25 applied, 18 rejected with the rulings above)
Publish: pr-opened https://github.com/brabos-ai/code-addiction/pull/55
