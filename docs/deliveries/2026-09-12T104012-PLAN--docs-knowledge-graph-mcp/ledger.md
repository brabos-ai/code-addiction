# Build ledger — plan: docs/plans/2026-09-12T104012-PLAN--docs-knowledge-graph-mcp.md

Branch: feat/docs-knowledge-graph-mcp
Baseline (before F1): `ADD_GRAPH_WARNINGS=1 node scripts/build.js` exit 0, ZERO graph warnings, zero LINT warnings.

READBACK: complete (7 gaps marked, all answerable from the design set — rulings below)

F0: Ruling: `mcp/` is written as ESM with the `.mjs` extension — the root package.json has no `"type"` field (CJS) while `cli/package.json` declares `"type": "module"`, so the same `.js` source would be read two different ways once `build.js` copies it into `cli/src/mcp/` — costs a rename of every file in `mcp/` plus its importers if a nested `mcp/package.json` turns out to be preferred
F0: Ruling: readback Gap 1 — `## Observations` takes its grammar from the design's Part 1 example, `- [<category>] <text> #<tag>`, which the plan names without restating — costs an unused section shape if the design example was illustrative rather than normative
F0: Ruling: readback Gap 2 — F18 reads frontmatter only for the ELIGIBILITY check (`type:` present), and reads the body for the four body-sourced harvests; the design's Part 4 states both halves — costs three of five harvest sources if the strict frontmatter-only reading was meant
F0: Ruling: readback Gap 4 — existing `related.md` files in a brownfield project are left on disk untouched; F18's additive-only contract forbids deleting a user file — costs a superseded file per past hotfix, visible and harmless
F1: Ruling: baseline corrected — the session's first build read 778 edges against a tree carrying three uncommitted files the user then reverted; the branch point is 777 edges, 0 graph warnings, 0 LINT warnings — costs nothing, the warning baseline (zero) is what the gate compares against and it did not move
F1: Ruling: L1.2 ("the build fails when a `## Relations` line names an id that resolves to no document") cannot mean `scripts/build.js`, which never sees a user's `docs/` — it is implemented as Validation Gate check 7 in `add-doc-schemas`, a FAIL distinct from check 6's WARNING, and asserted as schema text the way `plain-language-rule.test.js` already asserts schema text; the executable half lands in F7's indexer and F19's close-out gate — costs a weaker gate at F1 if the plan meant a new build.js pass over user docs, which build.js has no access to
F1: Ruling: USER SCOPE ADDITION, mid-build — the user asked for coverage of real MCP usage scenarios, not only the plan's minimum parity levels; F7 and F8 gain end-to-end suites over a fixture corpus covering all ten actions, both corpora, the stdio protocol and its error paths — costs build time in T2 and nothing else
F1: complete (commits cc74e6f..7fbac36, build.js exit 0 / 0 warnings / 777 edges unchanged, L2.1 22 dependants resolve, cli suite 47 files 1108 passed serially)
F2: complete (commits 7fbac36..9ccf8d0, build.js exit 0 / 0 warnings, docs-knowledge-graph + hotfix-review-0073 + plain-language-rule 59 passed)
F3: complete (commits 9ccf8d0..91e337c, build.js exit 0 / 0 warnings, docs-knowledge-graph 16 passed)
F4: Ruling: STEP 12 is deleted rather than repurposed, and the two validation gates fold into one, so the command runs 14 steps instead of 16 — the plan said STEP 12 stops writing `related.md` without saying what replaces it, and its `Cross-update` job is dead work under a graph: a `caused_by` declared on the hotfix already yields the inbound edge on the feature, so writing the inverse by hand records the same fact twice — costs a renumber back if a later plan wants a cross-update step
F4: Ruling: `add-ecosystem/SKILL.md`'s add.hotfix row is in F4's sweep though the plan's Impact table omits it — it stated the command "documents relationships in related.md", which F4 makes false — costs nothing; it is one row in the same layer and the same F-block
F4: Ruling: `scripts/init.sh`'s `[ -f "$WORK_DIR/related.md" ]` check is LEFT AS IS — it reads whatever a brownfield project already has on disk, and those files are never deleted, so the check stays correct — costs a dead branch in that script once every pre-format project is gone
F4: complete (commits 91e337c..db203ef, build.js exit 0 / 0 warnings, cli suite 47 files 1120 passed serially)
F5: Ruling: deleting `templates/related.md` staled `CLAUDE.md`'s generated inventory block, and `CLAUDE.md` is internal — outside this block's `[product]` tag — so the repair opens F5b `[internal]` per the command's "path not covered by the tag" rule rather than being written under F5 — costs one commit in which `inventory.test.js` L2.4 is red, closed by the very next commit; the plan's own F9-F11 cross-layer sweep sets the same precedent
F5: complete (commits db203ef..10dc2ff, build.js exit 0 / 0 warnings, L1.3 three levels green; inventory.test.js L2.4 red by design until F5b)
F5b: complete (commits 10dc2ff..2323dcc, build.js exit 0 / 0 warnings, inventory.test.js L2.4 green again, git status --porcelain framwork/ empty)
F6: Ruling: the artefact corpus ADAPTS the build's `artefact-graph.json` instead of re-parsing `framwork/.codeadd/` and `.claude/` — L2.4 gates F11 on both surfaces answering identically, and two parsers over one tree is precisely how two surfaces come to disagree — costs nothing; the sidecar is rebuilt on every build and is what `scripts/graph.js` already reads
F6: Ruling: `mcp/` carries its own frontmatter READER and takes no dependency, `yaml` included — `--corpus=artefacts` runs from the repository root where the CLI's node_modules is off the resolution path, so a dependency here would work in a user's project and fail in the repository that ships it; decision 23 promotes `yaml` for the MIGRATION, which writes user YAML back, and this only reads — costs a hand-rolled reader if a schema ever needs a nested map, which none does today
F6: Ruling: membership is decided by the PRESENCE of a `type:` key, not by matching a closed list of schema names — the design's table says "no `type:` key → skipped" and a real installation carries twenty distinct values including some no schema index lists (`feature-discovery`) — costs indexing a user's own file if they happen to write a `type:` key in their frontmatter
F6: Ruling: a reference page's node id is `wiki/<page>` — the design's table says the docs node id is "the `id:` value" and a wiki page has none, carrying `type`, `area`, `description` and `sources` instead — costs a rename of every reference-page id if a later version gives wiki pages an `id:`
F6: Ruling: an authored `## Relations` type beats a mechanically derived one for the same pair, and a self-edge or an edge pointing at an attachment is dropped and reported unresolved — a `{{doc:}}` reference and a `related:` id both mean "connected" and neither knows why, so letting `links_to` overwrite a typed edge would lose intent somebody wrote down — costs a lost edge if a document ever means to point at its own attachment, which relations are defined not to do
F6: complete (commits 2323dcc..4f4dacd, build.js exit 0 / 0 warnings, mcp-corpora 28 levels green, cli suite 48 files 1151 passed serially)
F7: Ruling: a docs query RE-SCANS the markdown before answering and writes the cache as a by-product; nothing reads the cache back — `artefact-graph-mcp.js` re-reads its sidecar per call for exactly this reason, and a server started before a write would otherwise answer from a stale graph for the rest of the session — costs a full scan per query, which the design's own "measure before optimising" line covers at a few hundred documents
F7: Ruling: `orphans` carries a DIFFERENT rule per corpus — artefacts keeps graph.js's "nothing depends on it, commands and fragments excluded", docs uses the design's "no relation or no TL;DR, the migration's work queue" — L2.4 asserts the artefact half matches graph.js and the design's action table defines the docs half; one rule forced on both would answer the wrong question in one of them — costs a reader assuming one meaning across corpora, which the action's own output names
F7: Ruling: `links_to` is excluded from `impact`, `dependencies` and `path` on the docs corpus, the same way `MENTIONS` is on the artefact one — both mark an edge recovered without a claim that anything depends on anything, and counting them would inflate every blast radius — costs a missed dependant if a `links_to` ever meant a real dependency, which is what promoting it to a typed relation is for
F7: Ruling: `reindex` REFUSES the artefact corpus rather than rebuilding it — `node scripts/build.js` owns `artefact-graph.json`, and a second writer for one sidecar is the failure the whole arrangement avoids — costs a manual build before an artefact query, which every other consumer already needs
F7: complete (commits 4f4dacd..8e69c88, build.js exit 0 / 0 warnings, mcp-engine 36 levels green incl. L2.4 all six verbs, cli suite 49 files 1187 passed serially)
F8: Ruling: F8's levels were written AFTER `mcp/server.mjs` rather than before it, so they were never observed RED — the RED-first claim is repaired by mutation instead: ping made to answer -32601, notifications made to get a reply, and the corpus cached across calls, each caught by exactly the level that should catch it and by no other — costs the weaker evidence that a mutation proves an assertion can fail while a RED run proves it was failing
F8: Ruling: the server carries a ONE-SHOT mode, `--action=<verb> [--args=<json>]`, beside the stdio server — the design states "the verbs also answer from the CLI with no MCP configured" as the mitigation for an offline `npx`, and it is what answers readback Gap 6, since `/add.done` needs one `reindex` and should not speak JSON-RPC to a subprocess it spawned for one call — costs a second entry shape to keep in step with the tool list, which one ACTIONS array prevents
F8: Ruling: `codeadd mcp` is dispatched BEFORE `runCli`'s try/catch and through a bridge module that imports only `node:` builtins — every other subcommand reports through `@clack/prompts`, which writes to stdout, and the catch block's `outro()` would put a human sentence into a JSON-RPC stream — costs one branch outside the shared error handling, which is why the bridge returns an exit code rather than throwing
F8: complete (commits 8e69c88..df91d6e, build.js exit 0 / 0 warnings, mcp-server 32 levels green incl. 3 mutation checks, cli suite 50 files 1219 passed serially)
F9: Ruling: the server gains an ELEVENTH action, `history`, which the design's verb table omits — `scripts/artefact-graph-mcp.js` exposes it and two of the suite's levels assert it, so deleting that server with only ten actions would take a working capability away, which F9's own contract ("Must NOT drop an assertion the retired server carried; each one moves") forbids — costs one verb beyond the design's table, delegating to `delivered.sh` exactly as `graph.js` does and never reimplementing the JSONL parse
F9: Ruling: `history`'s return shape is copied field for field from `scripts/graph.js`, `keys` and `unavailable: null` included — two surfaces answering one question must be swappable by a caller, and a renamed key is a divergence nothing here would catch — costs a shape this file must track if graph.js's ever changes, which the retargeted parity level asserts
F9: Ruling: `cli/tests/graph-query.test.js` is NOT retargeted at the new server, against the plan's wording — it tests `scripts/graph.js`, which survives as the shell-out surface every provider can reach; only its header prose claiming the MCP server is "a thin wrapper over this same module" was false after F6 and was corrected — costs nothing; the parity the plan wanted from it lives in `mcp-engine.test.js` L2.4
F9: Ruling: both `id` and the retired server's `node` argument name resolve on every action — a client configured against the old server keeps working across the swap — costs one alias per action, asserted
F9: complete (commits df91d6e..339576e, build.js exit 0 / 0 warnings, cli suite 50 files 1221 passed serially)
F10: Ruling: `scripts/graph.js`'s header comment is in F10's sweep though the plan's Impact table omits it — it claimed `scripts/artefact-graph-mcp.js` "wraps this same module", which F11 makes false, and the internal-layer skill requires a grep sweep after every removal precisely because `build.js` cannot see prose in a `.js` file — costs nothing; it is internal, the same layer tag, and the same block
F10: Ruling: `mcp/` is documented in `CLAUDE.md` as its own H3 under Project Anatomy with an explicit gate against the root-means-internal reading, rather than as a row in the existing layer table — the table's two rows are "Product Layer — framwork/.codeadd/" and "Internal Layer — .claude/", and a root directory that is product needs the reason stated where a reader meets it — costs a third section in a file the project deliberately keeps short
F10: complete (commits 339576e..5b97732, build.js exit 0 / 0 warnings, git status --porcelain framwork/ empty, grep sweep clean, cli suite 50 files 1221 passed)
F11: Ruling: the `.mcp.json` server KEY stays `artefact-graph` and only `command`/`args` change — `prompt-review-agent` and `building-commands` name "the artefact-graph MCP" in prose and every tool arrives as `mcp__artefact-graph__<verb>`, so renaming the key would break each of those pointers for no gain — costs a key whose name says artefact while the binary behind it serves both corpora, which the `--corpus` flag beside it makes plain
F11: Ruling: `CLAUDE.md`'s sidecar table said `artefact-graph.json` is read by "`scripts/graph.js` only", which F6 made false — corrected in this block since it names the file the sweep is about and sits in the same layer — costs nothing
F11: Ruling: five comments inside `mcp/` still name the deleted file as the rationale for a design call; `mcp/` is `[product]` and F11 is `[internal]`, so they cannot be written here — F11b opens with the product tag, per the command's "path not covered by the tag" rule — costs one commit in which the source cites a file that is gone, closed by the next
F11: complete (commits 5b97732..3c60d19, L2.4 gate verified green BEFORE the delete, build.js exit 0 / 0 warnings, framwork/ clean, cli suite 50 files 1221 passed)
F11b: complete (commits 3c60d19..af0120f, build.js exit 0 / 0 warnings, mcp suites 108 passed)
F12: Ruling: `cli/src/mcp/` is GITIGNORED and generated, not tracked — the release workflow's "Build framework" step runs `node scripts/build.js` unconditionally before `npm pack` and `npm publish`, so CI always has it, and tracking it would put two copies of one source under review — costs a fresh clone whose `codeadd mcp` reports "not packaged" until the first build, which the bridge says in those words
F12: Ruling: the copy is a MIRROR, not a merge — it prunes a file the source no longer has, the same way the sidecar clear-before-build does; a copy that only adds would leave a deleted module in every published package forever — costs one extra sweep per build over three files
F12: Ruling: `cli/src/mcp.js` takes an optional directory so the not-packaged branch stays reachable from a test after the build has generated the real copy — without it that branch became untestable the moment F12 landed, which is how an error path rots — costs one parameter nothing in production passes
F12: complete (commits af0120f..f7ba3db, build.js exit 0 / 0 warnings + MCP server 3 files, L1.4 and L1.5 green against the real tarball, cli suite 51 files 1228 passed)
F13: complete (commits f7ba3db..059d06c, build.js exit 0 / 0 warnings, framwork/ clean, L1.6 four levels green)
F14: Ruling: `writeMcpRegistration` lives in its own module, `cli/src/mcp-registration.js`, and `installer.js` calls it — the plan lists it under installer.js but F15 says the updater "calls the same registration writer", and two callers of one shape need one home or they drift — costs one module beyond the plan's file list, which is the CLI's own one-concern-per-module pattern
F14: Ruling: readback Gap 3 answered — the per-provider config path and shape are a table in that module: `.mcp.json`, `.cursor/mcp.json` and `.agent/mcp_config.json` take the common `mcpServers` object; `opencode.json` takes its own `mcp.<name>` shape with `type`/`command[]`/`enabled`; `.codex/config.toml` is TOML and DEGRADES TO PRINTING THE LINE — writing TOML by hand into a config the user already edited is the same shortcut decision 23 rejects for YAML, and this runs unattended — costs Codex users one paste, which the `postEnableHint` floor already establishes as acceptable
F14: Ruling: a config file that will not parse is left byte-identical and reported, never overwritten — treating an unreadable file as empty and writing would delete every server the user configured — costs a project whose broken config never gets the entry until they fix the file, which the printed line lets them do
F14: Ruling: F14's levels were written after the implementation, so they were never RED; three mutations were run instead and ONE WAS NOT CAUGHT — replacing the whole config object instead of merging into it passed all fifteen levels, because the only sibling-key test covered opencode. A sixteenth level was added for the `mcpServers` shape and the mutation then failed exactly there — costs nothing now; it is recorded because it is the one case in this build where mutation testing found a real hole in the assertions rather than confirming them
F14: complete (commits 059d06c..e968f0b, build.js exit 0 / 0 warnings, 16 levels green, 3 mutations run and 1 real assertion gap found and closed, cli suite 52 files 1248 passed)
F15: Ruling: registration runs BEFORE `update`'s already-up-to-date early return — the plan says registration only in `install` leaves an existing project unserved, and placing it after the download reopens that hole one branch earlier, since a project already on the latest version never downloads; the writer is idempotent and rewrites the pin, so on a current project it is either a no-op or the repair that project was missing — costs one writer call on every `update`, including the ones that do nothing else
F16: Ruling: the verb MOVES TO THE MIGRATION that did the work — both drivers now print `Migration: ${change}` and `pruneLegacyOrphans` pushes `removed <path>` — a neutral label with no verb reads as a bare path, and F18's additive migration will push `updated <path>` for the same reporter — costs one edit to every future migration's change strings, which is where the knowledge belongs
F15: Ruling: two of this block's levels failed on their own comment text — one matched the wording F16 replaced, which the file quotes to explain the change, and one sliced a fixed number of characters past the call and caught the next statement; both were tightened to read code with comments stripped — costs nothing, and it is the second time in this build that a level failed on prose rather than on behaviour
F15: complete (commits e968f0b..b4db348, build.js exit 0 / 0 warnings, cli suite 52 files 1255 passed)
F16: complete (same commit b4db348 — one file, two F-blocks, both levels green)
F15/F16: Ruling: F15 and F16 landed in ONE commit, against add-build-ledger's one-commit-per-F-block rule — they edit adjacent lines of `cli/src/updater.js` and splitting them would have left a commit whose own levels could not both pass; the ledger records both ids against the one range — costs a reviewer who bisects on F16 alone landing on F15 too
F17: Ruling: the plan states `yaml` "is already present transitively and is promoted to a direct dependency" — IT WAS NOT. `npm ls yaml` reported empty and an import threw ERR_MODULE_NOT_FOUND, so this block installs it rather than promoting it — costs one real dependency where the plan expected none, which is what decision 23 asked for either way
F17: Ruling: `^2.9.1`, ranged like both its neighbours, against the product-layer skill's "DO NOT bump cli/package.json" — that rule is about the VERSION field, which is the release command's, and adding a dependency the plan names is not a bump — costs nothing; `cli/package.json` version is untouched at 0.8.1
F17: complete (commits b4db348..90c02ea, build.js exit 0 / 0 warnings, 6 levels green, cli suite green)
F18: Ruling: the migration parses frontmatter with `yaml` and carries its OWN small body readers rather than importing `mcp/corpora.mjs` — F18's Consumes line is "`yaml` available to `cli/src`", `runMigrations` calls `run()` synchronously so an ESM dynamic import of the generated `cli/src/mcp/` could not be awaited there, and the two readers have different jobs: one reads to index, one reads to write — costs a second parser for `## Relations` and `## Follow-ups`, pinned on both sides by the level that parses every harvested line back through the indexer
F18: Ruling: `superseded_by` is COUNTED AND NOT WRITTEN as a relation line — the design says it is "the status edge it already is; not duplicated into `## Relations`", and it is outside the closed four-value vocabulary, so writing it would put a type the schema gate rejects into the section the gate checks; the indexer reads it straight from frontmatter — costs nothing, and the first implementation had it wrong until the design was re-read
F18: Ruling: several sources can name the same target and the BEST one wins rather than the first — a `related:` id knows nothing while a `related.md` Follow-up sentence is the `why` the format wants, and emitting on first sight let the poorest source beat the richest purely on read order; this was found by a level, not by review — costs one map per work item
F18: Ruling: it writes NO `tags: []` and NO empty `## Observations`, against the design's Vehicle 1 bullet list — both carry no information, and enlarging the diff the user must review works against F18's own contract that reviewing that diff is the judgement the CLI cannot make; the indexer treats an absent and an empty list identically — costs a user who never discovers the `tags:` key from a migrated document, which the schema documents instead
F18: Ruling: `## Relations` is appended at the END of a document when absent, never inserted between two existing sections — inserting moves lines somebody else wrote, which the additive-only rule forbids — costs a section order that does not match the schema's stated order on a migrated document, which no reader depends on
F18: complete (commits 90c02ea..d55cb78, build.js exit 0 / 0 warnings, 27 levels green incl. L3.1-L3.6, L4.1 and L4.5, cli suite 53 files 1282 passed)
F19: RED BUILD, reported and fixed inside the block, not ruled away: gate 4.4's prose names `/add.new` and `add.done.md`'s `uses:` block did not declare it, so `checkArtefactGraph`'s undeclared-reference gate failed the build. Fixed by declaring `- mention: /add.new` — the prose points at it as the writer of the section, away from any dependency. Graph goes 777 -> 778 edges; warnings stay at zero.
F19: Ruling: the close-out gate is 4.4 and binds FEATURE AND HOTFIX branches, where 4.0 to 4.3 are feature-only — both `/add.new` and `/add.hotfix` write a node, so a hotfix that closes without a TL;DR or a relation decays the format exactly as a feature would — costs a hotfix author one section the command already writes for them
F19: Ruling: the index rebuild is 6.7.1, NON-BLOCKING, and goes through the one-shot CLI form rather than the MCP server — it must run after the changelog and the wiki edits so the new document is in the index, and a rebuildable cache of the user's own markdown must never block a merge — costs an index one run out of date when `npx` cannot reach the package offline, which the next run repairs
F19: Ruling: regenerating `web/public/artefact-graph.mmd` is outside F19's `[product]` tag, so it opens F19b `[internal]`, the same route F5b took — costs one commit in which `graph-mermaid.test.js` is red, closed by the next
F19: complete (commits d55cb78..b1b57e7, build.js exit 0 / 0 warnings / 777->778 edges, 9 gate levels green; graph-mermaid red by design until F19b)
F19b: complete (commits b1b57e7..fdb436a, graph-mermaid green again, git status --porcelain framwork/ empty)
F20: Ruling: readback Gap 7 answered — "a named destination" is implemented as `RELATED_WORK`, a named carry-forward the GRAPH step produces, and each of the six commands names the STEP, dispatch prompt or document section it lands in; that is the shape four of the six already use for the wiki page set — costs a name six files must keep in step, which the level asserting all six checks
F20: Ruling: the hotfix wiki-blind exemption is EXTENDED to cover GRAPH, not only INDEX — the skill's stated reason is that the index "records what shipped, never what the code means", and a `## Relations` edge is the same kind of record; both are used at STEP 4 to RANK what the history agents look at, never to conclude anything about current behaviour, which is the use the index already had there — costs a triage that reads a past TL;DR, which the added guard sentence bounds
F20: Ruling: GRAPH goes in at position 2 and the procedure renumbers 2-8 to 3-9, rather than being appended at 9 — the index answers whether it shipped and the graph answers what it connects to, and both are cheap document reads that belong before the wiki; a level asserts the numbering stays contiguous and that the one internal pointer moved with it — costs a renumber, which is why that pointer has its own level
F20: complete (commits fdb436a..b4294e9, build.js exit 0 / 0 warnings / 778 edges, 11 levels green, cli suite 53 files 1300 passed serially)

REVIEW: complete (38 findings, 23 applied, 15 rejected)

REVIEW: Ruling: the systemic finding is applied in full — F20 named a RELATED_WORK destination in the PROSE of all six commands and in FIVE of them the slot did not exist; the level I wrote asked whether the prose names a destination, which is the wrong question — costs nothing now; the level was replaced with one that reads the wiring, and a mutation removing add.plan's bootstrap slot was run against it twice: the first rewrite still passed, because a file-wide search matched the paragraph describing the slot, so the level now reads the fenced template a subagent actually receives
REVIEW: Ruling: REJECTED — add.brainstorm's `/add.plan` with no `uses:` entry and its `/founder` pointing at a command that does not exist; both verified pre-existing by `git log -L` (commits 7308524 and b7e39e9) — costs two stale pointers in a file this plan touched by one bullet; they belong to whoever next opens that command
REVIEW: Ruling: REJECTED — add.diagnose's `blocked` verdict rests on two findings about a pre-existing methodology conflict: it defers the reformulation confirmation to STEP 7 while `add-investigation` says not to proceed to Phase 1 without it, and its own Rules row contradicts its STEP 2 body; entirely unrelated to this plan — costs stalling a finished delivery on a question the plan never raised; SURFACED to the user instead
REVIEW: Ruling: REJECTED — the form debt three auditors reported in files this plan touched by one line: add.diagnose's prohibitions and Rules as tables rather than blocks and ALWAYS/NEVER, add.plan's `## Execution Rules` table, add.new's Execution Constraints sitting after STEP 1, add-doc-schemas' four unnamed command mentions and its "MUST paste the following block" line, and six add-ecosystem rows the auditor itself marked "pre-existing and untouched by F4" — costs a backlog of real form debt this delivery did not create and does not widen
REVIEW: Ruling: REJECTED — `F-Block:` sits above the Co-Authored-By paragraph in all 22 F-block commits, so git's trailer parser returns empty and only a grep finds it; fixing it means rewriting 22 commits, which is a history rewrite and one of the four hard stops — costs tooling that joins by trailer rather than by grep; the next build should place the line last
REVIEW: Ruling: REJECTED — `web/src/pages/docs.astro` and `ecosystem.md` carry the pre-change description of `add-knowledge-discovery`; both are generated by `/add-framework--sync`, which STEP 1.3 recommends running after a product artefact change and which this build did not run — costs two stale generated files until sync runs; SURFACED as the recommended next command
REVIEW: Ruling: REJECTED as already recorded — F8 and F14 levels never observed RED (both disclosed, both mutation-tested), three cross-layer split pairs beyond the plan's one stated exception (each ruled, each closed by the next commit), and F15/F16 sharing one commit — costs nothing new; the ledger already carries each with its cost clause
REVIEW: Ruling: REJECTED — the ledger does not carry the Reviewer Handoff's per-block evidence shape (files touched per block, covering levels named per block); it carries commit ranges, build results, test counts and 30 rulings — costs a reviewer reconstructing the level-to-block mapping from test-file headers
REVIEW: Ruling: REVERSED AND APPLIED (was deferred) — two auditors found real gaps this delivery could close but the plan did not scope: `docs/changelog/CHG[NNNN].md` contributes no EDGES because both the indexer and the harvest attribute attachment content by directory only (decision 30 says both layouts are read, and the attachment LISTING does resolve; only the edges do not), and L3.3 is asserted by one hand-picked line per source plus one raw total over a nine-file fixture rather than per source edge — costs a changelog written to the out-of-directory layout carrying no edges, and a harvest level that would not notice a dropped 2nd..Nth edge; both are F7 and F18 contracts rather than new scope, so both were applied; thickening the fixture then exposed a real defect neither auditor saw, a sentence boundary matching only ". " that gave the 2nd and 3rd reference in a wrapped paragraph a reason naming the 1st document
REVIEW: Ruling: REJECTED — F9 weakened the `history` parity from one deep-equal to six field assertions, which no longer catches an EXTRA key appearing on one surface; the shapes are equal today and `mcp/engine.mjs` copies graph.js's return field for field — costs a divergence in one direction going unseen; reported rather than reopened, because F9's suite has already been through its one pass
REVIEW: Ruling: the graph's own finding (STEP 7.2) is REJECTED — `add-knowledge-discovery` has eight direct dependants and the plan names six; `conformance-agent` and `add-feature-discovery` were neither changed nor named, and both were verified unaffected: neither names a step of that skill by NUMBER, and neither runs the procedure — both consume the wiki half, handed to them as page paths by the command that dispatches them — costs nothing
REVIEW: Ruling: the review pass found and fixed a defect of its own making — the level replacing the prose-only RELATED_WORK check still passed when add.plan's bootstrap slot was deleted, because a file-wide search matched the paragraph describing the slot; it now reads the fenced template, and the same mistake was made a second time on the changelog-edge level, which passed under mutation until the fixture gained a document reachable ONLY through the out-of-directory changelog — costs nothing now; recorded because it is the same error twice in one hour and the lesson is to assert the wiring in isolation, never the sentence near it
PUBLISH: branch pushed to origin, PR #56 opened at https://github.com/brabos-ai/code-addiction/pull/56 (user approved at STEP 9)
