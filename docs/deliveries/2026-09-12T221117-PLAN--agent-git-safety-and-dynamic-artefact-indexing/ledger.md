# Build ledger — plan: docs/plans/2026-09-12T221117-PLAN--agent-git-safety-and-dynamic-artefact-indexing.md

Branch: feat/agent-git-safety-and-dynamic-indexing
Baseline: `ADD_GRAPH_WARNINGS=1 node scripts/build.js` exit 0, ZERO warnings, 216 nodes / 782 edges (measured on main before F1)

READBACK: complete (6 gaps marked, 4 rulings below, build continues per STEP 4.2)

F1: Ruling: F1 also adds `CONCERNS` to test-agent.md's `## Report` contract and to the two tdd-pipeline dispatch `Report:` lists — the plan's exit route names a field the agent's own contract never declares (its report has `ERRORS`, not `CONCERNS`), so as written the route pointed at nothing; the readback caught it as a ruler item-4 contract break — costs a second edit to three files if a different field name was intended, and nothing else, since all three are [product] like F1 itself
F1: Ruling: the git rule goes in a `## Git and the shared tree` H2 placed immediately after `## How You Work`, with the exit route also wired into step 3 itself, rather than into `## Constraints` at the end — step 3's "Iterate until they pass" is the instruction that creates the need, and `building-commands` item 7 calls a prohibition restated at the tempting shortcut load-bearing rather than repetition — costs a move of one H2 if a reviewer wants it consolidated under Constraints
F3: Ruling: `KNOWN_FAILURES` renders as one line per failure in `<test>: <owner/area>` form under a `## KNOWN_FAILURES` heading, and `task-brief.sh` takes it as an OPTIONAL 4th positional argument — three states stay distinguishable, which the plan requires: 3 args = not supplied, 4th empty = `none observed`, 4th with content = the list; no existing caller breaks — costs a rewrite of the brief section and its bats assertions if the consuming agent is later given a different shape
F10: Ruling: the drop decision for F10-F12 is taken while attempting F10, BEFORE F11 and F12 are touched — not build-all-three-then-revert; the readback flagged both readings as available — costs an under-tested container idea if the edge shape only reveals itself once the query side is wired
F1: Ruling: refining the allocation of the ruling above — F1 covers ONLY test-agent.md (its `## Report` contract plus the new H2); the two tdd-pipeline dispatch `Report:` lists move to F3, which already owns both files, so no file is touched by two commits in this build — costs one transient commit where the agent declares CONCERNS and the dispatch does not yet ask for it, which no gate reads
F1: BASE=21e33f8c54236da1e553dbcffe8cac146f896686
F1: Ruling: the first commit landed with a mangled subject — PowerShell here-string syntax leaked into the Bash tool — and was amended in place before anything else ran; the tree content was never wrong, only the message — costs nothing, the commit was never pushed
F1: complete (commits 21e33f8..adf714a, build.js exit 0 / zero warnings against a zero baseline, no leak into .claude/ or CLAUDE.md)
F2: BASE=adf714acc9b95e85750c5f6038563d44f49e2198
F2: complete (commits adf714a..8d05bef, build.js exit 0 / zero warnings, no internal-layer leak)
F20: BASE=8d05bef6805aa45cd5532c84bbd29f37eb6caba0
F20: Ruling: the plan said "replace with the same substitute F1 gives", and F1's substitute does not fit — `git diff -- <path>` compares FILE CONTENT, while this paragraph asks whether the whole SUITE passes on a clean checkout, which no path-scoped read can answer; used a throwaway `git worktree add ... HEAD --detach` instead, which the framework already uses elsewhere (build-setup.sh --worktree), and added a paragraph saying explicitly why the path-scoped read is not the answer here — costs a rewrite of one code block if a reviewer prefers deferring to CI instead of a local worktree
F20: complete (commits 8d05bef..1d66330, build.js exit 0 / zero warnings, framwork/ empty, grep sweep clean)
F3: BASE=1d66330672e45f3fd2b52bcaaef4aad4687e056f
F3: Ruling: F3 also edits add-subagent-driven-development/SKILL.md line ~211, a file F2 already committed — the skill documents the same task-brief.sh invocation, and leaving it at three arguments would create a second, stale copy of the script's contract, which is the exact defect class this plan exists to remove — costs one file appearing in two commits of this build, which no gate reads
F3: Ruling: bats test 23 (five arguments stay CLI misuse) passed on the RED run and is recorded as a pre-existing guard, not as a new RED — the plan's Reviewer Handoff gap 1 asks for exactly this distinction
F3: complete (commits 1d66330..158cb84, bats task-brief 23/23 green after 4 confirmed RED, build.js exit 0 / zero warnings, no internal-layer leak)
F4+F5: BASE=158cb849a2b5d1c9974b66d025bb16ad15ace6da (one commit — the gate fails between them by design)
F5: Ruling: the plan estimated "23 of 24 fragments" name an artefact; the gate's actual answer is 8 fragments and 22 references. The plan's number came from a loose regex run at planning time that counted filenames and already-declared names; the gate is the authority and its output was used as the worklist — costs nothing, the migration is smaller than planned
F5: Ruling: judged each of the 22 pairs individually rather than declaring them all as edges. `mention:` went to the 9 whose prose points AWAY — docs-pruning's retention table naming who READS a doc, "@ux-agent (review) does not drive", "the UX axes are @ux-agent's, not yours", and qa-pipeline's list of routes that are SURFACED rather than dispatched — costs a missing edge if one of those nine is a real dependency; L2.1 pins the one that mattered
F4+F5: complete (commits 158cb84..101241e, build.js exit 0 / zero warnings, edges 782->804, injection points 40 unchanged, L1.2 RED observed then L1.3+L2.1 GREEN)
F6: BASE=101241e54d53ac2fb7396eb991c16f046587bec3
F6: Ruling: making add-gitnexus a node makes it SNIFFABLE (skill is in SNIFFABLE_KINDS), so 16 artefacts that name it in prose now fail the undeclared-reference gate. The plan costed F5's migration and not this one — same shape, one level up. Opened F6b [product] for the 16 files rather than writing framwork/.codeadd/ under F6's [internal] tag, per the build command's path-not-covered rule; F6 and F6b land in ONE commit because the build is red between them, exactly like F4+F5 — costs one more commit spanning two layers, which the STEP 2 exception already covers
F6+F6b: complete (commits 101241e..933a641, build.js exit 0 / zero warnings, nodes 216->217, edges 804->820, skill nodes 52->53, add-gitnexus has 15 dependants and is not an orphan)
F7: BASE=933a64173b8568a6d92f475d52718563a147827d
F7: complete (commits 933a641..581fbc2, build.js exit 0 / zero warnings, nodes 217->221, 4 template nodes all declares:false, L2.2 green — orphans filtered to kind:template is exactly those four)
F8: BASE=581fbc21ec261f1df918f68ae65cf02055b96caa
F8: complete (commits 581fbc2..7d8d4d6, build.js exit 0, orphans unchanged at 26 — feature/plugin nodes do not exist until F10 — templates still reported, framwork/ clean)
F9: BASE=7d8d4d689c45fd1a85b076dd6fe261f0e5d729bc
F9: Ruling: the four assertions in cli/tests/build-artefact-graph.test.js pinning the old kind sets and counts were updated under F9 rather than retro-fitted into F4/F6/F7, which are already committed. They went red only when the cli suite ran, and the internal-layer skill's per-block validation is `node scripts/build.js` alone — it does not include the suite that covers build.js's own output. The test file is [product] like F9, so the tag holds — costs a commit boundary that does not match the F-block that caused each change; the commit message names F4/F6/F7 explicitly so the join is not lost
F9: Ruling: the discovery report said build-artefact-graph.test.js would NOT go red from new kinds. It did — three assertions, at lines 314, 332 and 840. Trusted the run over the report, as the skill instructs — costs nothing; it is recorded so the same claim is not believed twice
F9: complete (commits 7d8d4d6..dd72fcc, L3.2 both ENTRY_POINT_KINDS sets equal, L3.5 mcp/ still imports only relative and node: builtins, full cli suite serial 53 files / 1318 passed / 0 failed)
F13: BASE=dd72fcce17e0864c23e7cea7b029707cf2e95c90
F13: complete (commits dd72fcc..deee9db, build.js exit 0, framwork/ clean, comment-only change)
F10: Ruling: F10-F12 are NOT dropped. The plan made them droppable if the CONTAINS edge shape did not fall out of the existing set; it does — INJECTS_INTO is already a build-derived edge pushed straight into buildArtefactGraph with no uses: block behind it, and CONTAINS uses the same mechanism at the same site. Decision taken while attempting F10, before F11/F12 were touched, per the readback ruling — costs nothing; had it gone the other way, F1-F9 and F13 stood alone
F10: complete (commits deee9db..19b883b, build.js exit 0, nodes 221->226, 25 CONTAINS edges, framwork/ clean)
F11: BASE=19b883bc6b6102464b5da87657e53f0cd6d42b5a
F11: Ruling: the L2.6 test lives in cli/tests/graph-query.test.js, which is [product], while F11 itself is [internal] (scripts/graph.js). There is no internal test location — that suite's own header says it OWNS scripts/graph.js — so the cross-layer pairing is the repository's existing convention, not a boundary break — costs nothing; recorded so a reviewer does not read it as a stray product edit
F11: Ruling: my first "negative proof" (running orphans with and without CONTAINS on the real graph) returned 26 both ways and proved nothing. Replaced it with a deliberate break of orphans() against the synthetic fixture, which failed exactly one test — costs nothing; recorded because a passing check that cannot fail is worse than no check
F11: complete (commits 19b883b..73305f1, orphans held at 26, L2.6 fixture confirmed to bite under a deliberate revert, graph-query suite 34/34)
F12: BASE=73305f1ba6b985dc8376f56daa0099be45dd65bd
F12: Ruling: the kind-set and node-count assertions for F10's container kinds were updated here, not retro-fitted into F10 — same reason as F9's ruling: an internal F-block's validation is `node scripts/build.js`, which does not run the suite that covers build.js's own output. Recorded twice now because it is a gap in add-framework-internal-layer, not a one-off — costs a commit boundary that does not match the F-block; the message names F10
F12: complete (commits 73305f1..4e6d83a, L3.2/L3.3 parity tests added and confirmed to bite under two planted divergences, full cli suite serial 53 files / 1324 passed / 0 failed)

=== T2 COMPLETE — T3 next ===
F14: BASE=4e6d83a3a0dd1190d774d9ee0c7b6dff45c609ed
F14: complete (commits 4e6d83a..4e3e4d2, build.js exit 0, nodes 226->227, orphan as expected until F15-F18, framwork/ clean)
F15: BASE=4e3e4d20398785f007b2810c7afdfb6c2df2df41
F15: complete (commits 4e3e4d2..70bb3cf, build.js exit 0, add-artefact-graph no longer an orphan, framwork/ clean)
F16: BASE=70bb3cfbb71b9a390b9dad1e5f54669b140005ac
F16: complete (commits 70bb3cf..3e2d0f8, build.js exit 0, framwork/ clean)
F17: BASE=3e2d0f8d99c8da19f1a6a17bf3379e195fe64394
F17: complete (commits 3e2d0f8..8bb8ab0, build.js exit 0, framwork/ clean)
F18: BASE=8bb8ab04ee97fbb30bbe45c23c119f7432f8c20a
F18: Ruling: kept each command's concrete graph.js invocations instead of replacing them with a bare skill load. The plan said the inline enumeration "becomes a load of the skill"; read literally that deletes flags that matter at the point of use (--depth 1 grades risk, --json feeds the sync columns), and building-commands item 7 calls a rule restated at the decision point load-bearing. What changed is that the enumeration stops being the ONLY route — the skill is named as owner of the rest — costs a reviewer seeing four surviving code blocks where the plan implied none; the enumeration problem it was aimed at is gone either way
F18: complete (commits 8bb8ab0..7cf5cb3, build.js exit 0, edges 853->860, framwork/ clean)
F19: BASE=7cf5cb3ae484a5c8f7c997257fe36a0d702bb863
F19: complete (commits 7cf5cb3..0991fee, build.js exit 0, node scripts/inventory.js run and the generated block did not change, framwork/ clean)
REVIEW: Ruling: 14 .md command/skill/agent artefacts are in the diff, dispatched as 11 prompt-review-agent calls plus 3 scope auditors. Two pairs were combined because each pair received the same mirror-image edit in the same commit and one reader judging both together checks item 4/5 consistency BETWEEN them, which two separate readers cannot: plan-review-agent with prompt-review-agent, and add-framework--sync with add-framework--brainstorm — costs slightly less depth per artefact on those four; every one still gets all eight items ticked

REVIEW: Ruling: rejected — add.build.md has no `## Rules` section and hardcodes "Task tool" in two places. Both pre-existing, both in a product command this plan touched for one coordinator step — fixing them is a command refactor with its own blast radius — costs two ruler items staying red on that artefact until a plan takes it
REVIEW: Ruling: rejected — add-framework-development has two H2s numbered "8". Pre-existing, and `building-commands` quotes that exact pair as its own worked example of a broken reference; renumbering would invalidate the example in the artefact that teaches the rule — costs a known-red item 2 until both are changed together
REVIEW: Ruling: rejected — add-framework--brainstorm sends `prior_deliveries` to @framework-discovery-agent, whose Input Contract declares only topic and scope. Pre-existing and untouched by this delivery — costs a field computed and possibly ignored; worth a follow-up, not this plan's scope
REVIEW: Ruling: rejected — add-framework--sync restates the MENTIONS rule, and add-framework--build and --plan both restate the depth-1 reasoning that add-artefact-graph now owns. All three pre-existing; this delivery added pointers rather than rewriting text it did not come for — costs three item-5 duplications surviving one more delivery
REVIEW: Ruling: rejected — add.build.md and add-subagent-driven-development both carry the whole Handoff-by-Path mechanism. Genuine structural duplication with three consumers, pre-existing, and picking an owner is a refactor — costs the two drifting again; the KNOWN_FAILURES half was kept identical character-for-character in this delivery
REVIEW: Ruling: rejected — `stats` hubs count CONTAINS on both surfaces, so every contained node gains one dependant, and `history` publishes that number. Latent: today's top-10 is skills outside any container, so no published list moves. Same class as the orphans leak F11 closed, and it deserves the same treatment — costs a silently inflated hub count the day a container gains many members. Recorded as a follow-up rather than widened into this delivery
REVIEW: Ruling: rejected — MERMAID_SHAPE has no entry for template/feature/plugin so they render as skill boxes, and web/src/pages/docs.astro carries a stale node count. Both cosmetic: DOCS_PROFILE is command-only so the shipped diagram is unaffected, and /add-framework--sync regenerates the count — costs nothing until someone renders a feature node by hand
REVIEW: Ruling: rejected — add-framework-internal-layer still carries its own three-verb graph block and was not pointed at add-artefact-graph. Real, and the last surviving partial list; it is not in this plan's F-block set and adding it now would be scope taken on my own initiative — costs one more partial list until a plan names it
REVIEW: Ruling: rejected — the plan's Impact table omits `scripts/tests/task-brief.bats` and `add-ecosystem/SKILL.md`, and its F2 row does not mention that F3 touched the same file. These are defects in the PLAN document, which is gitignored and already closed — costs nothing to the tree; the delivery archive carries the ledger that records both
REVIEW: Ruling: rejected — F4+F5 landed across two commits rather than one. It was mandatory, not a slip: the 15 gitnexus fragments declare `add-gitnexus`, which only becomes a node in F6, so declaring it in F5's commit would have failed the dangling-target gate. The plan's ordering was wrong here, not the work — costs a boundary that does not match the plan text
REVIEW: Ruling: partially rejected — the auditors asked for a pure pointer in plan-review-agent, dropping the `node scripts/graph.js` invocation. `cli/tests/plan-review-agent-speed.test.js` L2.3 asserts that file names scripts/graph.js, because the agent must act without loading anything. Kept the command, added the reason and the test reference beside it — costs one item-5 duplication the suite requires
REVIEW: Ruling: the three files under .claude/agent-memory/prompt-review-agent/ are untracked side effects of this review's own auditors, not delivery content. No agent-memory path is tracked in this repo. Left in the working tree, uncommitted, for the user to decide — costs nothing; committing another agent's memory under this delivery would be scope it never had
REVIEW: complete (34 findings, 18 applied, 16 rejected)
