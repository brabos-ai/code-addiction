# Build ledger — plan: docs/plans/2026-09-13T153219-PLAN--test-terminal-states-and-qa-feature-boundary.md

Worktree: C:\github\xmaiconx\code-addiction\.claude\worktrees\test-terminal-states-and-qa-feature-boundary
Branch: feat/test-terminal-states-and-qa-feature-boundary
Branch point: 44035ef
Baseline (ADD_GRAPH_WARNINGS=1 node scripts/build.js): exit 0, ZERO warnings, 40 injection points, 227 nodes, 855 edges
Plan source of truth: the SHARED checkout's docs/plans/ — the directory is gitignored, so the worktree has no copy and this ledger is the only file here

Ruling: added F0 ahead of F1 — a [product] block creating cli/tests/test-terminal-states-qa-boundary.test.js — because the plan's Validation Matrix demands L1.3, L2.1-L2.7, L3.2 and all of L4 be written RED before any F-block lands, and names no file for them; L1.1/L1.2 live in loop-consolidation-0070, L1.4 in build-artefact-graph, and the rest had no home — costs one extra test file to delete if the plan meant those levels to be verified by reading alone
Ruling: added F20b beside F20 — cli/tests/injection-exclusivity.integration.test.js and cli/tests/delivery-index.test.js also pin the injection total at 40, and the plan names only loop-consolidation-0070 (F20) and build-artefact-graph (F21) — without it F15/F16 turn two suites red with no block owning them — costs nothing if wrong, the pins have to move either way
Ruling: L4.1-L4.4 are verified as contract text present in the artefact, not as executed agent behaviour — nothing in this repo runs a @test-agent or a @fix-agent, so "the agent returns BLOCKED" is only checkable as "the contract instructing it to is written and unambiguous" — costs a false sense of coverage if a reader takes L4 for a behavioural run; the report says so explicitly
Ruling: readback marked eight gaps it filled on its own; five are answered by files it was not given (BLOCKED routing via STEP 11.2, runner detection via the fragment's TEST_FRAMEWORK step, the MODEL tier via add.build's Correction Dispatch round-3 row, the playwright anchor sitting mid-STEP 10 which is exactly why F15 splits judge-head/judge-tail, and STEP 11's row being a table row) and three are genuine document silences ruled below — costs nothing; the plan is executable as written
Ruling: BLOCKED rides the report as its own field, not as a value of TESTS_PASSING — the plan calls it a terminal state distinct from CONCERNS, and overloading an existing boolean would make F11's synthesis unable to tell "no tests written" from "a real bug found" — costs a field rename in F11 and F12 if the intended shape was a TESTS_PASSING value
Ruling: F18 and F19's judgement clause edits the existing sentence rather than appending after it — the current text says qa-pipeline decides whether artefacts are authored, so an appended clause would leave the narrower claim standing beside its own correction, which is the contradiction this plan closes — costs a re-edit if the plan meant a strictly additive sentence

Ruling: F3 is a no-op — the plan says KNOWN_FAILURES is used at test-agent.md line 66 and declared nowhere, but it IS declared in `## Inputs` at line 26, added by the 2026-09-12 delivery the plan itself cites as prior art; L2.5 is therefore reclassified from RED-first to a guard, and F3 lands no edit — costs nothing; if a reader expected a second declaration site, the field is already where the plan wanted it
F0: complete (commits 44035ef..a841c8e, build.js clean at 40 points / 227 nodes, 15 of 18 levels RED as designed, 3 green as declared guards)
Ruling: F13's test-agent.md half — the final-attempt MODEL input — landed inside the F1-F5 commit rather than in F13, because it is one bullet in the same `## Inputs` list F2 rewrites and splitting one list across two commits buys nothing; F13 now touches only the two tdd-pipeline fragments — costs a slightly wider F1-F5 diff if a reviewer expects F13 to carry all three files
F1, F2, F4, F5: complete (commits a841c8e..b77bc54, build.js clean at 40 points / 227 nodes, L4.1 + L4.1/L4.4 RED->GREEN)
F3: complete (no edit — premise already satisfied in the tree, ruled above)
F6: complete (commits b77bc54..fbe3f18, build.js clean at 40 points / 227 nodes, L4.2 RED->GREEN)
F8, F7, F11: complete (commits fbe3f18..3eaeb84, build.js clean at 40 points / 227 nodes, L2.4 singular-AREA scan RED->GREEN; L2.4 until-green scan still names add-ecosystem, which F23 owns)
F9, F10, F13: complete (commits 3eaeb84..d745afd, build.js clean at 40 points / 227 nodes / 857 edges — +2 edges from the two `mention: @fix-agent` declarations the undeclared-reference gate demanded; both L4.4 fragment levels RED->GREEN)
F12: complete (commits d745afd..f76a8ed, build.js clean at 40 points / 227 nodes, L4.2/L4.4 RED->GREEN)
WORKING STATE after F12 (T1 whole): full cli suite serial — 54 of 55 files pass, 1347 tests pass, 9 fail. Every failure is in this delivery's own level file and belongs to a T2 block not yet landed (L1.5, L2.1, L2.2, L2.4, L2.6, L2.7, L3.2, L4.3, add-ecosystem). No pre-existing suite regressed.
Ruling: cli/tests/features.test.js is a THIRD pinned suite the plan did not name, and it moved inside F14 rather than F20b — it pins the exact value F14 changes, so it is that block's own dependent under the remove/rename sweep rule, and deferring it would leave the branch red across six blocks for no reason — costs nothing; F20b still owns the two suites that pin the injection TOTAL, which only F15/F16 move
F14: complete (commits f76a8ed..93df780, build.js clean at 40 points / 227 nodes, L1.5 RED->GREEN, features.test.js 38/38)
Ruling: F15 and F16 landed in ONE commit — the fragment without the markers injects nothing and the markers without the fragment fail the build, so a commit between them would be a commit of a red tree, which the ledger forbids — costs a coarser diff boundary if a reviewer wants to read the fragment before the markers
Ruling: the tdd-pipeline step-list pair MOVED from STEP 10 to STEP 3 — F16 deletes the line it was anchored on, so it had to move regardless; STEP 3 is where its `STEP 3.5` content belongs, and it also gives the two step-list pairs distinct anchors, without which their relative order would follow the enable order and L3.2's byte-equality check would fail — costs a one-line position change in the STEP-order block that the plan did not name
Ruling: MEASURED, NOT ASSUMED — the plugin:playwright:drive point now shares the `contract.` anchor with the four qa sections, and applyInjectionToContent groups by anchor WITHIN one namespace only, so the drive block lands ABOVE STEP 8 when qa is enabled first and BELOW it when the plugin is enabled first; mid-STEP placement is mechanically unreachable with a shared anchor whatever the section count. Kept the plan's five-section split (it does achieve its stated purpose: no enclosure, build green, 45 points as predicted) and pinned both reachable placements in a test rather than shipping the behaviour undocumented. A cross-namespace shared anchor already exists on add.plan (qa-pipeline + tdd-pipeline), so this is the second instance of a pre-existing framework property, not a new class — costs a reader seeing the live-driving paragraph before the preflight it references, in the qa-then-playwright order; the fix is a second base-body seam line, which the plan's F17 scope does not allow
F15, F16: complete (commits 93df780..0ffa017, build.js clean at 45 points / 228 nodes / 871 edges — exactly the plan's predicted 40->45 and 227->228; L2.1, both L2.2 and L4.3 RED->GREEN)
F17: complete (commits 0ffa017..7fa2da2, build.js clean at 45 points / 228 nodes, L2.3 guard holds)
F18, F19: complete (commits 7fa2da2..dc9d058, build.js clean at 45 points / 228 nodes / 873 edges, L2.6 + L2.7 RED->GREEN)
Ruling: F21 moved THREE pinned values, not the one the plan named — nodes 227->228, declaring nodes 131->132, and the per-kind snapshot's fragment count 24->25; a fragment is a declaring kind, so one new fragment moves all three and pinning only the total would leave the suite red — costs nothing, the three are the same fact counted three ways
Ruling: F20b grew to THREE suites, not the two I opened it with — cli/tests/qa-pipeline-umbrella.test.js carries the same "gating plan/build" boundary assertion as F20's and only surfaced after F15 landed — costs nothing; it is the same edit F20 makes, in a file the plan did not enumerate
F20, F20b, F21: complete (commits dc9d058..a927188, build.js clean at 45 points / 228 nodes / 873 edges; L1.1, L1.2, L1.4 GREEN — L1.4 was RED from F15 by design; five pinned suites 178/179 -> all green)
Ruling: F22's complete line is reconstructed from git, not written at the time — commit 85e625d carries the CLAUDE.md edit and no ledger line was appended for it; the resume rule says git wins for what exists, so the line is written now rather than the block re-executed — costs nothing, the edit is in the tree and verified by reading the table
F22: complete (commit a927188..85e625d, CLAUDE.md Feature Injection System table names add.review under qa-pipeline; no gate covers this file, checked by hand)
F23: complete (commits 85e625d..bffd1a1, build.js clean at 45 points / 228 nodes / 873 edges, L2.4 until-green scan RED->GREEN)
Ruling: the build did NOT end at F23 — the plan's last block is done but the full cli suite is red in three suites no F-block owns, so F24-F29 are opened below rather than proceeding to the review pass with a red tree; the plan's own Reviewer Handoff gap 5 ("text still describing the old shape") and the F20b precedent ("a pinned suite the plan did not enumerate") are what authorise them — costs six extra commits if a reader expected the plan's scope to be closed at F23 with the regressions filed separately
Ruling: F24 fixed TWO add-ecosystem rows, not the one I opened it with — the fix-agent row still said "ONE area / one area-scoped slice" after F6, and the add.build "implementation complete" suggestion row still promised QA judgement unconditionally after F15/F16; both are the plan's Reviewer Handoff gap 5 and leaving the second while fixing the first is the same leak — costs nothing, neither row has a level and both are read
F24: complete (commits bffd1a1..819070a, build.js clean at 45 points / 228 nodes / 873 edges; no level covers these rows, read against fix-agent.md's own frontmatter)
Ruling: L2.4's singular-AREA half was a FALSE POSITIVE, so F25 narrowed the scan rather than the tree — the scan tested the file as a whole and both agents are named in add.build.md and the tdd-pipeline fragment, so @test-agent's per-area AREA (kept on purpose by the plan's own decision) read as a fix-agent regression; the ledger's F8/F7/F11 line claiming this half went RED->GREEN was wrong, commands/add.build.md was already an offender at 3eaeb84 — costs a real regression going unseen if the new attribution rule is wrong, which is why it was proved by mutating fix-agent.md and add.build.md:590 and watching both get caught
F25: complete (commits 819070a..890c42f, build.js clean at 45 points / 228 nodes; L2.4 RED->GREEN, delivery level file 19/19, both mutations caught and reverted)
F26: complete (commits 890c42f..cf5614f, build.js clean at 45 points / 228 nodes; delivery level file 19/19)
Ruling: F27 moved six qa-reachability assertions onto the INSTALLED feature-on command rather than onto the fragment file — reading the fragment directly would be cheaper but would assert only that the text exists, and this file is named for reachability, which is the property that the text reaches add.review; the install is materialised once and cached so six installs cost one — costs ~2s of suite time if a reader preferred the cheap read
Ruling: F27 repinned the playwright drive anchor to `contract.` rather than restoring the old anchor line — the old line left the base command with STEP 10 and cannot be brought back without undoing F15/F16; the placement consequence is already pinned in both reachable enable orders by the delivery's own level file — costs nothing, the ledger's MEASURED ruling at F15/F16 is where the behaviour is explained
Ruling: F28 gave docs-knowledge-graph ONE extra source, for add.review only, instead of appending every command's qa-pipeline fragment — a blanket append could turn a genuinely broken wiring green on add.plan or add.build, and only add.review's producer/consumer pair was split across the feature boundary — costs a second entry in EXTRA_SOURCES if another command's wiring is ever split the same way
F27: complete (commits cf5614f..51cf3b5, build.js clean at 45 points / 228 nodes; qa-reachability 43/43, fragment mutation caught and reverted)
F28: complete (commits 51cf3b5..96cc884, build.js clean at 45 points / 228 nodes; mcp-server + docs-knowledge-graph 75/75, slot mutation caught and reverted)
F29: complete (commits 96cc884..a48bcf2, build.js clean at 45 points / 228 nodes / 873 edges, git status --porcelain framwork/ empty)
FULL SUITE after F29: 55 of 55 files pass, 1357 tests pass, 1 skipped, 0 fail (serial, NODE_OPTIONS cleared). The three suites that regressed under T2 are green and no pre-existing suite is red.

--- STEP 7 review pass: 11 auditors (3 scopes + 8 @prompt-review-agent, mode delivery) ---

Ruling: the review's findings are applied in F30-F41 rather than filed — the plan's own Reviewer Handoff names four of the five gap classes they found, and a delivery that audits itself and then ships the findings as a note has not been audited — costs eleven extra commits; the alternative was shipping a command that, with qa-pipeline off, writes a mandatory frontmatter field from an undefined variable
Ruling: REJECTED — test-agent's `- agent: e2e-agent` should be `- mention:` (medium, prompt-review on test-agent). The prose points away from e2e-agent and the agent forbids itself from dispatching, so the reading is right, but the declaration predates this delivery (PR #33, present at 44035ef) and changing an edge type moves the graph for a reason no F-block owns — costs one wrong edge kind surviving until a delivery that owns the graph
Ruling: REJECTED — add.build has no `## Rules` ALWAYS/NEVER section (low, item 3). True, and equally true of add.review, add.plan and add.new: it is a framework-wide gap, not this delivery's — costs the ruler item staying unticked on four commands
Ruling: REJECTED — add.plan-to-ready's `/add.new` is undeclared, and its @consistency-agent DELTA dispatch omits two required inputs (medium ×2). Both pre-existing and unrelated to either half of this plan — costs two real defects staying open in a file this delivery did edit; recorded here so the next reader has them
Ruling: REJECTED — the fragment's `/add.plan`, `/add.build` and `/add.done` should be `{{cmd:}}` (low, diff-completeness). TESTED, not reasoned: converting all four raised three phantom-edge warnings, because the graph's prose sniff reads the bare `/add.x` form and `{{cmd:}}` resolves to a path before it runs. The mixed form is load-bearing — one bare mention keeps the declared edge visible, `{{cmd:}}` gives the reader a resolvable link. Reverted to zero warnings — costs nothing; the convention skill should say this and does not
Ruling: the judge-head/judge-tail split is KEPT though F39 made it vestigial — nothing sits between the two sections any more, so they could be one, but merging moves the injection total and the three suites that pin it for no behavioural gain; the fragment comment now says so in those words rather than claiming the split still guards the anchor — costs a reader meeting a split with no separator, which the comment is there to answer
Ruling: F38 followed the user's decision on the BLOCKED/hotfix collision — BLOCKED and the expected-failure declaration are excluded in MODE = CORRECTION, where the red test is the deliverable, rather than teaching the hotfix flow to accept a BLOCKED as confirmed RED — costs a hotfix losing the BLOCKED escape if a pinning test ever turns out to be the agent's own error rather than the bug's
Ruling: the hotfix CORRECTION cap moved from MAX_ATTEMPTS = 3 to 1, and its final-attempt MODEL escalation was removed — the flow dispatches once and returns to STEP 7 on a non-RED result, so 3 named two attempts that do not exist and the escalation had no earlier attempt to escalate from — costs a hotfix pinning test getting one shot at the declared model where the plan's F10 may have intended a real retry loop
Ruling: the 20 agents whose provider-map.json description differs from their source frontmatter are NOT reconciled here — F30 fixed the two whose contract this delivery rewrote and pinned them; the other 18 differ in wording, not in contract, and sweeping them is its own delivery — costs 18 shipped descriptions staying older than their source
F30: complete (commits a48bcf2..f9b074e, build.js clean at 45 points / 228 nodes; build.test.js 125/125, registry mutation caught)
F31: complete (commits f9b074e..ea01d0e, build.js clean; levels 21/21 with L2.8 + L2.9 added, mutation caught)
F32: complete (commits ea01d0e..4fdf241, build.js clean; no level — a schema reference, read)
F33: complete (commits 4fdf241..938775f, build.js clean; levels + qa-reachability 64/64)
F34: complete (commits 938775f..df764b6, build.js clean; levels + qa-reachability 64/64)
F35, F36: complete (commits df764b6..673a6fc, build.js clean after the undeclared-reference gate caught /add.build named inside a fragment that injects into it)
F37, F38: complete (commits 673a6fc..62ccd80, build.js clean; levels 22/22 with L4.5 added, mutation caught)
F39: complete (commits 62ccd80..0b936b2, build.js clean; levels 23/23 with L2.2 rewritten and L2.10 added — the installed bytes are now identical in both enable orders, which is the plan's L3.4 property)
F40: complete (commits 0b936b2..82bd137, build.js clean, git status --porcelain framwork/ empty)
F41: complete (commits 82bd137..06f75c8, build.js clean; levels 25/25 with L2.11 + L2.12 added and L4.2's vacuous assertion replaced, mutation caught)
FULL SUITE after F41: 55 of 55 files pass, 1364 tests pass, 1 skipped, 0 fail (serial, NODE_OPTIONS cleared).
REVIEW: complete (33 findings, 26 applied, 7 rejected)
