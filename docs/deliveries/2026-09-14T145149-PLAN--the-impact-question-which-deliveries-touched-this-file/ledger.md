# Build ledger — plan: docs/plans/2026-09-14T145149-PLAN--the-impact-question-which-deliveries-touched-this-file.md

BASELINE: ADD_GRAPH_WARNINGS=1 node scripts/build.js — exit 0, zero warnings, at 5911c5c
WORKTREE: .claude/worktrees/impact-question on feat/impact-question-touched-by
DERIVATION: verified inside this worktree — `git log --format=%h -S'"id":"<id>"' -- docs/delivered.jsonl | tail -1` resolves 5911c5c for the 102848 entry

READBACK: complete (5 gaps filled by the cold reader, all real — the plan named what each deliverable must accomplish and not its shape)

Ruling: the new mode is `delivered.sh touched <path>...`, a subcommand parallel to read/write/verify, not a flag on read — `read` takes a free-text query and this takes paths, so overloading it would make one mode's argument mean two things — costs a rename across F1, F3 and the bats suite if a different name is preferred
Ruling: the answer uses the script's existing output contract, KEY=VALUE lines plus JSONL entries, because every other mode does and `mcp/engine.mjs` already parses exactly that split ("A line starting with `{` is an entry; anything else is a KEY=VALUE probe result") — costs a parser change in F3 if a richer shape is wanted
Ruling: each returned entry carries a `layer` field valued `complete` or `curated`, and the counts come back as their own keys — the plan says "never merged into one list" and this keeps them structurally separate while reusing one JSONL stream, rather than inventing a second output format — costs nothing if wrong; the keys are what L2.4 asserts
Ruling: the F3→F7 wiring arm is built BEFORE the F4→F5→F6 deletion arm. The reader is right that the plan names no boundary with F4 landed and F7 not, and rather than ruling that gap closed by fiat, the order is chosen so every stop lands on a boundary the plan does name — costs nothing; the plan permits either order and this picks the one it documents
Ruling: the new cli suite is `cli/tests/impact-question-touched-by.test.js` — vitest discovers by glob, so any `*.test.js` under `cli/tests/` runs, and the name follows the topic the way `knowledge-discovery-question-first.test.js` does — costs a rename if a convention exists that I did not find
Ruling: F5 removes `impactedFiles` from the `harvest` object and adds its own note. VERIFIED, not assumed: `harvest` is a flat count map whose non-zero keys are joined into one "harvested X n, Y m" note at `cli/src/migrations.js:331-335`, so leaving the key there would keep claiming a harvest that never happened — costs a test update in migrations.test.js, which F9 already covers

Ruling: F0 corrects the previous delivery's wrong anchor, tagged [internal] because `docs/delivered.jsonl` sits outside framwork/, cli/ and mcp/ — the same path test that put the roadmap edit in the last build's F0. It writes a NEW line rather than editing the old one, per hard ban 6 — costs one extra index line, which is what a correction costs by design
F0: complete (commits 5911c5c..5b7c4f0, entry verifies live, build.js clean, framwork/ porcelain empty)
F8: complete (commits 5b7c4f0..f4f0c8d, 7 cases RED, 1 guard green, build.js clean)
F9: Ruling: L3.6 asserts byte-identity against the branch point instead of checking for words — a word check failed on `PR_MERGE_COMMIT`, a pre-existing variable, which is the false positive that trains an author to loosen a guard until it means nothing — costs a guard that fails on any unrelated edit to those three files, which is the point
F9: complete (commits f4f0c8d..1b9fc90, 12 assertions RED, 5 guards green, build.js clean)
F1: Ruling: the L1.1 fixture was corrected, not the code. It committed source and index line in two commits, which is the docs/-only shape, not the squash the derivation depends on. `seed_delivery` now commits both together — costs nothing; L1.4 still builds the separate-commit case deliberately
F1: Ruling: paths reach node as one newline-separated blob in a single argv slot, because the node program takes a fixed argv and a variable-length list would need one slot per path — costs a re-shape if the mode ever takes flags as well as paths
F1: complete (commits 1b9fc90..f7b902f, 7 bats cases RED->GREEN, full bats suite 0 failures, build.js clean)
F2: complete (commits f7b902f..1866848, L2.1 and L2.2 RED->GREEN, build.js clean) — T1 closed, repo consistent
F3: Ruling: the delegation is scoped to the DOCS corpus only. The plan's F-block said "touched_by's work-item half delegates" without naming a corpus, and a first pass delegated unconditionally, which broke `--corpus=artefacts` where `files` is the node's own path and the local answer is correct — costs nothing; caught by running the verb against both corpora instead of only the one the plan is about
F3: Ruling: two assertions in mcp-engine.test.js were updated, not deleted. They asserted workItems fed by the fixture's `hotfix-related` attachment — the retired path this plan removes — so they now assert the page half and the degraded shape, which is what that fixture can honestly answer — costs the work-item half being unasserted at unit level; the end-to-end proof is a real user project, recorded in the F3 commit
F3: complete (commits 1866848..67989a6, docs delegation proven on a real project, artefacts corpus restored, mcp-engine 41 green, build.js clean)
F7: complete (commits 67989a6..db36dfb, L3.3 RED->GREEN, build.js clean) — the wiring arm closed, repo consistent
F4: Ruling: `files` stays on the node shape. The plan allowed deleting it "unless something still reads it" — two things do: the `get` action exposes it for both corpora, and the artefacts corpus fills it from the node's own path where it is real. Only the hotfix-related population goes — costs an always-empty field on docs nodes, which is honest: a work item has no file set, the delivery does
F4: complete (commits db36dfb..c203379, 24 lines deleted, both corpora verified, mcp-corpora 31 green, build.js clean)
F5: Ruling: added a BEHAVIOURAL assertion beyond the plan's L3.2, which specified text checks on the source. migrations.test.js never asserted `impactedFiles` at all, so nothing would have caught the note failing to reach a user — the new case runs 0002 over the brownfield fixture and reads the report — costs one slower test (385ms) for the only proof that the user is actually told
F5: complete (commits c203379..f633446, L3.2 fully RED->GREEN including the behavioural case, migrations 25 green, build.js clean)
F6: Ruling: the deleted section carried an active prohibition against writing a new related.md, which the plan did not name. Checked BEFORE deleting that add.hotfix.md:100 and :105 already carry both halves, so the section was the second copy and not the only one. The assertion moved to the command, where it is load-bearing — costs nothing; had the command not carried it, the deletion would have had to stop and ask
F6: complete (commits f633446..cffa509, 23 lines deleted, L3.1 RED->GREEN, docs-knowledge-graph 41 green, build.js clean) — the deletion arm closed, the whole plan implemented

GRAPH: delivered.sh — 3 direct dependants (add.done, add.hotfix, add-knowledge-discovery); all three accounted for: add-knowledge-discovery changed in F7, the other two are named "no change" in the plan's Impact table and L3.6 asserts both byte-identical. All three call `read`, which this delivery did not touch — `touched` is a new mode; shipped before: no entry
GRAPH: add-doc-schemas — 23 direct dependants; shipped before: yes, 2 entries, both live, none gone or superseded
GRAPH: add-knowledge-discovery — 8 direct dependants; shipped before: yes, 1 entry, live
GRAPH: mcp/engine.mjs, mcp/corpora.mjs, cli/src/migrations.js — not graph nodes, so no dependant answer exists for them; `add-artefact-graph`'s standing list covers this (any file that is not an artefact has no node)
GRAPH: no gone or superseded entry names a delivery this plan never mentions
F4/F5: Ruling: four more assertions across mcp-migration.test.js and mcp-server.test.js asserted the deleted legacy contract and were updated, not removed. Found by running the FULL cli suite serially rather than only the files the plan's Impact table named — the table listed four test files and the real count was six — costs nothing; each replacement asserts the successor behaviour with its reason

--- STEP 7 review, 7.3 dispositions ---
Ruling: APPLIED — L2.3, L2.4 and L2.5 were written. Two auditors independently found that every docs-corpus `touched_by` test asserted the DEGRADED path, so the engine's whole parsing block ran in no test: "green by degradation". The new block builds a project in the current format — typed frontmatter, the shipped script at a real install path, one squash commit — and exercises the delegation. L2.5 prints the cost (1486ms) instead of asserting a guessed threshold — costs a slower suite; it is the only proof the feature works
Ruling: APPLIED — doTouched rewritten from 2N git subprocesses to three calls plus one per queried path. Measured on this repository: 10.3s per query before, 2.65s after. A second attempt that read each commit's index blob measured 13.0s and was discarded. The plan had already recorded the cheaper shape and the build took the expensive one — costs a wider `git log` walk on a repository with a very long history
Ruling: APPLIED — CURATED_ONLY is scoped to the HITS, not the index. It counted every unusable entry whether or not it matched, making it a constant per repository (4 of 20 here) sitting beside two answer-scoped numbers — exactly the misreading the two labels exist to prevent
Ruling: APPLIED — `INDEX_PRESENT` added. The plan's L1.6 required an empty answer to be distinguishable from an absent index, and both emitted the same three zeros; the bats case papered over it by inspecting the fixture rather than the output
Ruling: APPLIED — L1.7 tested an absent index, not the shallow history the plan named. The case it asked for is now L1.7b, built by committing an index line no commit carries
Ruling: APPLIED — the skill teaches `unavailable`. `touched_by` can return `workItems: []` WITH a reason, and nothing told a reader that is not "no delivery touched this". It would have been written into RELATED_WORK as `none`, which is the inverse of the mislabelling the step already guards against
Ruling: APPLIED — `matched` has two shapes (paths on complete, anchors on curated) and neither the contract nor the skill said so. Both now do, with a gate
Ruling: APPLIED — mcp/server.mjs's tool description was stale. It is what an MCP client reads to decide how to use the tool, and the skill update does not reach a client that only sees tools/list
Ruling: APPLIED — two JSDoc blocks were stacked on touched_by; the older one still described the pre-delegation behaviour. The insertion added a block instead of replacing one
Ruling: APPLIED — gitnexus is no longer named in STEP 2. STEP 7 of the same file states the rule — "no hard reference to any specific graph plugin" — and naming it in one step while forbidding it in another is the file contradicting itself
Ruling: APPLIED — `.codeadd/` is gitignored. The docs-corpus cache written during F3's proof was untracked, and `delivered.sh`'s corpus is "every file git does not ignore", so it became an anchor-verification haystack member — the exact failure that rule's header says it exists to prevent. Pre-existing .gitignore gap, newly triggered
Ruling: APPLIED — L3.6 narrowed twice. A byte-identity check against `git show main:` throws in CI (actions/checkout defaults to fetch-depth 1 on a detached merge ref) and went red on any future legitimate edit to those files. A shape-matching regex then false-positived on `PR_MERGE_COMMIT` and across lines. What is left is the one unambiguous signal — a flag or record field carrying a merge sha — costs a narrower guard that means what it says
Ruling: APPLIED — commitFiles handles a true merge commit with `-m --first-parent`; `--name-only` prints nothing for one, so a delivery landed as a real merge would have dropped silently to curated
Ruling: APPLIED — cli/tests/hotfix-review-0073.test.js sliced to `indexOf('### hotfix-related (retired)')`, which became -1 when F6 deleted that section. Its assertions passed by accident
Ruling: REJECTED — L3.4 ("both MCP surfaces answer touched_by identically"). It is unsatisfiable as written: `scripts/graph.js` implements no `touched_by` and never did, so there is no second surface to compare. The plan copied the level from a verb that has one — costs the paired-surface guarantee for this verb, which does not exist to be guaranteed
Ruling: REJECTED — changing cli/tests/helpers/brownfield-fixture.js, which the Impact table marked "modify". F5's behavioural test needs that fixture's `hotfix-related` document intact to have something to find. Modifying it would have removed the evidence the test exists to read
Ruling: NOTED, not applied — roadmap 1.3's "Done when" carries two clauses this delivery deliberately did not meet: the migration CONVERTING the legacy list, and "no index at all is a documented git fallback rather than an empty answer". Both were excluded by decisions the user took. The item must be corrected at close-out rather than marked closed as written
Ruling: NOTED — commit 6000d6f carries no F-trailer, and two commits carry a test file their F-block's scope does not name. Each has a ruling; the trailers do not. Costs a slightly harder join between ledger and git log
REVIEW: complete (5 auditors, 38 findings, 14 applied, 2 rejected with reasons, 2 noted for close-out, the rest already fixed in flight or stale)

--- post-review, operator-directed ---
Ruling: APPLIED — the `legacyFileLists` counter and its report note are DELETED, not kept as a report-only read. I had argued a counter is a report and not a compatibility path; the operator's correction is that nobody hand-writes those lists — `/add.hotfix` STEP 12 wrote them — so "informing the author" was never a real argument. What remained was a live read of a dead schema's section name. "Nothing of the dead format survives" is one grep; "everything except a report line" is an exception the next author widens — costs a brownfield user no signal that a legacy `related.md` is on disk, which is correct because it is not their file and nothing reads it
Ruling: APPLIED — L3.1's guard widened from `hotfix-related` alone to `hotfix-related|Impacted Files|parseImpactedFiles`. It passed for the whole delivery while `migrations.js` still read `sectionBody(..., 'Impacted Files')`, because the section name is not the schema name. A guard that names one spelling of a dead format guards one spelling
Ruling: APPLIED — roadmap 1.3's "Done when" corrected rather than left for the close-out to reconcile. The conversion clause is recorded as REJECTED with the two findings that killed it; the git-fallback clause moved to the new item 1.4, where a project with no index is the extreme case of a project whose documents the MCP cannot read
Ruling: APPLIED — roadmap 1.4 registered, as directed: a doctor reporting per-file schema fitness plus an opt-in skill that rewrites feature headers. Recorded with its open question unanswered (MCP action, CLI verb, or both over one implementation) rather than planned — this command does not decide it
