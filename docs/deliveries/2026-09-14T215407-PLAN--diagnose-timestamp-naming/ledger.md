# Build ledger — plan: docs/plans/2026-09-14T215407-PLAN--diagnose-timestamp-naming.md

Ruling: built the new path exactly as the plan's Scope->Includes states it, `docs/diagnose/YYYY-MM-DDTHHMMSS-<slug>.md` with no DIAG--/type infix, though the readback flagged that neither real sibling filename it could see (plans' PLAN-- infix, brainstorming's session+NNN suffix) is a bare form — the plan's Validated Decisions table already grounds this: path identity and the DIAG-<slug> frontmatter ID are deliberately independent "in every other timestamped doc type" — costs a follow-up rename of already-written docs/diagnose/ reports if that reasoning is wrong and a type-marker infix turns out to be needed
Ruling: ran all 3 of the Validation Matrix's numbered L1 checks (2 greps + node scripts/build.js clean) rather than the 2 the Reviewer Handoff/RED-expectations text counts — the matrix is the more specific spec and Global Constraints independently requires build.js clean anyway; costs nothing if wrong since it is strictly more verification, but I flag the plan's own count mismatch in STEP 10 rather than silently matching the undercount
F1: complete (commits c8793ebead..bf8b39ff18, L1: 2 greps identical form + build.js clean 0 new warning (228 nodes/882 edges, same as baseline); L2: independent cold read confirms STEP 7.1 and STEP 8.3 state the same path)
GRAPH: product/command/add.diagnose — direct dependants (impact --depth 1): product/command/add (HANDS_OFF_TO), product/command/add.brainstorm (HANDS_OFF_TO), product/fragment/plugins/gitnexus/fragments/add.diagnose.md (INJECTS_INTO), product/skill/add-investigation (HANDS_OFF_TO) — none named in the plan; none affected, F1 reworded two path strings inside STEP 7.1/8.3 prose only, the command's contract (inputs, outputs, when it is dispatched) is unchanged; no shipped-before entry (history: 0 matched)
GRAPH: product/reference/add-doc-schemas/references/review.md — direct dependants (impact --depth 1): product/skill/add-cross-sf-consistency (USES_SKILL), product/skill/add-doc-schemas (USES_SKILL) — neither named in the plan; add-doc-schemas is the schema owner the plan itself updates through this file, and add-cross-sf-consistency's dependency is confirmed (grep) to be on review.md's evidence-citation format convention, unrelated to the diagnose-report schema's destination line (L44) that changed; no shipped-before entry (history: 0 matched)
Ruling: accepted @prompt-review-agent's findings on items 3 (Rules table -> ALWAYS/NEVER format) and 5 (duplicated Routes: list vs add-ecosystem's routing table) as mechanical, no decision needed per add-review-discipline's "a blocker the reviewer itself marks mechanical is not a user decision" -- applied both in commit fdc4a95; the item-5 fix initially broke graph closure (phantom edges on /add.hotfix and /add.plan, whose only prose grounding was the deleted list) -- caught by re-running build.js, fixed by naming the three commands inline without restoring the route->command mapping; costs nothing further, build.js confirmed clean (228 nodes/882 edges, same as baseline) after the correction
Ruling: did NOT apply @prompt-review-agent's findings on items 4 (add.diagnose sends "Knowledge base pages" to @feature-history-agent/@git-history-agent, whose own contracts are docs/-only and diff-file-only) and 6 (STEP 3.2 makes Phase-0 user confirmation conditional, STEP 4 dispatches unconditionally, against add-investigation's own mandatory-stop language) -- both are pre-existing defects this plan's F1 never touched, both need a person to pick between two real design forks per the agent's own report ("a person decides which" on both), and both would require editing artefacts this plan's Impact table never named (add-investigation skill and/or the two agents) -- deferring rather than inventing the decision; costs nothing to this delivery since neither is a regression F1 caused, but add.diagnose.md carries these two defects until a follow-up plan addresses them -- flagged in the STEP 10 report
Ruling: did not act on the side-effects auditor's out-of-scope observation (root .gitignore:178's unanchored `.codeadd/` rule also shadows `framwork/.codeadd/` for any gitignore-respecting recursive tool) -- it is [internal]-layer (repo root, per CLAUDE.md's layer table), this plan's only F-block is tagged [product], and the plan names nothing about .gitignore -- the same behavior was independently observed firsthand during this same build (git add printed the ignored-path warning but staged both files correctly, since already-tracked files are unaffected by gitignore) -- costs nothing today since build.js reads via fs.readdirSync (confirmed unaffected) and already-tracked files stage fine regardless, but a NEW file created directly under framwork/.codeadd/ would need `git add -f`, and any grep/search tool that respects .gitignore silently skips that whole tree unless re-rooted inside it -- flagged in the STEP 10 report as a recommended follow-up (anchor the .gitignore:178 rule to `/.codeadd/` so it only matches the repo-root state directory)
REVIEW: complete (5 findings, 2 applied, 3 rejected)
