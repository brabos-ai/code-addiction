# Build ledger — plan: docs/plans/2026-09-14T215437-PLAN--post-merge-checks-five-where-one-would-do.md

Ruling: @plan-readback-agent flagged 9 gaps, all from correctly not having the target files (add-framework--done.md, close-out-hardening.test.js, add.done.md, done.sh, both cited brainstorming docs) it was deliberately not given — cross-checked each against those files directly; none diverged from what the plan specifies, all resolved cleanly. It also did not surface the abandoned first-draft (object-id resolution) as a live question — it correctly read "Does NOT Include" as settled scope. Continuing with no scope change. Cost if wrong: none identified — every resolution rests on the cited files' own content, not on inference.
Ruling: Extended F1's "no leftover prose describing checks that no longer exist" clause beyond its explicitly cited line ranges (L482, L484, L487-493, L501-503, L509-513) to two more spots that name the deleted checks by count: L246 ("Check 2 of the five..." in the STEP 2.4 Recovery-path table) and L593-594 ("The five post-merge checks..." in STEP 9's report list) — both are leftover prose about checks F1 deletes, which its own bullet forbids even though not individually line-cited. Cost if wrong: two sentences carry a rephrasing not requested by line number; both stay factually true about the surviving two-check gate, so reverting is a one-line revert each.
Ruling: F3 gives no exact text or location for add.done.md's recorded parity-gap note. Placed it as a new paragraph in STEP 8's shared intro (after "including the one that asks", before "### 8.0") — the one spot both the PR route (8.1) and the Local route (8.2) share before branching, and both routes call done.sh's do_cleanup, which is what the note describes. Cost if wrong: a misplaced note is a cut-paste to wherever a reviewer prefers; its content is checked directly against do_cleanup's actual two checks (read at framwork/.codeadd/scripts/done.sh:422-495).
F1: complete (commits c8793ebeadc17c9c3daea586a948c9d49feb9b66..3c0d752, build.js clean no new warning, framwork/ untouched, L1 greps 1/2/3/4 pass, L1.5 pending F4)
Ruling: F1's STEP 6 sentence literally spelled out `docs/plans/<id>--ledger.md`, which regressed the pre-existing cli guard test L3.8 (ruler item 5 — the ledger's exact path belongs to add-build-ledger alone, not restated in add-framework--done.md). Found while running F2's own validation, not F1's. Fixed by saying "the ledger" generically, matching the file's existing style elsewhere (STEP 1.3). Cost if wrong: none — the reworded sentence still carries both properties L1.4 requires (assembled last, nothing writes after), verified by re-reading it.
F1: correction (commit 7b6a547, build.js clean no new warning, cli close-out-hardening.test.js 12/12 pass)
Ruling: Ran `npm install` at repo root and in `cli/` — this worktree started with no node_modules anywhere, so the cli suite could not run at all (ERR_MODULE_NOT_FOUND on vitest). Necessary to produce any L2 evidence. Reverted the root `package-lock.json` drift the install produced (two `"peer": true` flags removed by a different local npm version) with `git checkout --` before committing, since it is environment noise unrelated to this plan. Cost if wrong: none — the revert was verified with `git status` before F2's commit; cli/package-lock.json was untouched.
F2: complete (commit 6be697a, build.js clean no new warning, cli close-out-hardening.test.js 12/12 pass including L3.3 and L3.4, sweep of cli/tests for old STEP 8 text found nothing else)
Ruling: F3's first draft named the internal command literally ("`/add-framework--done`") when explaining the parity. `build.js`'s graph gate refused it: "internal command named by a distributed artefact... a user installing this artefact has no such command." Reworded to describe it without naming it ("This project's own internal development tooling runs a close-out with exactly the same two checks"). Cost if wrong: none — the gate is authoritative here and the reworded text still lets a cold reader of add.done.md alone state why the check is absent, without needing the internal file.
Ruling: `git add framwork/.codeadd/commands/add.done.md` printed "ignored by one of your .gitignore files" (bare `.codeadd/` at .gitignore:178, meant for an end-user's installed `.codeadd/` and incidentally matching this already-tracked source tree too) and exited 1, but `git status` showed the change staged (M in the index column) regardless — a known git quirk where the warning fires on an already-tracked path without actually blocking the stage. Verified staged before committing; not a scope item to fix. Cost if wrong: none — the commit that followed is proof the content landed; `git show` on it would show an empty diff if it hadn't.
F3: complete (commit e748dc8, build.js clean no new warning after the naming fix)
F4: complete (commit 0512abc, build.js clean no new warning, framwork/ untouched, L1 grep 5 now zero matches, cli close-out-hardening.test.js 12/12 pass)
Global Constraints verified after F4: full cli suite (npm --prefix cli exec vitest run --no-file-parallelism) 57 files / 1445 passed / 1 skipped / 0 failed; npm run test:scripts (bats) exit 0, 430/430 ok, 0 not-ok.

Ruling: STEP 7's auditor fan-out was collapsed from 3+N separate dispatches to ONE read-only dispatch covering all four scopes — the account hit a hard session rate limit (HTTP 429) that killed the original build session and every auditor it had dispatched, twice. One agent carrying all four scopes is the most independent read obtainable under that limit, and it is still a reader that did not write the delivery. Cost if wrong: the four scopes share one reader's attention instead of four, so a finding that only surfaces when one scope is read in isolation could be missed; the scopes were given explicitly and the report answers each separately, which bounds that risk.
GRAPH: .claude/commands/add-framework--done.md — direct dependants: none (entry-point internal command); shipped before: 3 live entries, newest 2026-09-10T203053-PLAN--close-out-hardening, the delivery that ADDED the five checks this one cuts to two — a deliberate partial supersession of live prior art, named in the plan, not a rediscovery
GRAPH: framwork/.codeadd/commands/add.done.md — direct dependants: 11 (add, add.build, add.plan, add.plan-to-ready, add.pull-request, add.review, fragments docs-pruning/add.done, qa-pipeline/add.review, gitnexus/add.done, add-doc-schemas, add-qa), all HANDS_OFF_TO or soft and none affected — F3 adds a documentation paragraph and changes no contract; shipped before: 1 live entry, 2026-09-11T014333-PLAN--product-close-out-parity
GRAPH: cli/tests/close-out-hardening.test.js — not a graph node (a test file is not an artefact); no dependants to query
REVIEW: complete (0 findings, 0 applied, 0 rejected)
