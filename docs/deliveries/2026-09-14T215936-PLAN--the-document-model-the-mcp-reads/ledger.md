# Build ledger — plan: docs/plans/2026-09-14T215936-PLAN--the-document-model-the-mcp-reads.md

Worktree: `.claude/worktrees/doc-model` — branch `feat/mcp-document-model` off `origin/main` (c8793eb).
Graph warning baseline, measured once at the branch point: **zero**.

--- STEP 4 readback, 4.1 dispositions ---
Ruling: the readback's gap C is a REAL PLAN DEFECT. Scope's "Does NOT Include" names four relation types (`caused_by | depends_on | part_of | links_to`) while Validated Decisions says "four of five are DCMI terms". The fifth is `superseded_by`, which the design doc's DCMI table carries as `dcterms:isReplacedBy`. The reader guessed `related` and flagged low confidence, correctly. F10 documents five, and the plan line is corrected — costs nothing; leaving it would have shipped a reference naming the wrong vocabulary
Ruling: gap B answered from the tree, not invented. Migration 0003's page-id transform is exactly `wikiNodeId()` in `mcp/corpora.mjs` — `wiki/` plus the path after `.codeadd/wiki/` with `.md` stripped. The plan said "from the path it already answers by" and never cited the function. Citing it is what makes L3.2's "no query result changes" checkable — costs a rewrite of 0003 if the transform is later changed in one place and not the other, which L3.2 catches
Ruling: gap E decided — the F10 generator is a STANDALONE script, and L4.1 running in CI is the enforcement. The plan excluded "a build gate in build.js"; it did not exclude a test. The reader's worry (drift the first time someone edits types.mjs) is answered by L4.1 failing in CI, which is mechanical without being a build gate — costs one manual regeneration step before a commit that edits the registry
Ruling: gap F decided — the full cli suite MAY be red in the F2..F6 window, but only for tests asserting a retired type name. Every block still enumerates its failures and any failure outside that set STOPS the block. The plan called the window "not shippable" and forbade stopping in it, but never said what a red suite there means — costs a slower per-block check than "scoped suite only"
Ruling: gaps A and D need no action. A (the registry's data shape) is a plain exported object keyed by type, which is what the design doc shows; the reader assumed it and was right. D (the two OKF divergences) the reader correctly refused to invent — they are in the design doc, which the plan cites rather than restates, per add-plan-authoring's rule that a plan points at the design and does not copy it. The reader not having the design doc is the instrument working, not a document defect
Ruling: the readback derived the same execution order the plan states, including F10 waiting on F4 and F5. No divergence to reconcile

--- pre-F1 findings, from reading the tree ---
Ruling: `prd` GOES INTO the registry, against the plan's F2 text. F2 says "must NOT carry `product`, `owner` or `prd` — their removal is a separate planning session's scope." That is wrong about `prd`: `docs/plans/2026-09-14T215223-PLAN--remove-owner-product-onboarding.md:119` states `prd` is explicitly OUT of its scope, writes to `docs/prd/PRD[NNNN]-<slug>.md`, "an unrelated directory in an unrelated schema category". Excluding it would make every PRD an unknown type — costs one registry entry to remove if that other plan later widens
Ruling: `audit-report`, `diagnose-report`, `brainstorm` and `prd` are classified **work-item**, not attachment. Each carries its own id (`AUDIT-<date>`, `DIAG-<slug>`, `BRN-<slug>`, `PRD[NNNN]`), lives in its own directory (`docs/audit/`, `docs/diagnose/`, `docs/brainstorm/`, `docs/prd/`) with no `*-about` sibling, and declares `related: []` that is routinely empty. As attachments they resolve to no owner and are dropped — the exact defect this plan removes. The design's own rule decides it: a document whose role is "a thing you look up" is a work-item, which is what reclassified `setup-receipt` — costs four registry entries if a later reading makes them annexes of something
Ruling: the emitted `kind` value `reference page` becomes `page`; `work item` is unchanged. The old name was coined when `reference` was the only page type; calling a `tutorial` a "reference page" is false. This is a BREAKING change to the result shape and to the `kind` filter argument of `search` and `orphans` — verified that argument declares no enum (`mcp/server.mjs:61`, `:142`) and that no product artefact passes the literal. It reaches the changelog's Breaking section and F8's skill table — costs any external consumer filtering on the old string

--- execution ---
F1: Ruling: L5.1 first drafted with `if (search) expect(true).toBe(true)` — an assertion asserting nothing, next to a dynamic import wrapped in a catch that hid its own failure. Rewritten to call `run('search', ...)` and assert the hit's id AND its kind. Caught by reading my own diff before committing — costs nothing; shipping it would have left a level that passes whatever the engine does
F1: complete (commits c8793eb..cc6c852, build.js clean, matrix 29 RED of 30 — the green one is L4b.4, which the plan marks a regression guard, not a RED level)
F2: Ruling: SUPERSEDES the pre-F1 ruling that said `work item` stays unchanged. The kind values are `work-item`, `page`, `attachment` — all hyphenated. That earlier ruling would have shipped `work item` (a space) beside `page`, an inconsistent vocabulary in a field that is also a QUERY ARGUMENT on search and orphans, where a value with a space is awkward to pass. `page` was already breaking, so the shape changes either way; a half-rename buys nothing and costs a permanent inconsistency. Caught because F1's own levels asserted `work-item` and the ruling said otherwise — costs one more line in the changelog's Breaking section
F2: Ruling: `product` and `owner` are absent from the registry while still present in the tree, so a project holding them reports them as unknown types until the sibling plan removes them. That is a REPORTED finding, not a drop, and it is the honest state of a type being retired elsewhere — costs a noisy line in a doctor run between the two merges
F2: complete (commits cc6c852..2cf649b, build.js clean, L1.1/L1.4/L1.5 GREEN — L1.2 and L1.3 still RED and belong to F3; full serial suite 57 of 58 files pass and every failure is inside the new RED file, so no regression)
F3: Ruling: L5.1's fixture was wrong, not the product. It built a wiki-only tree and called the ENGINE, whose probe requires `docs/` to exist. Fixture fixed. But the underlying behaviour is a real, pre-existing edge worth recording: the docs corpus declares `roots: ['docs', '.codeadd/wiki']` and `probe: 'docs'`, so a project that ran /add.wiki and never /add.new has a wiki the corpus refuses to open. Outside this plan's scope — widening a probe is a behaviour change nobody asked for — costs a wiki-only project no MCP answers until someone plans it
F3: Ruling: a type declaring a `root` is refused OUTSIDE it, with that reason, rather than falling through to attachment. The plan did not specify this. A `reference` sitting in `docs/` is not a page, and letting it become an attachment makes it fail later for an unrelated reason — costs one branch if a type should ever be allowed in two roots
F3: complete (commits 2cf649b..7282839, build.js clean, L1 fully GREEN plus L2.1/L2.2/L2.3/L2.6/L5.1/L5.2/L5.4. Full serial suite: 41 failures outside the RED file across mcp-engine, mcp-corpora, mcp-server and mcp-migration, ALL traced to one cause — the shared docs-corpus fixture still writes `type: feature-about`, so those documents are now unknown types. That is the F2..F6 window the plan declares, and F6 closes it)
F4: complete (commits 7282839..1f9955d, build.js clean, L2.5 and L5.3 GREEN. Window unchanged: every non-RED failure still traces to the shared fixture's retired type name)
F5: complete (commits 1f9955d..86d4be7, build.js clean, L2.3b GREEN, T1 closed, 16 of 30 levels pass)
F0: Ruling: NEW F-BLOCK, [internal], not in the plan. `.gitignore:178` carried `.codeadd/` with no leading slash, which git matches at ANY depth — so `framwork/.codeadd/`, the whole product source of truth, was ignored. I shipped that pattern in the previous delivery. Nothing broke because .gitignore does not reach a tracked file and all 216 were tracked; what broke was every file added afterwards, invisible to `git add` and `git status` with no error. Found because `git add framwork/.codeadd` refused this build's F6. Anchored to `/.codeadd/` and verified both directions — costs nothing, and leaving it would have swallowed the next new command or skill silently
F6: Ruling: `templates/feature-about-template.md` is NOT renamed, reversing the plan's own N1 note. The name refers to the `about.md` DOCUMENT it templates, which still exists, not to the retired `type:` value — and renaming breaks the `feature-<aspect>-template` convention `feature-discovery-template` also follows. L2.4's regex excludes it by lookahead. Found by reading what the name means instead of what it matches — costs one surviving `grep feature-about` hit, in one file, for a stated reason
F6: Ruling: the sweep found SIX files neither the plan's Impact table nor the plan review named — add.build.md, add.hotfix.md, add.new.md, add.qa-setup.md, plan-reviewer-agent.md, hotfix-review-0073.test.js. Third incomplete hand-written list in this delivery. The lesson is already written into L2.4 itself rather than into another table — costs nothing
F6: Ruling: `feature-discovery` is NOT added to the registry despite appearing in the shared fixture. No command writes it, no schema declares it, and its template carries no frontmatter at all — it is a legacy type like `hotfix-related`. Both stay in the fixture ON PURPOSE so the report-by-name path has something to exercise; `stats` now counts 5 skipped where it counted 3, and the assertion says why — costs one registry entry if a command later starts writing it
F6: Ruling: one commit briefly carried F0 and F6 together, because a failed `git add` had left paths staged and the next commit took them. Split with `git reset --soft` before any push. Caught by reading `git show --stat` on my own commit — costs nothing; leaving it would have made the ledger's commit ranges name work they do not contain
F6: complete (commits 86d4be7..205f182..ee1b06a, build.js clean, L2.4 GREEN. Full serial suite 55 of 58 files pass; the remaining failures are mcp-document-model (levels owned by F8/F9/F10/F11) and mcp-migration, which is migrations.js:233's second suffix test — F9's scope, and the window closes there)
F7: Ruling: the contract lived in THREE artefacts, not one. add.wiki.md was the plan's only named file; add-architecture-discovery/SKILL.md carried the same three-value enum, and spine-analyzer.md carried it in a different ORDER and had survived four manual passes. Added L2.4b, which compares the offered page types against the registry's page list — L2.4 cannot see this, because an artefact offering three of four names nothing retired, it is merely incomplete — costs one more level; without it the fifth file ships
F7: complete (commits fbdeaca..26bec23 amended, build.js clean, L2.4b GREEN)
F8: complete (commits 26bec23..9b3b53c, build.js clean, L4b GREEN in full)
F9: Ruling: an existing assertion pinned 0002 as the LAST entry in MIGRATIONS. That was a snapshot of the day it was written, not an invariant, and the third migration broke it while breaking nothing real. Rewritten to assert what the registry promises — each id once, in order — costs nothing
F9: complete (commits 9b3b53c..6d42ffd, build.js clean, L3 GREEN in full, 57 of 58 files pass, the F2..F6 window is CLOSED)
F10: Ruling: the reference/registry enforcement is L4.1 in CI, not a gate in build.js. The plan left this open. A test that fails when the generated table and the file disagree is mechanical without being a build gate, and the plan excluded the gate, not the test — costs a manual `node mcp/generate-reference.mjs` before a commit that edits the registry
F10: complete (commits 6d42ffd..8da550c, build.js clean, L4.1 and L4.2 GREEN)
F11: Ruling: TWO LEVELS OF MY OWN WERE WRONG and were fixed here rather than left green-by-luck. L3.6 grepped migrations.js for the suffix test and failed on the COMMENT explaining its removal — a level that cannot tell code from a note about code. It is now behavioural: it runs 0002 over documents carrying the new names and asserts the wikilink appears, which is what its name always claimed. L2.4 flagged migrations.js for its RETIRED_TYPE_NAMES map, which is legitimate migration input, so that one path is allowed with the reason written in the level — costs a slower L3.6; the grep version would have passed forever once someone deleted the comment
F11: complete (commits 8da550c..ab6349f, build.js clean, internal block touched no framwork/ path, matrix 31 of 31, full serial suite 58 files and 1476 tests GREEN)

--- STEP 7.2, the graph ---
GRAPH: add-doc-schemas — 23 direct dependants; shipped before: yes, live, no gone or superseded entry
GRAPH: add.build — 17 direct dependants; shipped before: yes, live
GRAPH: add.wiki — 16 direct dependants; shipped before: yes, live
GRAPH: add.new — 13 direct dependants; shipped before: yes, live
GRAPH: add-knowledge-discovery — 8 direct dependants; shipped before: yes, live (2026-09-14T102848 and 2026-09-14T145149 both touched it)
GRAPH: add.hotfix — 8 direct dependants; shipped before: yes, live
GRAPH: add-plan-review — 8 direct dependants; shipped before: yes, live
GRAPH: plan-reviewer-agent — 8 direct dependants; shipped before: yes, live
GRAPH: add.qa-setup — 7 direct dependants; shipped before: yes, live
GRAPH: add-architecture-discovery — 5 direct dependants; shipped before: yes, live
GRAPH: mcp/*, cli/src/migrations.js, cli/tests/*, .gitignore — no node; `add-artefact-graph`'s standing list covers every file that is not an artefact, so no dependant answer exists for them and none was invented
GRAPH: add-framework-product-layer (internal) — internal skill, read by /add-framework--build on its first [product] F-block
GRAPH: the delivery index, --layer product, returns MATCHED_LIVE=7 and NO gone or superseded entry. Nothing in this area was tried and dropped, so no superseded entry names a delivery this plan never mentions
GRAPH: a dependant the delivery did not change is NOT a finding here, and the reason is mechanical rather than a judgement — the rename only reaches a file that quotes the retired literal, and L2.4 sweeps framwork/.codeadd, mcp, cli/src, cli/tests and .claude and returns empty. A dependant that neither quotes it nor reads `kind` is untouched because nothing about it changed

--- STEP 7.3 dispositions ---
Ruling: APPLIED — `id:` reached the CONTRACT and none of the six places that actually run. add.wiki.md declared it mandatory and gated it, while the four dispatch prompts a cold subagent receives listed seven fields without it, and the STEP 4 checklist and STEP 5.3 gate checked that same seven-field list. A page written without `id:` passed both gates and failed later at indexing, for a reason neither gate could name. This delivery reproduced, inside itself, the exact failure it exists to remove — costs nothing; shipping it would have made every generated page unindexable while both gates said pass
Ruling: APPLIED — spine-analyzer.md now POINTS at the contract instead of restating it. Its own skill declares itself the owner in so many words, and this file restated the block anyway; the restatement had already drifted, missing `id:` entirely. Three copies are now two, and the one removed is the one that broke a rule its owner had already written down
Ruling: APPLIED — the sourcing ladder stopped contradicting the two no-op rules at SKILL.md:78 and :230. Same trigger, different outcome, unreconciled, in a skill eight commands load. They govern different things (flow vs sourcing) and now say so, and the git rung is stated as available-never-automatic, which the first draft implied by listing it as a step
Ruling: APPLIED — `mcp/reference.md` gained the corpus rules as a section. The product-layer rule's trigger named "the corpus rules" and the reference covered them nowhere, so an agent whose block changed one could not tell what to update. The trigger was not too wide; the reference was incomplete. It also records the probe-vs-roots gap I hit in F3 — costs one section; without it the trigger names a category nothing answers
Ruling: APPLIED — `product-close-out-parity.test.js`'s L14.4 guard, hollowed out by my own rename. It pinned `kind: feature-about` at add.new's dispatch site; the blind sed left it asserting the bare word `feature`, which appears throughout that file whatever the command does. All three guards now assert the `kind:` prefix — found by the diff auditor, not by me, and it is the strongest argument in this delivery for why a mechanical rename gets read line by line
Ruling: REJECTED — collapsing `add.wiki.md`'s own copy of the frontmatter contract into a pointer. The auditor is right that three statements of one contract drift, and spine-analyzer's copy proved it. But add.wiki.md is the command a user invokes, and a command stating the contract of the artefact it produces is defensible; collapsing it is a structural change this plan never scoped, across a command with 16 dependants. Recorded for a follow-up rather than done here — costs one surviving duplicate pair, now two rather than three
Ruling: REJECTED — having root `CLAUDE.md` delegate its "Two rules bind anyone editing an artefact" to this skill. The duplication is real and predates this delivery: I changed "Two" to "Three" and added a bullet that is NOT duplicated. The fix edits `CLAUDE.md`, which this plan explicitly excludes and which a [product] F-block may not touch at all — a product delivery rewriting the repository's own overview on its own initiative is the thing the build command's prohibition names. Follow-up
Ruling: NOTED — the plan's Impact table named `mcp/engine.mjs` for F3 ("reads kind as declared rather than inferred") and that file carries no diff. The auditor verified why and it is a plan defect, not a gap: engine.mjs only ever READ `node.kind`, a field set entirely inside corpora.mjs's classify(); it never inferred anything. The plan's premise about that file was wrong. Nothing to implement
Ruling: NOTED — the plan's Impact table listed four `add-doc-schemas/references/*.md` files as F6 "modify" and none were touched. Correct: none carried an `-about` literal, and the reclassification of `setup-receipt`, `audit-report`, `diagnose-report` and `prd` to work-item lives in `mcp/types.mjs` alone. The table conflated "the registry declares this type" with "this document needs a text edit"
Ruling: NOTED — seven `.md` artefacts changed by the rename alone were NOT individually ruler-audited: add.build.md, add.hotfix.md, add.new.md, add.qa-setup.md, plan-reviewer-agent.md, add-doc-schemas/SKILL.md and add-plan-review/SKILL.md. Each changed by a one-token literal that does not touch its structure, and the diff-completeness auditor read every renamed sentence in all of them for prose damage and found none. Dispatching a full eight-item ruler per artefact for a one-token rename buys nothing — costs the chance that a rename exposed a structural defect that only a ruler pass would name
Ruling: APPLIED — backend-analyzer.md, frontend-analyzer.md and database-analyzer.md restated the wiki contract inline, each missing `id:`, and I had fixed only spine-analyzer.md. add.wiki.md's self-bootstrap hands a specialist ONE file, so a cold dispatch of any of the three wrote a page the indexer drops — while the contract two files away and add.wiki.md's own gate both said `id:` was mandatory. Four subdocs, four drifted copies, and the owner's claim that "Analyzer-specific sections below reference this one, they never restate it" was false for all four
Ruling: APPLIED — L2.4c added, and it is the FOURTH enumerator this delivery needed. Four hand-written passes produced four different incomplete answers about the same class of file: the plan's Impact table, the plan review, the rename sweep, and my own fix after the sixth audit. Proved it bites by planting a violating file and watching it name it — a level that is green the moment it is written has not been shown to test anything
Ruling: NOTED — L2.4c is written line-wise, not as one multiline regex, because the first draft's regex was silently broken by an escaping bug and the suite failed to parse. A regex that stops matching without saying so is the same failure the level exists to catch
REVIEW: complete (7 auditors, 13 findings, 9 applied, 2 rejected with reasons, 2 noted as plan defects needing no code)
F0: complete (commit 205f182, build.js clean, both directions verified — a new file under framwork/.codeadd/ is reported by git status, and .codeadd/ at the root is still ignored). Written at close-out: the build recorded F0's ruling and omitted its complete line. The 2.2 gate reads the PLAN's Execution Order, which is F1..F11, so it passed either way — but a block with a ruling and no completion line reads like a block that was decided and never finished
