# Build ledger — plan: docs/plans/2026-09-15T224612-PLAN--pipeline-ceremony-rebalance.md

READBACK: complete — restatement matches the plan on goal, scope and execution order. Six gaps it filled in on its own become the rulings below.

Ruling: the three-fact test is defined in the design doc, not the plan, and each F-block carries the definition into the artefact it writes — intent gaps a builder would have to guess, irreversible actions (migration, data deletion, public contract change, external integration), footprint beyond a handful of files or into an area no delivered feature has touched — why: the plan points at the design rather than restating it, which is `add-plan-authoring`'s own rule, but the artefact must be cold-executable — costs a rewrite of F5's STEP 3 branch and F12's trigger if the design meant different predicates
Ruling: the short-plan shape for a `bounded` design is the plan template's mandatory sections only, dropping the five the template already marks *(optional)* — Current State, Accepted Trade-offs, Risks and Mitigations, the Combination matrix and References — why: the template already declares which sections a single-artefact plan may omit, so the shape exists and only needed naming — costs re-authoring F8's section list if a bounded plan was meant to be structurally different rather than shorter
Ruling: F10's size branch on `bounded` writes the short-plan shape and skips the consultative questionnaire, and it does NOT skip STEP 3.2's graph gate or STEP 3.3's delivery-index question — why: the F-block explicitly protects both, so a branch that skipped them would contradict its own instruction — costs a bounded internal plan shipping without a blast-radius answer if wrong
Ruling: the structured-question tool is named by function, never by a provider's tool name, in both layers — why: every dispatch in this framework is intent-based, and the internal layer having one provider is a reason the flag is unnecessary, not a reason to hardcode an identifier — costs nothing mechanical; a later provider change would have needed the rename
Ruling: the product layer's third path is `spike` and it gets no intent file and no offer to continue, exactly as the internal layer — why: `add.brainstorm` carries the same three-path classification and the same rule that a spike's output is throwaway — costs leaving spike on the old dead-end handoff if the plan meant otherwise
Ruling: `## Open` reads the literal `None` when closed; a section present but empty is treated as absent and falls back to the full ceremony — why: the plan's stated safety property is that a missing signal reads as "not closed", and an empty section is a missing signal — costs a confirmation screen that never fires on a correctly-closed file if the convention was meant to be "zero bullets means closed"
Ruling: the validation gate gained a general exemption for a declared narrowing, and the `tags:`-specific clause that carried half of it now points at it — why: the gate demanded `## TL;DR` and `updated:` unconditionally, so a schema that relaxes them was unusable the moment it was written, and F1's own instruction says "with its validation gate" — costs nothing if wrong; the narrower per-field clause it replaced still exists in history
F1: complete (commits af3a742..e7c26e4, build.js clean — exit 0, zero warnings against a zero baseline)

Ruling: the product-benchmark duty moves verbatim — the line is `Combine WebSearch + model knowledge for product feature benchmarks` and carries no reference to `add.new`'s position — why: read the line before moving it; nothing in it is positional — costs a dangling reference if a later edit makes it positional
F1: complete (commits af3a742..e7c26e4, build.js clean)
F2: complete (commits e7c26e4..e0fe804, build.js clean + cli mcp-document-model 32/32 pass)
F3: complete (commits e0fe804..d9fe666, build.js clean + per-provider flag verified: claude true, four false)
Ruling: Phase 3 loads the `feature` schema, not `feature-about` as the plan wrote it — why: `feature-about` does not exist; the Schema Index names it `feature`, and F6 carries the same correction — costs nothing; the plan text is stale, the registry is not
Ruling: the inline about.md template was deleted rather than migrated — why: it carried a `## Spec` JSON block that `building-commands` prohibits outright, and it duplicated the schema shape — costs re-authoring if some consumer depended on its JSON blocks, which no schema declares
F12: complete (commits d9fe666..5f30648, build.js clean)
Ruling: deleting the old STEP 5 forced four `uses:` declarations out, not the two the plan named — `add-plan-review` and `add-review-discipline` lost their only mention in the prose, and a declared edge nobody names fails the graph gate — why: the gate is symmetric, so removing a dispatch removes its skill too — costs restoring both lines if some later step needs them
Ruling: STEP 6 was renumbered to STEP 5 rather than leaving a gap — why: the ruler requires sequential integer numbering, and every internal reference was updated in the same block — costs a stale cross-reference from any artefact citing `add.brainstorm` STEP 6; `add-plan-review` is the only one and F6 rewrites that line
F4: complete (commits 5f30648..f6d37d8, build.js clean + zero stale STEP 6 references)
Ruling: STEP 6 lost its authoring rules but KEPT its Codebase Analysis dispatch — why: dispatching an agent is orchestration, the same reasoning F5 uses to keep the STEP 3 discovery dispatch; the plan said "STEP 6 removed" meaning its authoring content — costs a lost discovery.md refresh if the dispatch was meant to go too
Ruling: the STEP 8 reviewer became path-conditional rather than unconditional — why: the plan says "makes the STEP 8 reviewer conditional on the heavy path" and the accepted risk row covers the light path having none — costs an unreviewed about.md on bounded, which is the risk the user accepted
F5: complete (commits f6d37d8..0a381ce, build.js clean + injection map verified 45 points, add.new graph-map intact, add.brainstorm zero)
Ruling: the plan asked F6 to correct a stale `/add.plan` STEP 13 reference; it already read STEP 12 — why: commit af3a742 renumbered it before this build started, so the correction was already on disk — costs nothing
F6: complete (commits 0a381ce..bbfc435, build.js clean)
F7: complete (commits bbfc435..47bd2b5, build.js clean + .claude/ untouched, internal sibling byte-identical)
Ruling: `/add-framework--plan` was declared as `mention:` rather than `command:` in add-plan-authoring — why: the graph gate rejected the undeclared prose reference, and the file already treats build, done and brainstorm as mentions; the prose describes what that command does with this shape rather than handing off to it — costs an edge of the wrong type, which `impact` reads but `dependencies` excludes
F8: complete (commits 47bd2b5..6712769, build.js clean + framwork/ untouched + 867 edges vs 866 baseline, the one mention added)
Ruling: STEP 3 routing table cell for spike changed from "Runs in full" to naming 7.3 explicitly — why: 7.3 now stops instead of routing on that path, and "runs in full" contradicted it; ruler item 6 forbids two steps disagreeing — costs nothing
F9: complete (commits 6712769..204dbe0, build.js clean + framwork/ untouched)
Ruling: an invocation carrying `path: spike` is treated as NO intent file and runs everything — why: F9 stops the brainstorm routing spikes here, so a spike arriving means the user came deliberately, and the safe fall is toward more ceremony — costs an unnecessary full pass on a spike someone deliberately escalated
F10: complete (commits 204dbe0..f561b88, build.js clean + framwork/ untouched)
F11: complete (commits f561b88..16229a0, build.js clean + framwork/ untouched)

GRAPH: product/skill/add-doc-schemas — 21 direct dependants, every command in the pipeline among them; 3 delivery entries, latest live (the-document-model-the-mcp-reads). Change was purely additive: a new schema and a gate exemption. No dependant reads a removed thing
GRAPH: product/skill/add-feature-specification — add.brainstorm, add.new, AND product/agent/discovery-agent; no delivery recorded
GRAPH: product/command/add.brainstorm — add, add.new, add-feature-specification, add-review-discipline; no delivery recorded
GRAPH: product/command/add.new — add, add.audit, add.brainstorm, add.build, add.diagnose, gitnexus fragment (INJECTS_INTO), add-doc-schemas, add-feature-specification, add-id-convention, add-plan-review, add-qa-migration, add-review-discipline; no delivery recorded
GRAPH: product/skill/add-plan-review — plan-reviewer-agent, add.new, add.plan, add-cross-sf-consistency, add-doc-schemas, add-feature-readback, add-review-discipline; no delivery recorded. add.brainstorm is correctly gone from this list
GRAPH: product/skill/add-review-discipline — add.build, add.new, add.plan, add.plan-to-ready; no delivery recorded. add.brainstorm correctly gone
GRAPH: internal/skill/add-plan-authoring — add-framework--build, add-framework--done, add-framework--plan; no delivery recorded
GRAPH: internal/command/add-framework--brainstorm — none (entry point); no delivery recorded
GRAPH: internal/command/add-framework--plan — add-framework--brainstorm, add-framework--build; no delivery recorded
GRAPH: internal/command/add-framework--done — none (entry point); 4 delivery entries, latest live (post-merge-checks-five-where-one-would-do)

Ruling: `discovery-agent` is a direct dependant of `add-feature-specification` that the plan never named, and it stays untouched — why: it declares the skill in its frontmatter `skills:` list as available context, not as a dependency on any section; nothing it does reads the Phase 2 questionnaire or the Phase 3 template that F12 removed, and it is read-only so it never allocates an id or runs a gate — costs a discovery-agent loading a skill whose shape changed under it, if some prompt of its did lean on the old template

REVIEW: complete (13 auditors, ~30 findings; 2 verdicts blocked, 1 finding rejected with evidence)
Ruling: the auditor claim that `/add.plan` at add.brainstorm.md:47 fails build.js with "undeclared reference" is REJECTED — why: ran `ADD_GRAPH_WARNINGS=1 node scripts/build.js` against that exact tree and it exits 0; the name sits inside a fenced code block. A `- mention: /add.plan` is added anyway as cheap insurance — costs one declared edge that may be unnecessary
Ruling: F5's ledger line disclosed keeping only the Codebase Analysis dispatch and did NOT disclose keeping STEP 6.1's Relations/tags table, which the plan named for removal — that was an undisclosed deviation, and F13 below executes the removal the plan asked for — why: two auditors reached it independently, and the consequence is functional: the brainstorm offer-to-continue path cannot populate two mandatory schema sections — costs re-splitting if the table genuinely belonged in the command
USER DECISION: add.brainstorm runs the schema validation gate at STEP 5.1 (not declared exempt)
USER DECISION: fix the 7 failing tests only; the plan's RED-first matrix becomes a roadmap item rather than code in this delivery
USER DECISION: the add-framework--brainstorm STEP 6.2 vs internal add-review-discipline contradiction is LEFT AS IS — it predates this delivery, no F-block touched it, and deciding which artefact yields is design work
F13: complete (commits 16229a0..0df7e1c, build.js clean) — moves Relations/tags to the skill, fixes the STEP 8 contradiction and two dead references
F14: complete (commits 0df7e1c..9c50972, build.js clean) — gate at 5.1 per user decision, spike-safe handoff, Intent line narrowed to /add.new
Ruling: the `- mention: /add.plan` insurance was REMOVED — why: build.js emitted a new WARN saying the prose no longer names it, which both confirms the rejected finding was wrong AND makes the entry a stale acknowledgement the build explicitly warns silences later findings — costs nothing; the rejection now has two independent proofs
Ruling: F14 was committed after reading a grep COUNT instead of the build output, so a new warning landed and needed a follow-up commit — why: recorded because the same shortcut caused the missed test suite earlier in this build — costs nothing now; the warning is cleared
F15: complete (commits bd6cdd9, build.js clean) — plan-reviewer-agent, add-feature-readback, add-ecosystem; three files outside the plan Impact table, opened as their own block per the layer rule
Ruling: a `mention:` for a COMMAND needs the leading slash — `- mention: add.brainstorm` resolved to `product/skill/add.brainstorm` and failed the build as a dangling reference — why: build.js resolves kind from the name shape — costs nothing; caught by the gate immediately
F16: complete (commits bb1c246, build.js clean)
F17-F19: complete (commits 5daa3ea, build.js clean + framwork/ untouched) — blocked-item gap, 2.5 renumbered to 2.3, two single-owner pointers, plan-template Context gate widened for intent-only plans
Ruling: the three review-no-loops failures are ENVIRONMENTAL, not this delivery — why: all 96 offender lines resolve inside the gitignored `.claude/worktrees/readonly-agent-dispatch-capability/` checkout, zero outside it; a clean baseline worktree at af3a742 showed 2 failures, both injection-round-trip timeouts the layer skill documents as load-dependent — costs a real regression hiding behind the same three names if that worktree is ever removed without re-running
F20: complete (commits e2f0e40, full cli suite: 4 failures, all environmental; build.js clean) — test guards retargeted, CRLF normalized, mermaid regenerated
