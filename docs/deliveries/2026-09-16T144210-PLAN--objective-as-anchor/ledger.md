# Build ledger — plan: docs/plans/2026-09-16T144210-PLAN--objective-as-anchor.md

F0: Ruling: resolved the plan from conversation context, not from the argument — the invocation carried `PODE IMPLEMENTAR E ABRA PR AO FINALIZAR`, an instruction rather than a slug, and `objective-as-anchor` is the plan delivered one turn earlier — costs building the wrong plan if another draft had been meant, which `docs/plans/` rules out: the only other draft is `readonly-agent-dispatch-capability`, unrelated to this conversation
F0: Ruling: treated STEP 2's Design [STOP] as answered by the invocation — `PODE IMPLEMENTAR` is explicit approval and the full file list and F-block table were in the plan's completion report the user read — costs implementing without a seen design if the report had been wrong, which the readback at STEP 4 independently re-checks
F0: Ruling: treated STEP 9's Publish [STOP] as answered by the invocation — `ABRA PR AO FINALIZAR` answers it directly — costs an unwanted push if misread; the branch is `feat/objective-as-anchor`, never `main`, and no merge happens here
F0: baseline on branch point — ADD_GRAPH_WARNINGS=1 node scripts/build.js exit 0, zero warnings, 225 nodes, 867 edges
F0: Ruling: readback gap "what happens to a SET member that cannot write the serves-line" — the plan says "is not a member" and never says whether it is dropped, rerouted or halts. Decided: it STOPS and presents the two readings to the user — either the subtopic belongs to a different objective, or the umbrella's objective is too narrow. Derived from the case that produced this plan: 001 was BOTH, and a gate allowing only one diagnosis would have mis-read it. Costs one stop per failing subtopic if a soft warning was wanted; a soft warning is what let 001 through
F0: Ruling: readback gap "F12 states two acceptable resolutions and picks neither" — decided to give STEP 8.2 the delivery-index lookup rather than declare the omission deliberate. STEP 1.2's stated reason is that this command "is the one most likely to re-invent something that already shipped and was dropped", and a subtopic refinement proposes a concrete artefact, so the reason applies at least as strongly in Continue Mode. Costs one index read per subtopic refinement if the omission was in fact deliberate
F0: Ruling: readback gap "F9 names one line per test file — are there other 'eight' references?" — will grep both files rather than trust the two line numbers. Costs stale text saying eight beside an agent with nine
F0: Ruling: readback gap "Serves column and serves-line have no example" — both templates will carry one worked example row. Costs nothing; the readback named inconsistent formats as the likely failure and an example is the cheapest fix
F0: readback: no divergence on goal, file targets or execution order — its independently derived order matched the plan exactly
F1: RED observed by reading L146-154 before the edit — no objective question existed. A string assertion on a file already read is observed by the read
F1: complete (commits 3fa1332..ecd955b, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.1 both halves)
F2: Ruling: L3.2's defining-sentence check is line-wrap fragile — a plain grep returned 0 because the sentence wraps across two lines in both sites. Verified with `tr '\n' ' '` instead: 2 occurrences, one per site. Every remaining prose assertion in this build is run wrap-tolerant. Costs a false RED that looks like a missing edit, which is how this nearly got "fixed" twice
F2: complete (commits ecd955b..d4bfb9f, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.2 Objective above Discovery + sentence present)
F3: Ruling: F3 delivered at TWO sites, not one — 8.1's gate plus the Decomposition Map's new "Serves the objective by" column in 5.2. The plan named only 8.1. The map is where subtopics are first declared, so the same question there costs a table cell instead of a whole refinement. Costs one extra column if the map was meant to stay narrow
F3: L4.2 REPLAY RUN. Stand-in for the missing ## Objective: the umbrella's "The objective, in the owner's words" paragraph in ## Context & Motivation. Result — 003 CAN write the serves-line (it is the objective); 001 CANNOT (the chain always produces a plan, so a planless close-out advances nothing in it); 002 CANNOT (the umbrella introduces it with the word "Separately" at line 43, admitting it is not part of the objective)
F3: Ruling: the replay found 2-of-3 failing where the plan predicted 1-of-3. This does NOT weaken F3 — the discriminating requirement was "not a wall, not decorative", and 003 passing with two failing meets it. What it means is that 002 is ALSO mis-membered, which nobody had noticed, including the reordering of the Decomposition Map two turns before this build. Reported to the user rather than silently corrected: whether 002 leaves the set or the umbrella's objective widens is exactly the decision 8.1 now refuses to take alone. Costs nothing here — 002 is not in this plan's scope
F3: complete (commits d4bfb9f..a035faf, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.3 three clauses wrap-tolerant, L4.2 run with control group)
F4: complete (commits a035faf..b17b93f, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.4 four clauses wrap-tolerant, L3.6 no network reference)
F5: complete (commits b17b93f..d2724ec, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.5 header exact)
F6: complete (commits d2724ec..1991eff, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.7 order Created->Objective->Context, L3.8 both halves)
F7: complete (commits 1991eff..91910ca, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.9 two new clauses + the reference-do-not-restate guard survives)
F8: complete (commits 91910ca..3f1a50b, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.10 all eight originals present unchanged + Objective fit, L3.11 both fail cases named)
F9: Ruling: the plan named two sites (one line per test file); the grep the readback asked for found FOUR and all four were fixed — BUT THAT CLAIM WAS WRONG. There were FIVE. The diff-completeness auditor at STEP 7 found plan-review-agent-speed.test.js:16 still reading "the eight / dimensions": the word sat at the end of a wrapped line and my `cut -c1-120` truncated it, so I read the grep hit as a non-match and wrote a false completion claim into this ledger. Fixed at STEP 7.3. A false line in the ledger is worse than the stale text it described, because the ledger is what reaches the archive. Costs stale text saying eight beside a nine-dimension agent, which is exactly what the readback flagged
F9: complete (commits 3f1a50b..13cce19, cli suite SERIAL 58 files / 1478 passed / 1 skipped / 0 failed, L4.1 green)
F10: Ruling: F10 and F11 committed together. The plan requires one F-block per ruler item and this is the exception it did not foresee — both items target the SAME four lines of the 1.2 dispatch, so separating them means two commits editing identical text. Both item numbers are cited here and in the commit trailer, so STEP 7 can still send mode:confirm with 3 and 4. Costs a coarser bisect boundary across two ruler fixes
F10: complete (commits 13cce19..393ccc0, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.12 two dispatch blocks both with Capability read-only)
F11: complete (same commit 13cce19..393ccc0, L3.13 the literal "none" is gone)
F12: complete (commits 393ccc0..4c0640e, build.js exit 0 / zero warnings / 225 nodes 867 edges, framwork clean, L3.14 lookup present and no deliberate-omission clause)
GRAPH: add-framework--brainstorm — none (entry point); no entry in the delivery index
GRAPH: add-plan-authoring — add-framework--build, add-framework--done, add-framework--plan (all USES_SKILL); no entry in the delivery index
GRAPH: plan-review-agent — add-framework--brainstorm, add-framework--plan, add-plan-authoring, add-review-discipline (all DISPATCHES); one live entry, 2026-09-09T061636-PLAN--plan-review-agent-speed, nothing gone or superseded
GRAPH: plan-template.md and the two cli/tests files produce no graph node — references/ subdocs and cli/tests/ are both outside the corpus. Checked by grep instead, which is what found the two extra "eight" sites at F9
GRAPH: every dependant above was already named in the plan's Current State table. No direct dependant went unchanged-and-unnamed, and no superseded entry names a delivery the plan never mentions
REVIEW: complete (9 findings, 6 applied, 3 rejected)
REVIEW: Ruling: REJECTED "two in-flight drafts carry no ## Objective and will fail the new dimension" (medium, side-effects auditor). Not a defect — it is the dimension working. planless-close-out and readonly-agent-dispatch-capability predate the feature; docs/plans/ is gitignored so nothing outside this machine is affected, and a plan document is revised by /add-framework--plan [slug], which is the tool for it. Retrofitting them here is scope this plan does not have. Costs one retrofit at each plan's next review, which is visible at the moment it happens
REVIEW: Ruling: REJECTED as action "F3 landed at two sites where the plan named one" (medium, two auditors). It is not a finding to fix — it is a disclosure already carrying a ruling and a cost clause from the build. Both auditors classified it medium precisely BECAUSE it was disclosed rather than silent. No edit
REVIEW: Ruling: REJECTED as action "F10 and F11 share one commit, losing atomic revert" (medium, conformance auditor). The characterisation is accepted and is sharper than my own ruling: reverting 393ccc0 reverts both ruler fixes together. But the remedy is rewriting history on a branch already 14 commits deep, which trades a real risk for a bisect nicety. Both F-Block trailers and both validation lines survive, so bisect-by-trailer and audit-by-validation still work. Costs one coarse revert boundary if either ruler fix ever needs undoing alone
REVIEW: the conformance auditor claimed F9's four sites were "confirmed by diff" while the diff-completeness auditor found a fifth still stale. Checked by hand: the diff auditor was right. A diff shows what changed, never what should have — the conformance reading was inferring completeness from the wrong evidence
