# Build ledger — plan: docs/plans/2026-09-16T170340-PLAN--the-pipeline-chains.md

BASELINE: ADD_GRAPH_WARNINGS=1 node scripts/build.js on 3fa1332/e7850e4 — exit 0, zero graph warnings, 225 nodes, 867 edges
F7: Ruling: built F7 as required work — the F7 block calls it a hard prerequisite while Validated Decisions still says "optional"; the readback read it the same way — costs one unused declaration kind if the fallback was meant
F0: Ruling: the Reviewer Handoff's "evidence file" is not written; the evidence goes in this ledger — add-review-discipline forbids a review file on disk — costs a reviewer looking for a file that does not exist
F7: Ruling: the declaration keyword is `- handoff: <target>`, resolving by sigil like mention (`/x` command, `@x` agent, bare name skill) — the plan names the edge type but no key (readback gap c) — costs a rename of the key in the four skills if another spelling was intended
F13: Ruling: the preview's shape is authored in add-plan-authoring as a short labelled list of the five elements F13 names — the plan gives no template (readback gap d) — costs a reshaping of one section
F20: Ruling: the Lists-items row's behaviour for sections 1, 2, 4, 5 is derived from the file's own STEP 4.1 (sections run in full; section 3 is replaced by the Open items) — the plan states the defect but not the target (readback gap e) — costs a wrong documented branch if the planner meant a confirmation screen
F1: complete (commits e7850e4..c8d5199, build.js exit 0 no new warning, framwork/ clean, 5 related test files 156 passed)
F2: Ruling: the parity test's 'gh pr merge --squash' token was updated inside F2, not F5 — F2's own serial suite would be red otherwise and a red block cannot commit; F5 keeps the sweep — costs nothing but F5's diff being smaller than the plan describes
F2: Ruling: also rewrote add.done's "a squash creates a NEW commit" reason for passing the merge sha to --cleanup — it justified the flag F2 changes and went false with it — costs one paragraph if the planner wanted it untouched
F2: complete (commits c8d5199..fa25f35, build.js exit 0 no new warning, cli suite serial 58 files 1478 passed 1 skipped)
F3: complete (commits fa25f35..ea1f233, build.js exit 0 no new warning, framwork/ clean, 6 related test files 179 passed; L3.3 internal --squash=0)
F4: Ruling: parity test L7.3 pinned 'git show --name-status' and was updated inside F4 so the block's serial suite is green — found by the suite, which is what F5's sweep was for — costs nothing
F4: complete (commits ea1f233..fba09ea, build.js exit 0 no new warning, cli suite serial 58 files 1478 passed 1 skipped)
F24: Ruling: added F24 [product] on framwork/.codeadd/scripts/delivered.sh, executed after F4 and before F23 — measured in a scratch repo: under --merge, `delivered.sh touched` returns CURATED_ONLY=1 for a PR-merged delivery, because the branch commit that introduces the index line touches only docs/. The plan's F23 claim "the mechanism still answers" is false, and L4.6 requires the right commit on both routes, so making it answer is derivable from the plan — the fix walks --first-parent -m. costs: a shipped script the plan did not list; the local route and this repo's existing history answer identically (measured, 15 complete before and after)
F24: complete (commits fba09ea..0216a90, RED observed: bats L1.7c failed on answer complete; after fix delivered.bats+done.bats 132 ok via docker runner; build.js exit 0; cli suite serial 58 files 1478 passed 1 skipped)
F23: Ruling: the sweep for other squash justifications (Reviewer Handoff item 7) also rewrote the delivered.sh header and the delivered.bats L1 comments — both stated the same false reason; done.sh line 634 is left, because done.sh is excluded and its local route still squashes — costs nothing
F23: Ruling: L2.3b carries an explicit 60s timeout — the fixture's git subprocesses measured ~5.5s against vitest's 5000ms default, and the first RED run failed the squash case on timeout, not on the assertion — costs a slow test hiding behind a wide budget
F23: complete (commits 0216a90..8c1bd5d, RED observed: L2.3b merge case failed 'curated' vs 'complete' against the pre-F24 script; L4.6 real repo: contract pickaxe returns e7850e4 for the latest squash-merged entry, same as the old pickaxe; merge route proven by L2.3b and bats L1.7c; build.js exit 0; delivered.bats 0 not ok; cli suite serial 58 files 1480 passed 1 skipped)
F5: complete (commits 8c1bd5d..6ca5dbb, grep of cli/tests and scripts/tests for squash pins: none left outside done.bats, which covers done.sh's excluded local route; build.js exit 0; cli suite serial  Tests 1480 passed | 1 skipped (1481))
F6: Ruling: F6 changes comments and the gate's failure message only — measured: the skills walk already finds any .claude/skills/<name>/SKILL.md and INTERNAL_COMMAND_NS matches text, never a node, so "what it resolves to moves to a skill node" has no code to move — costs nothing if right; a missed discovery path would show at F8's L2.2
F6: complete (commits 6ca5dbb..4be9119, build.js exit 0 no new warning 225/867 unchanged, framwork/ clean, graph/mcp/build test files 11 passed 357 tests)
F7: Ruling: F7 also documents `handoff:` in add-framework-development § 8 — that section owns the uses: syntax, and a sixth kind it does not list is a second owner by omission — costs nothing
F7: complete (commits 4be9119..28f3638, build.js exit 0 no new warning 225/867, framwork/ clean)
F7b: Ruling: added F7b [product] for cli/tests/build-artefact-graph.test.js (the RED assertion L1.9b) and mcp/corpora.mjs's "five kinds" — both paths are product and F7 is internal, so they cannot ride F7's commit — costs one extra commit
F7b: complete (commits 28f3638..e096c88, RED observed: L1.9b failed "unknown uses: kind handoff" before F7; cli suite serial  Tests 1481 passed | 1 skipped (1482))
F8: Ruling: the three pipeline links (brainstorm->plan, plan->build, build->done) are declared `handoff:`; every other `- command:` naming a stage became `- skill:` and every `- mention: /x` became a bare `- mention: x` — the plan asks for `- skill:` everywhere and a hand-off for build->done, and the other two links were HANDS_OFF_TO before, so keeping them as hand-offs preserves their edge type — costs three declarations if every link was meant to flatten into USES_SKILL
F8: Ruling: add-framework-development gained `- mention:` for add-framework--plan and --build — a skill's bare name now counts as a prose reference where a command needed its slash, and that file names both as callers — costs nothing
F8: Ruling: "this command"/"THIS COMMAND" self-references inside the four stages now say skill, and three "planning command"/"one command"/"the command" phrases that named a stage were retyped; git/shell "commands" and the historical "single-layer commands this one replaced" were left — costs wording, not behaviour
F8: Ruling: no `disable-model-invocation` in the four frontmatters — the automatic path needs the agent to load the next stage through the Skill tool, which that flag blocks; add-framework--done's description says it is never reached unattended — costs a model auto-loading a stage on a loose match
F8: Ruling: the local, gitignored .claude/agent-memory note naming add-framework--build.md and --plan.md was updated to the SKILL.md paths — L3.6's grep reaches it — costs nothing, it never reaches git
F8: complete (commits e096c88..6b505ad, build.js exit 0 no new warning; L2.1 225 nodes, 867->869 edges, no drop; L2.2 all four internal/skill/*, none internal/command/*; L2.3 add-framework--build HANDS_OFF_TO add-framework--done; L2.4 no dangling; L3.1 double dash in all four paths; L3.2 four distinct frontmatter descriptions; L3.6 zero old-path pointers in .claude/ CLAUDE.md scripts/; L4.4 the harness lists all four as invocable skills after the move; framwork/ clean. cli suite: 48 failures, all tests reading the old paths or the old node ids — F8b)
F8c: Ruling: added F8c [internal] to regenerate web/public/artefact-graph.mmd — graph-mermaid.test asserts it matches the emitted graph, and its DOCS_PROFILE renders kind command only, so the four stages drop out of the public diagram; widening the profile is a docs decision the plan never made — costs the four stages missing from the web diagram until /add-framework--sync or a later plan widens it
F8c: complete (commits 6b505ad..2f8b700, node scripts/graph.js mermaid --write, 14 lines removed)
F8b: Ruling: added F8b [product] for the ten cli/tests suites that read the old command paths or node ids — F8 is [internal] and cannot write cli/, and a rename's dependants belong in the same F-block, so this is the nearest legal home, executed immediately after F8 — costs one commit boundary (6b505ad) at which the cli suite is red; CI never sees it alone
F8b: complete (commits 2f8b700..a3f322d, mechanical path/id repoint plus the node inventory snapshot; build.js exit 0; cli suite serial 58 files 1481 passed 1 skipped)
F9: Ruling: CLAUDE.md's brainstorm and done rows describe the three-option approval and "never reached unattended" one commit before F10 writes them — the rows are written once for the delivery rather than twice — costs a CLAUDE.md ahead of its skills between edc4db7 and F10's commit
F9: complete (commits a3f322d..edc4db7, L3.6 grep zero old-namespace path pointers in .claude/ CLAUDE.md scripts/; build.js exit 0; framwork/ clean; inventory/close-out-hardening/review-no-loops 89 passed; inventory block untouched)
F10: Ruling: the automatic mode travels as a `delivery: confirm|automatic` field in the intent file, and the intent file is now written AFTER the approval instead of before it — the plan decides the three options but not how a later stage learns which was chosen, and the intent file is already the one contract from brainstorm to plan — costs a reorder of 7.3 if the approval was meant to follow the file
F10: Ruling: a spike is asked no three-option question — it writes nothing and routes nowhere, so neither approve option has anything to start — costs nothing
F10: complete (commits edc4db7..50cc5c3, RED: the three option labels absent before; L3.5 three options present, terminus named; build.js exit 0 after declaring the new add-framework--done mention; framwork/ clean; 6 related test files 215 passed)
F11: Ruling: the stopping rule's single owner is add-plan-authoring (new "The Delivery Mode"), beside the intent file it extends; the plan carries the mode in a `> **Delivery:**` header line added to plan-template — the plan names the rule but no owner and no carrier into the build — costs a move if a separate skill was intended
F11: Ruling: plan STEP 4 is confirming only when Open reads None AND no blocked audit item AND delivery is automatic; build STEP 2 Design is confirming only on automatic AND no Global Constraint overriding a gate — both exceptions are choices the brainstorm approval never saw — costs extra waits on the automatic path if the planner meant fewer
F11: Ruling: plan STEP 7 on automatic loads /add-framework--build with the plan basename; the "DO NOT proceed with implementation" line stays, with a sentence saying the handoff is not implementation — the chain cannot pass from plan to build otherwise, and the plan has no F-block for it — costs nothing if F12's NO-INVOKE reasoning is accepted for the plan stage too
F11: complete (commits 50cc5c3..cee5bd8, RED: no stop carried a kind before; L3.7 every [STOP] in the four stages carries an explicit kind and build STEP 9 names both states; build.js exit 0; framwork/ clean; 6 related test files 215 passed)
F12: complete (commits cee5bd8..1c0969b, L3.10: the gate block's lines are unchanged — the diff removes only the Rules line that said handoff is text-only "on every path", which the authorised chain made false, and re-adds it with the exception; the authorised-chain sentence present; build.js exit 0; framwork/ clean; 3 related test files 151 passed)
F13: complete (commits 1c0969b..40e98a3, RED: add-plan-authoring had no preview shape; L3.8 first half present; L3.8 guard: [STOP]/[HARD STOP] markers inside plan STEP 4 counted 2 before and 2 after; build.js exit 0; framwork/ clean)
F14: complete (commits 40e98a3..a6f8733, RED: no such rule before; L3.9 present in the stopping rule's owner; L4.3 traced in the phase summary; build.js exit 0; framwork/ clean)
F15: complete (commits a6f8733..3e1e5e6, L3.11 neither build nor done restates the match/companions/stops, both name add-plan-authoring's Argument Resolution; ruler item 5; build.js exit 0; framwork/ clean)
F15b: Ruling: added F15b [product] for review-no-loops L2.9, which pinned the review-v exclusion in the build's own text — the exclusion moved to its owner, so the test now pins it there and asserts the delegation — costs one commit boundary (3e1e5e6) with that test red
F15b: complete (commits 3e1e5e6..c306f05, cli suite serial  Tests 1438 passed | 1 skipped (1439))
F15b: Ruling: the F15b line above was appended on a run whose suite FAILED to parse review-no-loops.test.js — the commit command was chained after the suite instead of gated on it, which this build forbids; the fix is its own commit 3af9c21 rather than an amend, and this line supersedes that entry's "cli suite" figure — costs one red commit (c306f05) in the branch history
F15b: complete (commits 3e1e5e6..3af9c21, RED: the previous L2.9 failed after F15 — the build's text carried no --review-v line; after the repin and the regex repair the cli suite serial 58 files 1481 passed 1 skipped)
F16: Ruling: the owner is building-commands/references/agent-dispatch.md, not add-plan-authoring — building-commands already points at it for dispatch, and it is where the block was defined; add-framework--sync keeps its own copy — sync is not a pipeline stage and the plan excludes it — costs one remaining internal copy
F16: complete (commits 3af9c21..7f4ba6d, L3.12 the owner holds the rules and the three stages delegate to it by name; ruler item 5; build.js exit 0; framwork/ clean; cli suite serial 58 files 1481 passed 1 skipped)
F16: complete (commits 7f4ba6d..cfc0142, follow-up: the three pointer sections renamed so "Agent Dispatch Rules" is a heading in the owner and in add-framework--sync only; build.js exit 0; related tests green)
F17: complete (commits cfc0142..bce2c37, L3.13 add-build-ledger documents the GRAPH: shape beside the other three; ruler item 4; build.js exit 0; framwork/ clean; related tests green)
F18: complete (commits bce2c37..4546f8c, L3.14 the STEP 9 gate line states both behaviours — ask on the first push, push without asking once a PR exists; the IF block names the no-PR state; ruler item 6; build.js exit 0; framwork/ clean; inventory/review-no-loops/prompt-quality-ruler 95 passed)
F19: complete (commits 4546f8c..d7f1ab0, L3.15 the cleanup gate states the condition is the delivery being on main, never which STEP merged it; ruler item 6; build.js exit 0; framwork/ clean; 6 related test files 182 passed)
F20: complete (commits d7f1ab0..2805851, L3.16 the Lists-items row states all five sections; ruler items 6 and 8; L4.5 traced: a bounded intent with Open None still takes the confirmation row and the short-plan shape at 2.3, untouched by this edit; build.js exit 0; framwork/ clean; 4 related test files 169 passed)
F21: complete (commits 2805851..dcc7681, L3.17 done's Rules has a NEVER heading with two items, neither derivable from the STEP order — the merge method is stated as deliberate in STEP 7 but no step forbids switching it on refusal, and the never-unattended rule lives in a stop-kind note, not a STEP; ruler item 3; build.js exit 0; framwork/ clean; close-out-hardening L3.9 guard green)
F22: complete (commits dcc7681..312f9aa, L3.18 the three Rules items are gone and the prohibitions survive at their sites — Write outside docs/plans/ at L50, Present ... as delivered at L68, DO NOT split a topic into two plans at L220; ruler item 7; build.js exit 0; framwork/ clean; cli suite serial 58 files 1481 passed 1 skipped)
GRAPH: add-framework--brainstorm — none; no gone/superseded entry
GRAPH: add-framework--plan — add-framework--brainstorm (HANDS_OFF_TO), add-framework--build (USES_SKILL), both changed in this delivery; no gone/superseded entry
GRAPH: add-framework--build — add-framework--done, add-framework--plan, building-commands, all changed; no gone/superseded entry
GRAPH: add-framework--done — add-framework--build (HANDS_OFF_TO), changed; no gone/superseded entry
GRAPH: add-plan-authoring — the three stages, all changed; no gone/superseded entry
GRAPH: add-build-ledger — add-framework--build, add-framework--done, both changed; no gone/superseded entry
GRAPH: add-framework-development — build, internal-layer, product-layer, all changed; no gone/superseded entry
GRAPH: add-framework-internal-layer — add-framework--build, changed; no entry
GRAPH: add-framework-product-layer — add-framework--build, changed; no entry
GRAPH: add-review-discipline — brainstorm, build, plan, add-plan-authoring, all changed; no entry
GRAPH: building-commands — prompt-review-agent, build, add-framework-development, internal-layer, product-layer, all changed; no entry
GRAPH: add-final-report — add-framework--release, --roadmap, --sync UNCHANGED and unnamed by the plan; the change here was one mention declaration, which they do not read — not a finding; no entry
GRAPH: plan-readback-agent — build, add-review-discipline, both changed; no entry
GRAPH: plan-review-agent — brainstorm, plan, add-plan-authoring, add-review-discipline, all changed; no entry
GRAPH: prompt-review-agent — build, plan, add-review-discipline, all changed; no entry
GRAPH: add.done — add, add.build, add.plan, add.plan-to-ready, add.pull-request, add.review, add-doc-schemas, add-qa, two INJECTS_INTO fragments (docs-pruning, gitnexus) and fragments/qa-pipeline/add.review.md, all UNCHANGED; the plan graded these and neither the flag nor the diff command is part of add.done's interface — not a finding; no gone/superseded entry
GRAPH: delivered.sh — add.done (changed), add.hotfix and add-knowledge-discovery UNCHANGED and unnamed by the plan (F24 was added by the build); both call `touched`, whose answer is identical on linear and squashed history (measured 15/15 on this repo) and now correct on merge commits — FINDING, low: two unnamed dependants of a behaviour change, verified unaffected; no entry
GRAPH: delivery-index.md — add.done, add-doc-schemas; add-doc-schemas UNCHANGED, it loads the reference and needs nothing from the rewrite; no entry
GRAPH: agent-dispatch.md — brainstorm, build, plan, building-commands, all changed; no entry
GRAPH: plan-template.md — add-plan-authoring, changed; no entry
REVIEW: Ruling: rejected add-review-discipline #2 and prompt-review-agent #1 (drop the leading slash on /add-framework--* in prose) — a skill is invoked as /<name>, the harness lists all four as invocable skills, and the product-layer auditor checked the same form and cleared it; the two auditors disagree and the one that checked invocation wins — costs nothing but the notation if the uses:-block form was meant to be copied into prose
REVIEW: Ruling: rejected add-framework-development #1 (two sections numbered 8) — building-commands' ruler quotes exactly that duplicate as its live "Not expected" example for item 2, so renumbering falsifies another artefact's example and belongs in its own change — costs a known duplicate heading left standing
REVIEW: Ruling: rejected add-framework-development #3 (fallback table shown twice) — low, pre-existing, and the two copies sit under different jobs (anatomy vs patterns to enforce) — costs one repeated table
REVIEW: Ruling: rejected scope 3 #2 (add.done's paragraph on why the merge sha is passed) — it replaced a reason F2 made false, and dropping it leaves the invocation unexplained; the roadmap's own sweep item 3.1 is where mechanism prose is weighed — costs a paragraph that sweep may remove
REVIEW: Ruling: no action on scope 1 #1 (plan self-contradiction on F7) and #2 (plan and brainstorm express the authorised chain asymmetrically) — the first is already ruled at F7, the second is F12's plan-stated scope and already ruled at F11 — costs nothing new
REVIEW: Ruling: no action on scope 2's two low notes (two commits sharing an F-block trailer, for F16 and F15b) — both are follow-ups recorded in this ledger, not amends — costs nothing
REVIEW: Ruling: scope 1's auditor reported running node scripts/build.js and git status despite the read-only mandate; it verified only gitignored sidecars were regenerated — recorded, no effect on the tree — costs nothing
REVIEW: Ruling: plan-readback-agent #1, add-framework-internal-layer #1 and add-framework-development's count column were pre-existing defects a delivery-mode tick found on artefacts this delivery touched by one declaration; applied because each fix is mechanical and inside a file already in the diff — costs scope beyond the plan's Impact table
REVIEW: complete (21 findings, 9 applied, 12 rejected) — applied in f192d0c [internal] and e4d8ce8 [product]; build.js exit 0; cli suite serial 58 files 1481 passed 1 skipped
DONE: Ruling: the archive holds three design members — the plan's Context table names the umbrella and subtopics 002 and 003, and The Delivered Home has a single design.md slot — so the umbrella is design.md and the two subtopics are design-002-merge-not-squash.md and design-003-commands-as-skills-and-chain.md, byte-identical copies; subtopic 001 is not cited by the plan and stays out — costs two filenames outside the member table if the slot was meant to hold the umbrella alone
DONE: Ruling: the index entry's items point at the new skill paths, while three earlier entries (objective-as-anchor and the build/done deliveries) still anchor at .claude/commands/add-framework--*.md; verify ran report-only as STEP 3.1 requires, and those items now resolve as `changed` until a `verify --repair` run — costs stale `at` hints on older entries until then
DONE: Ruling: the entry's four LOOSE anchors (confirming, Preview, handoff, --merge) were kept — they sit under the over-match threshold and are the names the delivered behaviours carry; `automatically` was refused as over-matched and replaced by `terminus` — costs a noisier `read` for those four
