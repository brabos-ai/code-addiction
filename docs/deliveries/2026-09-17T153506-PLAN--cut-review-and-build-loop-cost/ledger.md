# Build ledger — plan: docs/plans/2026-09-17T153506-PLAN--cut-review-and-build-loop-cost.md

BASELINE: HEAD 077d256f0077b41b8e596cbb291f740b782bd446, ADD_GRAPH_WARNINGS=1 node scripts/build.js → 0 warnings, exit 0, working tree clean
READBACK: complete — restatement matches plan intent on scope, order and the three independent groups. No divergence severe enough to reopen the plan; four implementation-level gaps it surfaced become rulings at the F-block that resolves each (F1 below; F3, F6, F7 at their own blocks).
Ruling: `Confidence` field (F1) applies to every finding `reviewer-agent` returns, in any MODE — including the `MODE: owasp` findings F5 adds later — not scoped to `MODE: task` alone — because F2's confidence-based routing has no other branch for an owasp finding, and inventing a second finding shape violates single-owner (building-commands Ruler item 5) — costs a rework of F1's Report Format wording if a future subtopic wants owasp findings to skip confidence-routing entirely
Ruling: batched-form payload (F3) is one artefact id per line, `<node id> mode=<audit|delivery|confirm> items=<n,n,...|->` — no existing convention to follow (single form never had a list), chose the plainest text shape consistent with this agent's other line-oriented inputs — costs a reformat of F4's dispatch instruction if the build later needs a structured (JSON) batch instead
F3: complete (commits 077d256..8c509f1, build.js clean, framwork/ untouched)
Ruling: F3 landed on `main` (the prior branch had already merged via PR #74 before this session resumed, so HEAD was `main` without a feature branch) — created `feat/cut-review-and-build-loop-cost` at 8c509f1 and force-moved local `main` back to `origin/main` before starting F4 — costs nothing: no push had happened yet, and every commit from here on lands on the new branch
F4: complete (commits 8c509f1..d61698a, build.js clean, framwork/ untouched)
F1: complete (commits d61698a..2527442, build.js clean)
Ruling: F2's confirm dispatch for a needs-verification finding reuses `reviewer-agent`'s existing `MODE: task`, scoped to just that finding's file and claim — no new MODE added to reviewer-agent for this, since F2's scope names only add-subagent-driven-development — costs adding a dedicated verify MODE later if a scoped `task` dispatch proves too heavy for a one-finding check
F2: complete (commits 2527442..2d6fdaa, build.js clean)
Ruling: F5's `MODE: owasp` prose named `add-subagent-driven-development` and `/add.review` — build.js failed on both as undeclared references; added `- mention:` lines for each — costs nothing, mechanical fix caught by the gate itself
F5: complete (commits 2d6fdaa..d8a2b60, build.js clean)
Ruling: F6's owasp trigger (4.1) is implemented as a path/filename heuristic for all five categories, "unsanitized input" included — matches the existing frontend/backend Detect Scope style (fast, mechanical, no diff-content analysis) rather than inspecting diff content — costs a miss on a sensitive change whose path doesn't signal it, already named as a risk in the plan
F6: complete (commits d8a2b60..47478a0, build.js clean)
Ruling: F7's CI-tier match is by file name/path against what wiki/workflows.md's Test Workflow section names — literal/glob match, no deeper parsing — matches how add-architecture-discovery already documents that section (file/suite names, not abstract categories) — costs a miss if a project's wiki records tiers by suite name only with no path correlation, degrading to today's un-scoped Verify: for that task
F7: complete (commits 47478a0..eee0ed0, build.js clean)
Ruling: F8's prose named `add-tasks-checklist` — build.js failed as undeclared; added `- mention:` — mechanical fix
F8: complete (commits eee0ed0..bcd7c03, build.js clean)
F9: complete (commits bcd7c03..e266555, build.js clean)
GRAPH: internal/agent/prompt-review-agent — add-framework--build, add-framework--plan, add-review-discipline; no entry
GRAPH: internal/skill/add-framework--build — add-framework--done, add-framework--plan, building-commands; no entry
GRAPH: product/agent/reviewer-agent — consistency-agent, plan-reviewer-agent, add.build, add.review, gitnexus reviewer-agent fragment, add-cross-sf-consistency, add-plan-review, add-review-discipline, add-subagent-driven-development; no entry
GRAPH: product/skill/add-subagent-driven-development — add.audit, add.build, add.diagnose, add.hotfix, add.new, add.plan, add.qa-setup, add.review, add.wiki, add-qa-migration, add-review-discipline; no entry
GRAPH: product/command/add.review — consistency-agent, e2e-agent, qa-agent, ux-agent, add, add.build, add.done, add.plan, add.qa-setup, qa-pipeline/tdd-pipeline/playwright fragments, add-delivery-validation, add-id-convention, add-qa, add-qa-migration; no entry
GRAPH: product/skill/add-tasks-checklist — add.build, add.plan, add.review, add-doc-schemas, add-subagent-driven-development; no entry
GRAPH: product/fragment/fragments/tdd-pipeline/add.build.md — feature tdd-pipeline; no entry
GRAPH: product/skill/add-tdd — tdd-pipeline fragments (add.build, add.hotfix), add-test-specification; no entry
All direct dependants above were already named or accounted for in the plan's Ecosystem Impact table — no untouched caller surfaced, no `superseded`/`gone` entry for any artefact this delivery touched.
Ruling: rejected — add-framework--build L20 `- skill: add-framework--plan` should be `- mention:` (prompt-review-agent batched audit, item 1, medium) — pre-existing, none of this delivery's F-blocks touched that declaration or its citing lines — costs nothing to leave; a follow-up cleanup, not this delivery's
Ruling: rejected — add-subagent-driven-development L416-421 fix-agent dispatch contract mismatch (batched audit, item 4, high) — pre-existing, the text sits immediately after F2's own addition but F2 did not write or touch it — costs nothing to leave; real defect, belongs to a separate delivery
Ruling: rejected — add-subagent-driven-development references/persistent-logging-and-tasks.md stale tasks.md schema + dead command names (batched audit, item 5, high) — a whole reference subdoc this delivery never opened — costs nothing to leave; real defect, out of scope
Ruling: rejected — add.review.md STEP 11.3 missing add-doc-schemas validation gate block for the `review` schema (batched audit, item 4, high) — pre-existing, F6 only touched STEP 4.1/4.2 and added a new dispatch block; STEP 11.3 predates this delivery — costs nothing to leave; real defect, out of scope
Ruling: rejected — add-tasks-checklist "undeclared add.build/add.review references, build.js gate fails" (batched audit, item 1, high, twice) — empirically false: `ADD_GRAPH_WARNINGS=1 node scripts/build.js` ran clean immediately before and after this claim, 0 warnings, both before and after F7 landed — costs nothing; the auditor's claim was not verified against the actual tool
Ruling: rejected — add-tdd L8 non-canonical LANG line (batched audit, item 3, low) — pre-existing, F9 only touched the GREEN step — costs nothing to leave; real defect, out of scope, deferred for consistency with the other pre-existing-and-unrelated rejections above rather than fixed as a drive-by
Ruling: rejected — plan-review-agent's Consumes/Produces substring-vs-exact-match finding on F1→F2 and F5→F6 (plan conformance audit, finding 3, low) — already adjudicated at this plan's own review pass (A5): the earlier @plan-review-agent explicitly accepted substring matches for these two pairs while requiring exact matches only for F3→F4 and F7→F8/F9, which this delivery's Consumes/Produces lines satisfy — not a new defect, and does not affect what was built
Ruling: F5's OWASP Mode overclaim ("every finding still routes through §7's confidence gate") was my own wording defect, not pre-existing — fixed directly (commit 8cdd80a) rather than deferred, since it is about text I wrote this delivery, not adjacent unrelated content
Ruling: S3-2's bigger gap (Confidence not wired into `/add.review` → Fix Routing → `/add.build` STEP 12) was presented to the user as a deciding stop, per STEP 7.3's own rule — not invented. User chose: no schema/merge/gate extension; instead state explicitly, at both pipeline sites, that this path is "one review, one fix wave, done" and Confidence stays informational there — costs a follow-up plan if this pipeline's own false-positive cost later turns out to matter enough to revisit
REVIEW: complete (9 findings from the 4 STEP 7.1 auditors + 1 self-caught wording defect, 6 applied, 7 rejected/deferred with rulings above, 1 resolved by explicit user decision)
Ruling: T4/F10 added post-hoc, from a follow-up conversation after PR #75 was already open — user asked to design and land it in the same PR rather than open a new brainstorm/plan. Design: narrow add-subagent-driven-development step 5's per-task review from add-code-review's ten categories to task-contract + wiki-conformance + a named "sniff test" for obvious mistakes, confirmed with the user in two rounds (first cut, then narrowed further on their request) before implementing — costs a rework if a real defect turns out to need one of the dropped categories caught earlier than /add.review's end-of-feature pass
F10: complete (commits e266555*..9dc8a9b, build.js clean, cli suite 1527 passed/1 skipped) — *chain continues from the STEP 7 fix commits (8cd733e..8fb571f), not directly from e266555
Ruling: the restored Architecture Contract check (b9336d2) had no guard for CLAUDE.md lacking a `## Architecture Contract` section — user raised the risk of a reviewer enforcing patterns the project never adopted, and the missing guard was exactly that door. Guarded in 73a6552: check runs only against the section the project actually carries, skips with a note otherwise — costs nothing; the contract is discovered from real dependency edges, so its absence means there is nothing legitimate to check against, not that generic rules apply
Ruling: F10 as first landed narrowed the per-task review too far, found on a re-read after the model was switched to Opus — `SPEC_STATUS` was still demanded and still gated the commit at step 6 with no stated derivation, the Architecture Contract check lost its procedure, the wiki page was conditioned on the wiki existing rather than that area's page existing, and "do not hunt for it" read as permission to skim. All four corrected in commit b9336d2, keeping the six dropped categories (RESTful, OWASP, SOLID, Code Quality, Database, Environment) exclusively `/add.review`'s — costs nothing beyond the extra commit; the defect never reached `main`, and `/add.build`'s own validator dispatch was never affected because it derives `SPEC_STATUS` from its own TASK B, not from this template
