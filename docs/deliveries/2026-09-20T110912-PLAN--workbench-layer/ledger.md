# Build ledger — plan: docs/plans/2026-09-20T110912-PLAN--workbench-layer.md

BASELINE: ADD_GRAPH_WARNINGS=1 node scripts/build.js — exit 0, 0 warnings, measured on worktree-feat+workbench-layer before F1
F1: Ruling: F4's classification rule is fixed as — a `.claude/` string that POINTS AT an artefact becomes `{{cmd:}}`/`{{skill:}}`; one that DESCRIBES the build's output path stays literal — the readback filled this gap on its own and the plan named it the delivery's highest silent risk — costs a text that resolves cleanly and says something false, in whichever direction it is misapplied
F1: Ruling: F10 narrows to add-framework--brainstorm for the `Skill tool` phrase — grep found 3 occurrences there and none in --plan, --build or --done, so the plan's four-skill scope was wider than the tree — costs nothing if wrong, because L2.5 still greps all four and would catch a missed one
F1: Ruling: F9's "pattern the product layer already uses" resolves to add.brainstorm.md:317 — "where the `structuredQuestions` capability declares one ... where it declares none, present the same content as an option table with the recommendation stated below it" — the readback could not resolve this citation from the document alone — costs an inconsistent fallback UX if the wrong pattern was copied
F1: Ruling: F13 moves to run immediately AFTER F1, not after F3 — the plan timed it "before any build-workbench.js run", but measurement shows the graph goes blind the moment F1 moves the tree: 228 nodes before, 201 after, exit 0 and zero warnings, so nothing reports it — costs nothing if wrong, and running F2/F3 first would grade two F-blocks against a graph missing its whole internal half
F1: Ruling: moved workbench/skills/add-commit/evals/evals.json along with its skill — the plan says skill subdirectories move whole and names references/ as the example, not the only case — costs a stray file under .claude/ if that eval set was meant to stay behind
F1: complete (commits c6017c0..037eb2a, build.js exit 0 / 0 new warnings / framwork clean)
F13: complete (commits 037eb2a..df488ab, build.js exit 0 / 228 nodes restored / framwork clean)
F2: complete (commits df488ab..31fce48, build.js exit 0 / 0 new warnings / 228 nodes / framwork clean)
F3: Ruling: F3 makes ONE edit to scripts/build.js after all — lintResourcePaths now exempts sources under workbench/, beside the exemption it already had for add-resource-path-convention — the plan's "must NOT edit build.js" was reasoned about the transformation path buildResources takes, and this is a product-only convention producing 13 unactionable warnings on every workbench run — costs nothing if wrong, and a warning stream nobody reads is what it buys instead
F3: complete (commits 31fce48..3bd50d1, 56 files emitted / build.js exit 0 / 228 nodes / framwork clean)
F5: Ruling: F5 runs before F4, not after — the plan ordered F4 → F5, but F3's commit tracked all 56 generated files, so every later F-block commit would carry them as noise and the diff a reviewer reads would not be the change — costs nothing if wrong; F5's own Consumes (F3's Produces) was already satisfied
F5: complete (commits 3bd50d1..492ad98, git ls-files .claude/ returns settings.json alone / source not ignored / build.js exit 0 / 0 warnings / framwork clean)
F4: Ruling: only 8 of the 48 occurrences became {{skill:}} — the plan's framing implied most would — because the classification rule sorts them three ways, not two: 8 POINT AT a workbench artefact and take a variable, 28 DESCRIBE the internal layer's own paths and become literal workbench/, and 12 are PRODUCT paths (framwork/.claude/, the resource-path examples, .claude/agent-memory/) and are correct untouched — costs a wrong conversion in either direction, which resolves cleanly and reads as true
F4: Ruling: L2.2 narrows from "no .claude/ literal reaches opencode output" to "none that names a workbench artefact" — 12 product-path occurrences are correct and must survive, so the assertion as written would fail on correct text — costs a test that passes by asserting nothing if narrowed too far
F4: Ruling: add-framework-development:220 documents the {{skill:}} syntax with a NAME placeholder, and the build resolves it into a concrete path, mangling the documentation — left as is, because the product's own add-resource-path-convention has shipped with exactly this for as long as it has existed (verified in framwork/.opencode/skills/add-resource-path-convention/SKILL.md) — costs one confusing line in a built file; fixing it here would be a second, unplanned change to how resolveResourcePaths treats fenced blocks
F4: complete (commits 492ad98..ee89f83, per-provider resolution verified / build.js exit 0 / 0 warnings / graph edges intact / framwork clean)
F6: complete (commits ee89f83..7b1b0cf, npm run build:workbench emits 56 files / build.js exit 0 / 0 warnings / framwork clean)
F7: complete (commits 7b1b0cf..c3f1584, MCP stdio handshake verified / build.js exit 0 / framwork clean)
F8: Ruling: F8 also fixes a live PRODUCT defect, not only a workbench one — git-history-agent declares `readonly: true` with `tools: Bash, Read`, is described as "read-only git", and was shipping with `bash: deny`, so its only route was closed while its own declaration said otherwise — costs a product agent gaining bash it should not have, bounded by `edit: deny` staying unconditional
F8: complete (commits c3f1584..ef94afd, L1.6 exception set = {git-history-agent, framework-discovery-agent}, 1 line diff over 22 product agents / build.js exit 0 / framwork clean)
F9: complete (commits ef94afd..a8d4139, both sites carry the capability check / build.js exit 0 / framwork clean)
F10: complete (commits a8d4139..cc64492, L2.5 GREEN across all four skills / prohibitions intact / build.js exit 0)
F11: complete (commits cc64492..b90830c, no stale claim survives the sweep / build.js exit 0 / framwork clean)
F12: Ruling: the frontmatter comments are MOVED to a source-only HTML block, not deleted — the plan said "remove", but they carry design rationale (why haiku, why Bash is granted, why reindex is absent, and a stated cost about shell writes), and CLAUDE.md names HTML comments as exactly the mechanism for source-only notes since stripHtmlComments erases them at build — costs the reasoning being one scroll further from the frontmatter it explains
F12: Ruling: framwork/.codeadd/agents/readback-agent.md has the SAME leak — 7 comment lines glued into its built frontmatter — and is NOT fixed here — it is a [product] path and this is an [internal] F-block, and the plan's Does NOT Include forbids widening into the product layer beyond F13/F18/F19 — costs one product agent shipping 7 harmless comment lines in its frontmatter until someone plans that fix
F12: complete (commits b90830c..da054d2, 4 workbench agents clean, 59/60 frontmatters comment-free / build.js exit 0 / framwork clean)
F15: Ruling: F15 runs before F14, not after — F1's move left 12 tests red, and committing F14 first would have closed a product block with the cli suite failing, which add-framework-product-layer forbids outright — costs nothing if wrong; F15's Consumes (F13's Produces) was already satisfied
F15: Ruling: I converted 13 tracked files from LF to CRLF in F4 and F9-F12, by opening them in Python text mode without newline='' — .gitattributes says "Normalize all line endings to LF in the repository AND the working tree ... the test suite asserts LF-strict content" and close-out-hardening L3.7 is one of those assertions — normalized all 13 back to LF inside this block rather than deferring — costs nothing; it is a repair of my own defect, and the suite went 12 red to 0 red
F15: Ruling: the two injection-exclusivity failures seen in one run were suite instability, not a regression — they did not reproduce on the isolated run or the final full run, and the suite is known to rewrite the sidecars while it runs — costs a real injection regression going unnoticed if wrong, which CI would catch
F15: complete (commits da054d2..11cdcb5, cli suite serial 63 files / 1550 passed / 0 failed / LF restored on 13 files)
F14: complete (commits 11cdcb5..d6bf4c6, MCP get returns workbench/ path / cli suite green / build.js exit 0)
F16: complete (commits d6bf4c6..41dbc8e, ci.yml carries the step / release.yml does not / build.js exit 0)
F17: complete (commits 41dbc8e..8849f71, no stale pointer survives the sweep / inventory already current / build.js exit 0)
F18: complete (commits 8849f71..4b4c4ab, L2.6 proven RED by deliberate violation then restored / build.js exit 0 / 228 nodes)
F19: Ruling: L1.4 asserts only commands and agents are absent from the product registry, not skills — four skill NAMES exist in both layers on purpose (add-commit, add-final-report, add-review-discipline and neighbours), and add-review-discipline's own text records that duplication as deliberate — costs a workbench skill being registered in framwork/provider-map.json without this suite noticing, which the build's own unregistered gate would still catch
F19: Ruling: three assertions were proven RED by deliberate mutation rather than assumed — L4.5 by reverting the graph root to '.claude', L3.1 by making bash: deny unconditional again, L2.5 by turning one {{skill:}} back into a literal — each failed, the tree was restored, and the suite returned green — costs nothing; the plan's own Reviewer Handoff names "a validation level that was never RED" as gap 1
F19: complete (commits 4b4c4ab..11cc7fd, 17 new assertions, 3 proven RED by mutation / full suite 64 files 1567 passed 0 failed)
GRAPH: add-framework--brainstorm — none (pipeline entry point, orphaned before this delivery too); no gone/superseded entry
GRAPH: add-framework--plan — add-framework--brainstorm, add-framework--build; no gone/superseded entry
GRAPH: add-framework--build — add-framework--done, add-framework--plan, building-commands; no gone/superseded entry
GRAPH: add-framework--done — add-framework--build; no gone/superseded entry
GRAPH: add-artefact-graph — 11 dependants, all internal, all changed or named by this plan; 2 live entries, no gone/superseded
GRAPH: add-plan-authoring — add-framework--build, --done, --plan; no gone/superseded entry
GRAPH: building-commands — prompt-review-agent, add-framework--build, add-framework-development, add-framework-internal-layer, add-framework-product-layer; no gone/superseded entry
GRAPH: add-framework-internal-layer — add-framework--build; no gone/superseded entry
GRAPH: add-framework-development — add-framework--build, add-framework-internal-layer, add-framework-product-layer; no gone/superseded entry
GRAPH: add-framework-product-layer — add-framework--build; no gone/superseded entry
GRAPH: add-framework--roadmap — none (entry point); no gone/superseded entry
GRAPH: framework-discovery-agent — add-framework--brainstorm, add-framework--plan (DISPATCHES); no gone/superseded entry
GRAPH: plan-review-agent — add-framework--brainstorm, --plan, add-plan-authoring, add-review-discipline (DISPATCHES); no gone/superseded entry
GRAPH: plan-readback-agent — add-framework--build, add-review-discipline (DISPATCHES); no gone/superseded entry
GRAPH: prompt-review-agent — add-framework--build, --plan, add-review-discipline (DISPATCHES); no gone/superseded entry
GRAPH: EVERY dependant returned is itself internal and was either changed by this delivery or named in the plan's Ecosystem Impact table. No artefact depends on a touched artefact without appearing in the plan. Orphans: 27 total, 1 internal (add-framework--brainstorm, the pipeline entry point, orphaned before this delivery too). No new orphan.
GRAPH: NOT COVERED — scripts/build.js, scripts/graph.js, mcp/corpora.mjs, CLAUDE.md, .gitignore, .github/workflows/ and cli/tests/ produce no graph node, so nothing reports what depends on them. Each was swept by hand instead; add-artefact-graph's "What the Graph Cannot See" names this class.
F20: Ruling: F20 was NOT in this plan — it comes from a @prompt-review-agent audit of add-framework-internal-layer run at plan time for a SECOND plan document (2026-09-20T211543) that the user then dropped as redundant, this branch being the real delivery — the item is a live one-line defect independent of the workbench change and the plan was amended to carry it rather than shipping a known ❌ ruler item — costs one F-block executed under an amended plan, which the plan document now states
F20: Ruling: renumbered `## 8. Declaring Relationships` to `## 9.` rather than renumbering `## 8. Patterns to Enforce` — both citers (add-framework-internal-layer:84, CLAUDE.md:225) already mean the Declaring Relationships section, so they move together and Patterns to Enforce, which nothing cites, keeps its number — costs a reader holding an old `§ 8` pointer landing on Patterns to Enforce, which is what they land on today anyway
F20: Ruling: building-commands:480 still reads "§ 8" and was left untouched — it is the ruler's own worked example of two sections numbered alike, so it quotes the defect rather than pointing at a section — costs the example reading as stale once the defect is gone, against breaking the illustration if changed
F20: complete (commit 11cc7fd..36fbf5f, ruler item 2 resolves to one heading / ADD_GRAPH_WARNINGS=1 build.js exit 0, 0 warnings, 228 nodes 898 edges / build-workbench.js exit 0, 56 files / tree clean)
MERGE: Ruling: merged origin/main into the branch rather than rebasing — main had moved 14 commits (PR #78, the project backlog) since the base c6017c0, and a rebase rewrites every SHA this ledger cites as a commit range, destroying the delivery's own record — costs a merge commit in the history, which is what `gh pr merge --merge` produces here anyway
MERGE: complete (merge of origin/main a73519f, no conflict — CLAUDE.md merged clean, the inventory block and the § 9 table cell being different lines)
GATES: ADD_GRAPH_WARNINGS=1 node scripts/build.js — exit 0, 0 warnings, 230 nodes 901 edges (228/898 before the merge; backlog.sh and add-doc-schemas/references/backlog.md are the two new nodes)
GATES: node scripts/build-workbench.js — exit 0, 56 files, claude + opencode
GATES: Ruling: the cli suite failed once on mcp-engine.test.js 'exposes history, and answers it identically to the CLI module' and was ruled suite instability, not a regression — the isolated run passed 41/41 and a second full serial run passed 64 files / 1567 tests / 0 failed, and the suite is known to rewrite the sidecars this test reads while it runs — costs a real divergence between mcp/engine.mjs and scripts/graph.js going unnoticed if wrong, which CI would catch on a clean checkout
GATES: cli suite — 64 files / 1567 passed / 1 skipped / 0 failed (serial, second run)
PR: https://github.com/brabos-ai/code-addiction/pull/79 — branch feat/workbench-layer (local worktree-feat+workbench-layer), 21 commits over main
