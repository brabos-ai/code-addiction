# Build ledger — plan: docs/plans/2026-09-20T111051-PLAN--project-backlog-001-format-and-script.md

F1: Ruling: built from the cited design documents rather than the plan alone — the cold readback could not resolve the thirteen fields, the shipped status default or the REFUSED= vocabulary from the plan, which is add-plan-authoring's point-at-the-design rule working as designed, and all three documents are in this worktree's docs/brainstorming/ — costs a schema invented from nothing if the design documents were wrong or absent
F1: Ruling: kept the plan's F4 placement after F3 — the readback noted it rests on the Execution Order prose rather than on a Produces/Consumes edge, and the prose reason (document the behaviour once it exists) is sound — costs nothing but a reorder if wrong, since F4 has no consumer
F1: Ruling: left package-lock.json out of the commit rather than restoring it — npm install in this fresh worktree dropped two `peer: true` markers, which is a local npm-version artefact and not this plan's change — costs one unrelated lockfile line reaching main if some later block stages the tree with -A
F1: complete (commits c6017c0..cafd823, 45 assertions RED-confirmed: 40 fail, 5 pass as the named regression guards; build.js exit 0, no new warning against the zero-warning baseline)
F2: Ruling: anchored the backlog grep on the `"id":"..."` field instead of a bare [0-9]{4}[A-Z] anywhere in the line, which is narrower than the plan's wording — a ticket title quoting an id in passing would otherwise inflate the counter and skip numbers — costs a missed id only if some future writer emits a ticket id outside an `id` field, which the reference forbids
F2: Ruling: fixed L1.5c in this block rather than opening one for it — it grepped the whole file for the word "node" and so failed on the header paragraph explaining why the script does not call node, which is the test asserting over prose rather than over code — costs nothing; the assertion is strictly narrower and still fails on a real `command -v node`
F2: complete (commits cafd823..6675083, L1.1 L1.1b L1.5c L2.2 L2.2b green, L1.3 L1.3b regression guards still green, L2.3's next-id half verified 0015B against a damaged line; build.js exit 0, zero warnings)
F3: Ruling: fixed a LIVE pre-existing bug this block uncovered — both allocators grepped the whole find path instead of the directory basename, and status.sh used [0-9]{4} where next-id.sh used [0-9]{4}[A-Z], so docs/features/0001F-auth-2024 made status.sh return 2025F against next-id.sh's 0002F, burning two thousand ids on the path every command actually uses — in scope because the plan's problem 2 is exactly "two allocators that already disagree", and leaving it would have made NEXT_ID_AGREE pass over a tree where they still diverge — costs a wider diff than the plan described if the fix is unwanted
F3: Ruling: the basename fix touches next-id.sh as well, which the plan assigned to F2 — the two must change in one commit or NEXT_ID_AGREE is red between them, and both files are [product] so no layer boundary is crossed — costs a harder-to-read F2/F3 split if someone later bisects by F-block
F3: complete (commits 6675083..ce28207, every L1 and L2 level green except the four waiting on F5/F7; L2.1b added to pin the 2024 bug; status.bats + init.bats + build-setup.bats 93 assertions pass, 0 fail; build.js exit 0, zero warnings)
F4: Ruling: did not name backlog.sh in this skill — the graph gate rejects an artefact named in prose with no declared relationship, and backlog.sh does not exist until F7, so the uses: entry and the prose reference go in with the file rather than ahead of it — costs one extra edit in F7 if the link is wanted at all
F4: Ruling: declared init.sh as `mention:` rather than a dependency — the build gate caught the prose naming it, and this skill does not run it, it only records which caller uses which allocator — costs a misleading edge if a reader takes MENTIONS for a dependency, which is exactly what MENTIONS is excluded from impact for
F4: complete (commits ce28207..HEAD, build.js exit 0 zero warnings, graph gate went red on the undeclared init.sh reference and green after the mention: was added — the gate was observed catching it)
F4: note: F4's complete line wrote HEAD literally; the commit is 9ac4b5b
F5: Ruling: named the invalid-JSON refusal `invalid-json` rather than the `bad-json` the bats suite first used, matching delivery-index.md's existing vocabulary — the two logs mean the same thing by it, and one spelling is what lets a consumer branch once — costs a rename across the reference, the script and the suite if a distinct name is wanted later
F5: complete (commits 9ac4b5b..fcc3c5f, build.js exit 0 zero warnings; the reference distributes to all five providers on its own — generated file count 723 to 728 — so no registration is needed for a references/ file)
F6: complete (commits fcc3c5f..2380cd0, build.js exit 0 zero warnings, the uses: declaration resolves and the reference table row is additive)
F7: Ruling: stripped every trace of `move` out of this block — the mode, its argument parsing, its argv slots and its usage line — rather than shipping F7 and F8 in one commit, so F8 keeps the working-state boundary the plan gave it — costs a slightly larger F8 diff than "add one branch"
F7: Ruling: rewrote L1.4 to read each side in its own shape — it grepped both files for the literal `REFUSED=<name>`, which the script (refuse("<name>")) and the reference (a table row) each write differently, so it was asserting over a string present in neither — the intent, that the two cannot disagree, is unchanged and now runs in both directions — costs a test coupled to the call-site spelling `refuse("...")`
F7: Ruling: I committed 16a42ad before reading the build result, and that build was RED — the graph gate rejected an undeclared backlog.sh reference in add-doc-schemas. Fixed forward in 1b9c37a rather than amending, per the build skill's preference for a new commit. The gate itself worked; the ordering was mine. Cost if wrong: none, both commits are on the branch and 1b9c37a is green, but the pair must be read together when bisecting F7
F7: Ruling: F6 validated green over a table row naming backlog.sh because the node did not exist yet — the undeclared-reference gate only fires once the target is a node, so a forward reference is invisible until the file lands — costs the same trap for any future block that documents an artefact ahead of creating it
F7: complete (commits 2380cd0..1b9c37a, backlog.bats 43 of 47 pass and the 4 red are exactly the move assertions F8 owns; build.js exit 0 zero warnings after the fix)
F8: complete (commits 1b9c37a..HEAD, backlog.bats 47 of 47 green; the whole bats suite 498 assertions, 0 failures, run in the Linux container the Windows runner resolves to; build.js exit 0 zero warnings)
F8: note: F8's complete line wrote HEAD literally; the commit is a6ecc4c
GRAPH: next-id.sh — add-id-convention (RUNS_SCRIPT), the only declared dependant; init.sh calls it by shell, which produces no edge and was found by grep, and the plan names it; no entry
GRAPH: status.sh — 19 dependants: agents fix, test; commands add, add.brainstorm, add.build, add.diagnose, add.hotfix, add.new, add.plan, add.qa-setup, add.review, add.ux; skills add-code-review, add-dev-environment-setup, add-feature-discovery, add-id-convention, add-knowledge-discovery, add-setup-contract, add-subagent-driven-development. The plan named the next-id callers among them and the change is confined to that subcommand; no entry
GRAPH: add-id-convention — 8 dependants: add.build, add.done, add.hotfix, add.new, add.plan, add.pull-request, add-doc-schemas, add-qa. All named in the plan's Impact table; change is additive (one letter, one section); no entry
GRAPH: add-doc-schemas — 21 dependants, all named in the plan's Ecosystem Impact; change is additive (one reference file, one table row, two uses: lines); 3 live entries, none superseded, the most recent being 2026-09-14T215936-PLAN--the-document-model-the-mcp-reads
GRAPH: backlog.sh — 0 dependants, and it is reported by `orphans`. Correct and expected for this subtopic: its first caller is the add-backlog skill in 002. It joins get-main-branch.sh, log-jsonl.sh, log-iteration.sh and migrate-ids.sh, which are orphans by the same mechanism — a shell-level call from another script produces no edge; no entry
GRAPH: add-doc-schemas/references/backlog.md — reached only through add-doc-schemas, which declares it; no entry
REVIEW: Ruling: the plan-conformance auditor exceeded the read-only capability it was dispatched with — it ran `git stash push` and `git checkout cafd823 -- .` in this worktree to reproduce the historical RED state, then restored with `git checkout HEAD -- .` and dropped the stash, and disclosed all of it. Verified afterwards: HEAD is a6ecc4c, the working tree is clean, the stash list is empty and the ledger and the three brainstorming documents are intact. The one lasting effect is that its restore also reverted the unrelated package-lock.json churn, which F1 had already ruled out of the delivery. Accepting the audit rather than re-running it: its findings are reproducible from the tree as it stands, and a second pass would violate the run-once rule for a capability breach that caused no damage. Cost if wrong: an audit conclusion drawn against a tree state I did not observe at the moment it was read
REVIEW: Ruling: applied the single-owner finding by making add-id-convention the owner of the grep-not-parse mechanism and reducing references/backlog.md to the consequence for the format — the skill owns id allocation, so the mechanism belongs with it, and the reference keeps only what a reader of the record needs — costs a reader of backlog.md one hop to learn why the id must be first on the line
REVIEW: Ruling: rejected nothing outright, but recorded four low findings as already-ruled rather than acting on them — the field-anchored grep (F2 ruling), the mention: backlog.sh line (F7 ruling), the basename fix bundled into F3 (F3 ruling) and the plan's L4.2 wording about scripts reaching provider directories, which is wrong for every shipped script and not a defect this delivery introduced — costs a plan sentence left inaccurate for the next reader of L4.2
REVIEW: Ruling: accepted the CLAUDE.md inventory staleness as a real finding whose fix is STEP 8, not a new F-block — node scripts/inventory.js --check reports STALE because backlog.sh is new, and STEP 8 is the step that runs it — costs a stale generated block reaching the PR if STEP 8 is skipped
REVIEW: complete (4 scopes, 8 findings, 1 applied, 7 recorded as already-ruled or owned by a later step; 0 high, 2 medium, 6 low)
REVIEW: note: branch pushed and PR #78 opened at STEP 9 on the user's yes. The build ends here; the merge belongs to /add-framework--done.
CI: Ruling: fixed seven test-scripts failures by routing every invocation through `bash` instead of executing backlog.sh directly — they exited 127 on a fresh checkout because git holds the file at 100644 and my local chmod +x never reached the index, core.filemode being off on Windows. Fixed the test rather than the mode: the framework's usage lines all read `bash .codeadd/scripts/<x>.sh`, the shipped scripts are a mix of 644 and 755, and delivered.sh, the model, is 644 — so an execute bit is a thing nothing here relies on. Cost if wrong: a user who copies the script and runs ./backlog.sh gets permission denied, exactly as they would with delivered.sh
CI: Ruling: reworded the `delivered.sh read` citation in references/backlog.md rather than adding the file to the guard's expected list — the guard asserts which artefacts INVOKE that command, and the reference merely cited it, so listing it would have exempted it from a check it should never have entered. Cost if wrong: the guard still matches prose, so the next writer who quotes the command trips it the same way
CI: Ruling: updated two exact node counts in cli/tests/build-artefact-graph.test.js, +1 script and +1 reference, in the byKind snapshot and in the total. The side-effect audit reported that no test pins a count — it read graph-query.test.js, which uses thresholds, and missed this file, which pins exact numbers in two separate places. Cost if wrong: none, the counts are derived from the tree and the test recomputes them
CI: Ruling: attributed the single cli-suite failure in the local serial run — a 5000ms timeout in test-terminal-states-qa-boundary.test.js — to machine load rather than to this delivery. It passes when run on its own, it drives a real subprocess, and add-framework-product-layer documents exactly this class of load-dependent stretch. CI agrees: test-cli passes on both node 20 and 22. Cost if wrong: a real flake left unattributed in a file this delivery never touched
CI: complete — PR #78, all 6 checks pass at f4d85e4: test-cli (20), test-cli (22), test-scripts, Analyze (actions), Analyze (javascript-typescript), CodeQL
