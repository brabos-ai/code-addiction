# Build ledger — plan: docs/plans/2026-09-20T222814-PLAN--project-backlog-skill-lifecycle-and-rename.md

F0: Ruling: one worktree for the delivery, not one per subtopic — the Global Constraint "every subtopic is built in a separate git worktree" came from the umbrella intent, written when each subtopic was its own delivery; three topics now share one branch and a branch lives in one worktree — costs nothing if wrong, the constraint's intent (not the primary checkout) is satisfied either way
F0: Ruling: the readback read F13's "byte-identical" as possibly forbidding edits inside items 1.2/1.4 — built the plan's own wording, which exempts "the word roadmap in their own text"; item 1.2 does carry `git log docs/roadmap/index.md` twice and those ARE rewritten — costs a stale citation inside a renamed file if wrong
F0: Ruling: the readback asked whether "read first, skip" in F10/F11 is prose or an enforced gate — built as prose, because the five artefacts are markdown commands read by an agent and there is no script at those points to enforce anything; the enforcement that exists is backlog.sh refusing an undefined status — costs a duplicate no-op write on a resumed close-out if an agent skips the read
F1: Ruling: the four degradations of the design are four CATEGORIES, and DEGRADED= carries a finer string within them (no-remote and push-refused both sit under "the push could not land") — the design already required exits 1 and 2 to be distinguishable, so precise strings are what it asked for — costs a renamed string in F2 if wrong
F1: Ruling: a locked worktree exits 2 with REFUSED=worktree-locked rather than a DEGRADED= result — it is a state the caller must clear before the script can run, which is what exit 2 means in this script family; a degradation is a write that landed anyway, and this one does not land — costs one exit-code change in F2 if wrong
F1: Ruling: L1.6's first run went RED on its own fixture (a bare clone landing on no branch) instead of on the missing script — fixed the fixture and re-ran before committing, because a RED for the wrong reason proves nothing — costs nothing, the corrected test now fails on its assertion
F1: complete (commits 56665bc..dc79397, 17/17 RED for the right reason, build.js exit 0 with 0 warnings against a 0-warning baseline)
F2: Ruling: the worktree is created DETACHED rather than checked out on the base branch — `git worktree add <branch>` refuses a branch already checked out elsewhere, which is precisely L1.8's case (a capture from inside a linked worktree, base checked out in the primary); the local base ref is fast-forwarded afterwards, and only when nothing has it checked out — costs a ref nobody advanced if wrong, and DEGRADED=base-checked-out-elsewhere already reports that
F2: Ruling: a sixth DEGRADED string, base-checked-out-elsewhere, was added beyond the five the design names — it is the only state where the commit exists, the push failed AND the local ref could not be advanced, so silence would leave a sha reachable by nothing a reader was told about — costs one unused string if that state never occurs
F2: complete (commits dc79397..6f5327d, backlog-commit.bats 17/17 green, backlog.bats 47/47 unchanged with L1.5b passing, build.js exit 0 with 0 warnings)
F2: Ruling: MISS, recorded rather than hidden — F2's commit (6f5327d) left cli/tests/build-artefact-graph.test.js's node snapshot red (script 19 vs 20), because F2 touched only a .sh file and I ran bats and build.js but not the cli suite, which pins script counts; fixed forward in b993f59 rather than rewriting a commit — costs one red intermediate commit in the branch history, which a bisect across F2 alone would hit
F3: Ruling: F3 and F4 land as ONE commit — the plan orders F3 (write the skill) before F4 (register it), but build.js's graph gate fails an unregistered artefact, so F3 alone is a red build and the ledger forbids committing one; the two are the same unit of work seen from two files — costs nothing, both F-block ids are on the commit
F3: Ruling: two cli count assertions the plan never named are updated here — build-artefact-graph's node snapshot and mcp-server's add-doc-schemas dependant count (21 -> 22, because add-backlog declares skill: add-doc-schemas); each carries a ledger comment per the file's own convention — costs a second edit if the plan meant them for a later block, and there is no later block that could land them green
F3: Ruling: cli/tests/inventory.test.js L2.4 and run-bats.test.js L3.5 stay RED from this commit until STEP 8 — both assert the CLAUDE.md inventory block is current, a [product] F-block is forbidden to write CLAUDE.md, and add-framework--build assigns the sync to STEP 8 by design; attributed in isolation (fail here, pass on a clean 56665bc worktree) — costs a red cli suite at every intermediate commit from F3 to STEP 8, which CI on the PR will not see because STEP 8 lands first
F3: complete (commits 6f5327d..b993f59, build.js exit 0 with 0 warnings, add-backlog emitted to all 5 providers, graph edges resolve, cli suite delta vs clean baseline = the 2 inventory assertions owned by STEP 8, ruler items 1-4 ticked with evidence)
F4: complete (commits 6f5327d..b993f59, landed with F3 — see F3's ruling)
F5: Ruling: a ticket is read by id through `backlog.sh list --all` filtered on `"id":"<id>"`, not through a new get-by-id mode — backlog.sh's search matches title, tldr and notes and never the id (probed: search 0002B returns TICKETS_RETURNED=0), and adding a mode would reopen subtopic 001, which a Global Constraint forbids; the record format writes id first on every line precisely so it stays recoverable from raw text — costs a whole-board read per hook, which is one small file
F5: Ruling: lifecycle.md does not claim add.done's write takes the direct route — design 003 said it would, because the tree is on the base branch after a merge, but on the PR route gh merges on the forge and the local tree may still sit on the feature branch; backlog-commit.sh picks the route itself either way, so the claim was an optimisation stated as a fact — costs nothing, the behaviour is identical
F5: complete (commits b993f59..0ba7a51, build.js exit 0 with 0 warnings, full cli suite 1565 passed / 2 failed — exactly the two inventory assertions STEP 8 owns, node snapshot reference 71 -> 72)
F5: Ruling: CORRECTION to the line above — its commit range reads b993f59..0ba7a51, a sha typed before the commit existed; the real range is b993f59..f6b35c8 (git log is authoritative for what exists). Appended rather than edited, because the ledger is append-only — costs nothing, git wins for what exists
F6: complete (commits f6b35c8..3b7b67e, build.js exit 0 with 0 warnings, the four cli suites that read new-feature.md 149/149, validation gate's required set confirmed unchanged)
F7: Ruling: the five hooks declare the lifecycle reference and its parent skill, and NO script — a first pass declared script: backlog.sh and build.js flagged it a phantom edge, because the command's prose never names the script; lifecycle.md does. The command's contract is "follow your row of the reference", so the script edge belongs to the reference's owner (add-backlog declares both scripts) — costs a graph that cannot show add.brainstorm -> backlog.sh directly, which is a transitive fact through the reference
F7: complete (commits 3b7b67e..558acf4, build.js exit 0 with 0 warnings, injection anchors byte-identical to baseline — TOTAL=46 and ANCHOR_HASH=2422ad2810bc76c8 — and the five cli suites that read add.brainstorm 234/234)
F8: complete (commits 558acf4..9c93606, build.js exit 0 with 0 warnings, injection anchors byte-identical to baseline, the ten cli suites that read add.new 384/384, READ-ONLY GUARANTEE text at its original lines unedited)
F9: complete (commits 9c93606..583c7cf, build.js exit 0 with 0 warnings, injection anchors byte-identical to baseline, the 22 cli suites that read add.plan 806/807 — the one failure is inventory L2.4, owned by STEP 8)
F10: Ruling: the hook is inserted as STEP 2 item 5 and the former item 5 renumbered to 6 — rather than a free paragraph after the list, because the list is the step's execution order and the write belongs between build-setup.sh returning and status.sh re-running; grep confirmed no cli test pins that list's numbering — costs one renumbered line if a reader had cited "STEP 2 item 5" elsewhere
F10: Ruling: caught before commit — a first draft cited "STEP 14's report", which is Integration Verification; add.build's completion step is STEP 18, verified by which STEP loads add-final-report — costs nothing, fixed before the commit
F10: complete (commits 583c7cf..d9e0e48, build.js exit 0 with 0 warnings, injection anchors byte-identical to baseline, the 19 cli suites that read add.build 755/755)
F11: Ruling: 8.3 is placed BEFORE the "After merge, carry this into STEP 9" block, not at the very end of STEP 8 — the gitnexus graph-reindex anchor is the line "READ skill add-ecosystem ..." with next "---", so anything inserted between those two lines changes the anchor and breaks the plugin's injection at install; placing 8.3 earlier also puts the ticket result where the carry-list can name it — costs one extra bullet in that list
F11: complete (commits d9e0e48..07266a6, build.js exit 0 with 0 warnings, injection anchors byte-identical to baseline including add.done's two, the 14 cli suites that read add.done 560/560)
F12: Ruling: cli/tests/review-no-loops.test.js is a [product] path under an [internal] F-block — the tag table puts cli/ in product, and the plan tagged all of F12 internal; opened it as F12b [product] but landed both in one commit, because the plan requires the test to move with the rename or CI goes red, and applied both layers' checks (build.js, build-workbench.js, the cli suites) — costs nothing, the same precedent as F3+F4
F12: Ruling: web/public/artefact-graph.mmd IS updated here, although the plan listed it under "Does NOT Include ... generated" — it is generated AND tracked, so graph-mermaid.test.js failed on the stale node id; regenerated with the documented command (node scripts/graph.js mermaid --write), not by hand, and exactly one line changed — costs nothing, the plan's intent was "never hand-edit", which holds
F12: Ruling: framwork/.codeadd/scripts/tests/delivered.bats:878 keeps the string "add-framework--roadmap" — it is a fixture query that asserts NO entry matches (MATCHED_LIVE=0) against an index the test writes itself, so any non-matching string serves and the name points at nothing; changing it would edit another delivery's test to satisfy a grep, the reason design 004 left readme-analyzer alone. The plan's L3.5 sweep will list it, and this ruling is why it stays — costs one line a reviewer may flag
F12: complete (commits 07266a6..0d1a844, build.js exit 0 with 0 warnings, build-workbench.js exit 0 emitting only add-framework--backlog, graph resolves internal/command/add-framework--backlog with one USES_SKILL edge and no longer resolves --roadmap, cli suites graph-mermaid/review-no-loops/build-workbench/build-artefact-graph 122/122)
F13: complete (commits 0d1a844..c8740cb, build.js exit 0 with 0 warnings, git status --porcelain framwork/ empty, rename-aware diff = 4 lines (H1 + 3 --follow citations), CRLF preserved 104/104, git log --follow on the new path recovers 20 commits vs 1 without it)
F14: Ruling: re-tagged [product] from the plan's [internal] — both files are under cli/tests/, which the tag table puts in the product layer; applied the product checks (build.js, the cli suites) — costs nothing, no internal path is touched
F14: Ruling: the historical ledger comments in build-artefact-graph.test.js keep the name add-framework--roadmap and gain an appended line recording the rename, rather than being rewritten — they record why a count moved in a past plan, when the command WAS called roadmap; rewriting would falsify that record, the reason design 004 leaves delivered changelogs alone. close-out-hardening.test.js's sentence is present-tense and was renamed — costs a reader seeing the old name in a historical line, next to the note explaining it
F14: complete (commits c8740cb..e694862, build.js exit 0 with 0 warnings, diff proven comment-only — zero non-comment lines changed, both suites 63/63)
F15: Ruling: F15 also carries backlog-commit.bats line 50, which the plan never named — it is this delivery's own text from F1, it cited add-framework--roadmap as precedent, and it was the only add-framework-- string this delivery put into a product file; the L3.5 sweep surfaced it at F12, and F15 is the block for product text the rename made stale. Rephrased to describe the internal command without naming it — costs nothing, a comment
F15: complete (commits e694862..268eb24, build.js exit 0 with 0 warnings, references/backlog.md exactly 1 line changed, zero add-framework-- strings across the five product files this delivery wrote, bats backlog + backlog-commit 64/64, full cli suite 1565 passed / 2 failed — the two inventory assertions STEP 8 owns)
GRAPH: backlog-commit.sh — add-backlog (changed here); no entry
GRAPH: add-backlog — add.brainstorm, add.new, add.plan, add.build, add.done (all changed here); no entry
GRAPH: add-backlog/references/lifecycle.md — the same five commands and add-backlog (all changed here); no entry
GRAPH: add-doc-schemas/references/new-feature.md — plan-reviewer-agent, add.review, add-qa-spec, fragments/qa-pipeline/add.review.md and add-doc-schemas are direct dependants this delivery neither changed nor named; judged: the change is one OPTIONAL frontmatter field, outside the gate's required set, documented in the reference they already read — none needs a change; no entry
GRAPH: add-doc-schemas/references/backlog.md — add-backlog, add-doc-schemas (both named); no entry
GRAPH: add.brainstorm — add (the router) not changed or named; judged: it hands off to add.brainstorm and reads none of the text the hook added; no entry
GRAPH: add.new — 12 handoffs plus the gitnexus fragment; fragment anchor verified byte-identical, no handoff reads the edited STEP 1.1 table or STEP 2 list; no entry
GRAPH: add.plan — 21 dependants incl. three INJECTS_INTO fragments, anchors verified byte-identical; live 2026-09-13T100745-PLAN--readonly-agent-dispatch-capability, named in the plan's References
GRAPH: add.build — 20 dependants incl. three INJECTS_INTO fragments, anchors verified byte-identical; live 2026-09-19T122048-PLAN--optional-review-build-final-review, named in the plan's References
GRAPH: add.done — 10 dependants incl. two INJECTS_INTO fragments, anchors verified byte-identical; live 2026-09-11T014333-PLAN--product-close-out-parity, named in the plan's References
GRAPH: add-framework--backlog — none; FINDING: history resolves NOTHING under either name — both index entries for 2026-09-09T065116-PLAN--roadmap-command (by:done live, and by:verify changed from ff3bec2) carry node internal/command/add-framework--roadmap, which the rename removed. Text search still finds them (delivered.sh read roadmap). Not fixed here: the only fix is editing docs/delivered.jsonl, which design 004 forbids as the record; the close-out's verify step owns re-anchoring
F9: Ruling: PROCESS MISS, recorded as it happened — review-fix commit 8cbcab0 landed while its validation run showed a failure the baseline did not, because the validation and the commit were chained with `;` instead of being run and read separately; the failure (injection-exclusivity "all 46 full blocks") was then attributed in isolation and does NOT reproduce — 2/2 green on this branch and 2/2 on the clean 56665bc baseline — so it is the known load-dependent flake and the commit's content stands. From here on, validation and commit are separate commands and the commit waits on the result — costs nothing to the tree, but the ledger rule "commit after validation" was broken once and a reader should know
REVIEW: Ruling: REJECTED A1 high "STEP 8 (inventory sync) never ran" — STEP 8 runs AFTER STEP 7 by design ("after STEP 7's review pass, and not before"), and the auditor read the delivery during STEP 7; the two inventory assertions it cites are the ones every F-block ledger line already assigned to STEP 8, which runs next — costs nothing, STEP 8 is the very next step
REVIEW: Ruling: REJECTED A2 low "delivered.bats:878 still carries add-framework--roadmap" — already ruled at F12: a fixture query asserting NO match against a self-written index; the auditor itself called it "a known, deliberate, and already-documented exception" — costs one line a future reader may question, answered by the F12 ruling
REVIEW: Ruling: APPLIED A4 high add.plan ${TICKET} had no consumer — STEP 9.1 now writes the ticket's done_when under the objective, and STEP 4 names where each field goes (8cbcab0)
REVIEW: Ruling: APPLIED A4 medium add.build promised a ticket line in STEP 18 that STEP 18 did not have — added to metadata, and to Needs your attention when the write did not land (4c1d6ca)
REVIEW: Ruling: APPLIED A4 medium add.done promised a ticket line in STEP 9 that STEP 9 did not have — added to its "state, after the seven blocks" list (efdec14)
REVIEW: Ruling: APPLIED A1 medium add.brainstorm's spike instruction lived in 5.3, which a spike never runs — moved to 5.1, which runs on all three paths (4ee4859)
REVIEW: Ruling: APPLIED A1 medium the refused push had no test — L1.5b added, written green because the script already existed, then proven to bite by breaking the push-refused branch on purpose and watching it go red; script restored with no diff (47dbdd7)
REVIEW: Ruling: PATTERN noted — four of the five applied findings (A4 x3, A1 M3) share one shape: a hook promised a fact at its own site and never delivered it at the destination. Two auditors found it independently. It was this build's systematic blind spot, and a reviewer of a future hook should check the destination, not only the promise
REVIEW: complete (7 findings, 5 applied, 2 rejected)
