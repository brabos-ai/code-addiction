# Build ledger — plan: docs/plans/2026-09-21T002449-PLAN--agents-md-only-context-file.md

F1: Ruling: migration is per candidate — AGENTS.md is written through a temp file and renamed, the candidate is deleted only after that; a failure on a later candidate exits 1 and leaves earlier ones migrated — readback gap; all-or-nothing across three files adds rollback for no reader — costs a half-migrated root on an I/O error mid-run, which a re-run completes
F1: Ruling: an appended candidate goes in verbatim under `## Migrated from <file>`, its own headings not demoted — readback gap; rewriting user headings is an edit the user did not ask for — costs a heading-level mismatch the user tidies by hand
F1: Ruling: "contained" = the whole candidate, CRLF→LF and trailing newlines trimmed, is an exact substring of AGENTS.md (same normalisation) — readback gap; matches both old-copy shapes (AGENTS.md = CLAUDE.md + shell policy, GEMINI.md = copy) — costs a duplicate appended block when the user reordered text, never a lost line
F3: Ruling: the `codeadd-shell` block keeps the exact current policy text, only the markers are new — readback gap; the 2026-09-17 plan owns that text — costs nothing if the text was already right
F5: Ruling: `add.new`'s LEGACY_CONTEXT notice is informational and never blocks — readback gap; a feature spec does not depend on the context file's name — costs a user who keeps working with Claude Code reading the stale file until they run /add.wiki update
F1: Ruling: cli `inventory.test.js` L2.4 ("the block on disk is current") stays red from F1 until F8 regenerates the inventory block — a new script makes the CLAUDE.md scripts list stale, and a [product] block may not write CLAUDE.md; the plan assigns the sync to F8 — costs one known red derived-docs check on the commits F1..F7
F1: Ruling: baseline cli failures (build-workbench 8, inventory L3 9) came from an unbuilt workbench in the fresh worktree; `npm run setup` cleared them, so the baseline is 0 failures — costs nothing
F1: complete (commits 2a68d4c..7a97b70, bats migrate-context-files 10/10 via docker, build.js exit 0 no warnings)
F2: Ruling: the rename sweep also rewrote the old skill name in add.wiki, add-architecture-discovery, add-ecosystem and documentation-analyzer (name only — their content changes stay in F3/F4/F6/F7), plus add-ecosystem's catalog row for the skill — a rename leaves no dependent on the old name inside its own block — costs nothing; those files are edited again in their own blocks
F2: Ruling: the node snapshot in cli/tests/build-artefact-graph.test.js (script 20→21, nodes 233→234) is updated here, though the count moved in F1 — F1 missed it; fixed in the next block rather than amending a commit — costs a red snapshot test on commit 7a97b70
F2: Ruling: `run-bats.test.js` L3.5 fails on the same stale inventory block as `inventory.test.js` L2.4 — it runs `inventory.js --check`; same cause, same F8 fix — costs nothing beyond the F1 ruling
F2: complete (commits 7a97b70..dd18da0, build.js exit 0 no warnings; cli serial 1564 pass, 3 fail → 2 after the snapshot fix, both the known inventory staleness; RED seen for build.test documentation-analyzer and plain-language-rule on the old tree)
F3: Ruling: the shell-policy OS detection moved from the old STEP 7.2 into a new STEP 6.2, and the updater agent writes the block as task item 6 — the coordinator resolves the path, the agent owns every write to AGENTS.md, as it already did for the other blocks — costs nothing observable; the policy text is byte-identical apart from the markers and the dropped `---` separator the append used
F3: Ruling: on Linux/macOS the updater leaves an existing codeadd-shell block untouched rather than removing it — a repo shared with a Windows developer would otherwise lose it on every non-Windows run — costs a stale block on a repo that stopped using Windows, removable by hand
F3: Ruling: `add-agents-md-style` also gained the Shell Policy managed-block section and two checklist items, and add.wiki declares `script: migrate-context-files.sh` — the skill is the managed blocks' reference and build.js rejects an undeclared script name in prose — costs nothing
F3: complete (commits dd18da0..0fa1417, build.js exit 0 no warnings, injection-points still carries graph-contract; cli serial 1567 pass, 2 fail = the known inventory staleness; RED seen for plain-language L3.1/L3.2/L3.3/L3.6/L3.7 on the old tree)
F4: Ruling: the discovery skill's migration gate says "a leftover legacy context file" instead of naming CLAUDE.md/GEMINI.md — keeps the skill off F11's allowlist, which exists for migration and detection owners only — costs nothing; the Migration it points at names the files
F4: complete (commits 0fa1417..effcfab, build.js exit 0 no warnings; cli build, mcp-document-model, plain-language-rule, build-artefact-graph 232/232; no RED level — the plan puts F4's proof in L3, which goes RED→GREEN at F11)
F5: Ruling: RED was taken by running the new init.bats against the unchanged init.sh in this worktree, not in the throwaway worktree — its node_modules junction is not visible inside the docker mount — costs nothing; the script was untouched at that moment
F5: complete (commits effcfab..22e68da, init.bats 17/17 via docker with 3 new cases RED first, build.js exit 0 no warnings; cli tests reading add.new 289/289)
F6: Ruling: the leftover finding is DOC-008, not the unused DOC-003 — the plan says "next free id"; a gap may be a retired id, and reusing it would make two meanings share one number across old reports — costs nothing
F6: Ruling: the analyzer re-tests the three files itself rather than reading init.sh's LEGACY_CONTEXT line — it runs as a dispatched subagent with no init.sh output in hand; the plan allowed either, provided the filenames match F5's list exactly, which they do — costs nothing
F6: complete (commits 22e68da..5858d57, build.js exit 0 no warnings; cli build + build-artefact-graph 177/177; proof otherwise in L3 at F11)
F7: Ruling: conformance-agent's RULE_SOURCE value `claude-md+code` became `agents-md+code` — no command, test or cli module parses it (grep), and the old value named a file that no longer exists — costs a mismatch only for an external consumer this repo cannot see
F7: Ruling: `add-skill-creator/testing-skills-with-subagents.md` still links `examples/CLAUDE_MD_TESTING.md`, which does not exist — pre-existing dangling link, unrelated to the context file; only the prose "CLAUDE.md documentation variants" changed — costs one dangling link left for a later fix
F7: Ruling: plain-language-rule L3.5's guard (add-wiki-maintenance refuses the context file) now asserts AGENTS.md — the plan's L2.2 names it — costs nothing
F7: complete (commits 5858d57..9b4f788, build.js exit 0 no warnings; converge-gates.bats + delivered.bats 151/151 via docker; cli serial 1567 pass, 2 fail = known inventory staleness)
F8: Ruling: inventory.js's internal names `claudeMd`/`claudeMdPath` became `contextFile`/`contextFilePath` — not exported, no test reads them — costs nothing
F8: complete (commits 9b4f788..118834c, build.js exit 0 no warnings, `git status --porcelain framwork/` empty, `inventory.js --check` exit 0; root-file cli tests go red here by design and green in F9)
F9: Ruling: inventory.test L3.5 (the [product] block of add-framework--build forbids writing the context file) is red on commit e41c4dd and goes green in F10 — the test is [product] and the text it reads is [internal]; either order leaves one boundary red — costs one red test on one commit
F9: Ruling: two comments F2 wrote in build-artefact-graph.test.js were rephrased so they no longer name the legacy file or the old skill name — they would otherwise need an L3 allowlist entry for no reason — costs nothing
F9: complete (commits 118834c..e41c4dd, cli serial 1568 pass, 1 fail = inventory L3.5 awaiting F10; inventory L2.4 and run-bats L3.5 now green)
F10: complete (commits e41c4dd..b407eb6, `npm run setup` 56 files, no built copy names the old skill or CLAUDE.md; build.js exit 0 no warnings; `git status --porcelain framwork/` empty; cli serial 1569/1569 — inventory L3.5 green)
F11: Ruling: the gate searches for `CLAUDE.md`, `GEMINI.md` and `add-claude-md-style`, not the bare `claude-md` the plan listed — historical plan basenames (`...-PLAN--claude-md-inventory-block`) are correct citations and would force allowlist entries that mean nothing — costs a miss only for a new artefact named `*claude-md*` that is not the old skill
F11: Ruling: the test was written after F10 and its RED was shown against the throwaway worktree at 7a97b70 (L3.1 and L3.2 fail there) — the allowlist can only be stated once the sweep landed — costs nothing; the failing run on the old tree is the RED evidence
F11: Ruling: two injection integration tests failed once in the full serial run and passed on two isolated re-runs (one a 5000ms timeout, a different test each time); they passed in the F10 full run — load-dependent, not this change — costs a masked real failure only if both re-runs were also lucky
F11: complete (commits b407eb6..10565e5, agents-md-only 3/3 GREEN, RED on the old tree; L4: build.js exit 0 no warnings, injection-points keeps graph-contract, impact add-agents-md-style --depth 1 = add.wiki, add-architecture-discovery, add-doc-schemas, add-token-efficiency; add-claude-md-style has no node; inventory --check 0; cli serial 1570 pass + 2 load-flaky passing in isolation)
GRAPH: add-agents-md-style — add.wiki, add-architecture-discovery, add-doc-schemas, add-token-efficiency (all changed here); no entry
GRAPH: migrate-context-files.sh — add.wiki, add-agents-md-style (both changed here); no entry
GRAPH: add.wiki — unchanged direct dependants add.diagnose, add.done, add-knowledge-discovery, add-resource-path-convention hand off to it by name only; STEP numbers 6-9 kept, so none of them breaks; gitnexus fragment add.wiki.md unchanged and its graph-contract anchor survives; no entry
GRAPH: add-architecture-discovery — unchanged architecture-agent, system-design-agent load the skill and name no context file (grep); no entry
GRAPH: init.sh — unchanged add-feature-discovery reads RECENT_CHANGELOGS only, not ARCH:; no entry
GRAPH: add.new — unchanged dependants (add, add.audit, add.brainstorm, add.build, add.diagnose, add-feature-specification, add-id-convention, add-plan-review, add-qa-migration, add-review-discipline) hand off by name; the change is an informational notice; no entry
GRAPH: conformance-agent — unchanged failure-analysis-agent, security-agent; neither reads RULE_SOURCE (grep); no entry
GRAPH: add-health-check — add.audit (changed here); no entry
GRAPH: add-wiki-maintenance — unchanged add.done, add-knowledge-discovery load it for wiki upkeep, which this delivery did not change; no entry
GRAPH: add-tasks-checklist — add.build, add.plan, add.review, add-doc-schemas, add-subagent-driven-development (all changed here); no entry
GRAPH: add-project-scaffolding — none; no entry
GRAPH: rename-only artefacts (the remaining 33 .md in the diff) — each changed only the context file's name; its dependants read the same sections under the new file name; not queried one by one — costs a missed dependant that parsed the literal string "CLAUDE.md", which F11's repo-wide gate would have caught
GRAPH: repo root CLAUDE.md→AGENTS.md, scripts/inventory.js — NOT VERIFIED — neither is a graph node; swept by grep and by F11's test instead
REVIEW: Ruling: applied — the dead `examples/CLAUDE_MD_TESTING.md` line in add-skill-creator is removed (supersedes the F7 ruling that left it) — the rename made the sentence read wrong, and the link pointed at nothing — costs nothing (94f3f23)
REVIEW: Ruling: applied — "a AGENTS.md" → "an AGENTS.md" in inventory.test.js:420 and add-framework--build:559 — costs nothing (94f3f23, acbc259)
REVIEW: Ruling: rejected — ecosystem.md still names add-claude-md-style (side-effects auditor, medium) — the file is headed "AUTO-GENERATED by /add-framework--sync. DO NOT EDIT MANUALLY", the same owner the plan defers README and web/ to — costs a stale node in the ecosystem map until the next /add-framework--sync
REVIEW: Ruling: rejected — web/src/pages/docs.astro names the old skill (low) — declared out of scope in the plan, owned by /add-framework--sync — costs the same stale entry on the docs page until that run
REVIEW: Ruling: rejected — framwork/.claude|.cursor|.opencode/skills/add-claude-md-style left locally (informational) — gitignored build output that no commit carries and a fresh build drops — costs nothing
REVIEW: complete (6 findings, 3 applied, 3 rejected)
