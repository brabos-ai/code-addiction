# Build ledger — plan: docs/plans/2026-09-21T145331-PLAN--backlog-board-002-board-app.md

F1: Ruling: readback matched goal, order and F-blocks; its gaps are closed as — "reserved and not rendered" means no DOM at all: the shell's grid leaves room for a right panel and a nav group but mounts nothing for them, per the design's "rendered NOT AT ALL"; F4's visual direction is recorded as a ledger ruling, not a file; the definitions file carries `statuses[].order` (add-doc-schemas/references/backlog.md) and derived columns take first-seen order; "no new warning" is against the baseline measured below; when ports 4317–4327 are all busy the server exits 1 with one line naming the range — costs a later fallback to an OS-assigned port if that range proves crowded
F1: Ruling: pinned typescript ~5.9 instead of the current 7.x — 7 is the native port, and the TanStack plugin, vitest and Playwright are proven on 5.x — costs a later major bump
F1: Ruling: pinned jsdom ^27 — jsdom 30 requires node ^22.22 || ^24.15 and this machine runs 24.11; it is test-only — costs nothing at runtime
F1: Ruling: routeTree.gen.ts is committed — tsc -b in `npm test` runs before any vite build in CI and needs it; TanStack's own guidance commits it — costs a regenerated file in diffs that add routes
F1: complete (commits fe30dd2..99ceef1, npm run build emits dist/index.html; npm test 1/1 + tsc clean; build.js clean vs baseline; framwork/ clean)
F2: Ruling: added a Host-header check (127.0.0.1:<port> or localhost:<port>, else 403) and a --dist flag beyond the plan — the check closes DNS rebinding for the /api namespace that will carry writes; --dist lets the server tests run without a prior vite build — costs a 403 for anyone who reaches the board through another hostname
F2: complete (commits 99ceef1..cb1dce9, server.test.ts 13/13, observed RED 13/13 before server.mjs existed; npm test + tsc clean; build.js clean vs baseline; framwork/ clean)
F3: Ruling: routes.test.tsx (L2.3) was written after the route files; its bite was proven by breaking the board view's column order and filter on purpose (2 of 4 failed) and restoring it — tickets.test.ts (L2.1–L2.2) was RED first on the missing modules — costs nothing if the break-and-restore is accepted as the RED proof
F3: complete (commits cb1dce9..a58b589, tickets.test.ts 12/12 RED-first, routes.test.tsx 4/4 with bite proven, npm test 30/30 + tsc clean; vite build ok; build.js clean vs baseline; framwork/ clean)
F4: Ruling: visual direction — tone Data-Dense + utilitarian (add-ux-design allows two); differentiation anchor is the priority rank: each card and row carries its board position as a large tabular numeral, justified because line order IS the priority (frontend-design allows numbering only for a real sequence); palette "stone and cobalt" — light bg #F2F3EF, surface #FFFFFF, ink #15171C, muted #6A6F77, line #DFE1DB, accent cobalt #2D4BE0; dark bg #111317, surface #181B20, ink #ECEDEF, muted #9097A1, line #282C33, accent #8398FF; status hues open slate, doing amber, done green, dropped muted; one family, Geist Variable, bundled via @fontsource so the board works offline — chosen to avoid the listed AI defaults (cream+terracotta, black+acid green, identical SaaS card kit, all-caps eyebrows, mono data labels) — costs a re-skin if the user wants a warmer tone
F4: Ruling: from high-end-visual-design, adopted the spring curve cubic-bezier(0.32,0.72,0,1), tinted diffuse shadows and no default grey borders; rejected py-24 macro-whitespace, scroll-entry animations and double-bezel on every card — they fight a data-dense tool with dozens of cards, and frontend-design says motion only on user action; kept lucide icons at strokeWidth 1.5 instead of its banned-icons list — lucide ships with shadcn and a thin stroke meets the intent — costs a later icon swap if the thin lucide set still reads heavy
F4: Ruling: the status filter shows on the list only — on the board the columns already are the statuses, and the chips there were the noise that broke the toolbar into two rows at 1080 — costs one query param the board ignores in the UI (a status in the URL still filters columns)
F4: Ruling: the phone's selected column is component state, not a URL param — it is viewport-specific and meaningless on a wider screen where every column shows — costs a reset to the first non-empty column on reload
F4: Ruling: sentence case kept over web-interface-guidelines' Title Case rule — frontend-design and add-ux-design both ask for sentence case, and the two cannot both hold — costs a copy pass if Title Case is wanted
F4: Ruling: dates render in English (Intl 'en'), not the system locale — the interface copy is English and the real-data screenshot showed "Live, read este minuto" — costs the i18n pass the app will need anyway if it ever localises
F4: Ruling: the e2e spec was written after the screens; its bite is on record — its first run against the stale bundle failed 17 of 21, and three design passes each read 13 screenshots and fixed what they showed (toolbar wrapping, snap past padding, untruncated theme chip, orphaned id, raw plan basename as work id, unbroken long titles, mid-animation capture, tablet list column, focus ring on open, mixed-language date) — costs nothing if the recorded runs are accepted as the RED proof
F4: complete (commits a58b589..b796eee, npm test 30/30 + tsc; e2e 22/22 twice at mobile-360, tablet-768, desktop-1080 incl. dark mode; screenshots read at every width; real board of this repository served and read at 1080 and 360; build.js clean vs baseline; framwork/ clean)
F5: Ruling: the root cli vitest suite was not re-run for the two added TREE_EXCLUDES / NATIVE_COPY_EXCLUDES entries — they only remove board build directories from the copies, node --check passes, and the local cli suite is unstable here (memory: CI is the verdict) — costs a red CI run if a test enumerates the exclude lists
F5: complete (commits b796eee..61ecb68, npm run build:board ok from the root; npm run test:board 30/30; ci.yml parses with jobs test-cli, test-scripts, board; build.js clean vs baseline; build-workbench exit 0; framwork/ clean)
F6: complete (commits 61ecb68..c6a560d, AGENTS.md: board/ section with the zero-dependency asymmetry, setup paragraph, key-files row; build.js clean vs baseline; framwork/ clean)
F6: Ruling: corrected the new board/ section, which said it ships to users today — distribution is subtopic 004, not delivered yet — costs a wording update when 004 lands
F6: complete (commits c6a560d..479209d, follow-up wording fix; build.js unaffected)
GRAPH: board/ — not a node: the graph does not see app source (add-artefact-graph, What the Graph Cannot See); no entry
GRAPH: package.json, .gitignore, scripts/run-tests.js, .github/workflows/ci.yml, AGENTS.md — not nodes; checked by hand and by the side-effects auditor instead
GRAPH: backlog.sh — read-only runtime callee of board/server.mjs, unchanged; its graph dependant is add-backlog only; live entry, no supersession
F2: complete (commits 479209d..84309f5, review fix: SSE writes guarded and errored clients dropped — proven by dropping 20 live SSE clients then triggering a broadcast, server answered 200 after; engines ^20.19 || >=22.12; npm test 30/30)
F2: Ruling: rejected the finding that HEAD returns a body — Node's http server drops the body of a HEAD response itself; curl -X HEAD /api/board returned 0 bytes — costs nothing
F5: complete (commits 84309f5..40978f0, review fix: board/dist joins NATIVE_COPY_EXCLUDES; node --check passes)
REVIEW: complete (4 findings, 3 applied, 1 rejected)
