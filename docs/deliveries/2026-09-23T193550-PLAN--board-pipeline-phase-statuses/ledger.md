# Build ledger — plan: docs/plans/2026-09-23T193550-PLAN--board-pipeline-phase-statuses.md

BASELINE: build.js exit 0, 3 pre-existing warnings (add-ecosystem/SKILL.md stale `mention:` for backlog.sh, backlog-commit.sh, migrate-context-files.sh), injection points 46, graph 234 nodes / 925 edges — measured on main at branch point
BRANCH: feat/board-pipeline-phase-statuses
F20: Ruling: the phase check is TWO NAMED STATUSES per phase plus one work_id equality test, never an ordering comparison — the cold readback, given only the plan, reconstructed it as "a status at or past this phase", which is the ordering the design forbids and the exact failure design 003's risk table predicted; the plan described the check only through its test cases and never stated the rule, so F20's scope line now states it — costs a phase check that silently depends on status order if wrong, which would break the moment a user reorders their statuses
F4: Ruling: L1.2-L1.5 assert fallback resolution through backlog.bats, but backlog.sh reads a status in only two places (rejectUnknownStatus:259, UNDEFINED_STATUS:396) and both read s.name alone — it never consumes column, label or hidden, and board/server.mjs reads the definitions file directly with fs rather than through the script, so making backlog.sh resolve fallbacks would be surface nobody reads; L1.2-L1.5 are reframed to assert the canonical shape DEFAULT_DEFS writes, and the fallback SEMANTICS stay where their consumer is, in checkpoint 4's L13 — costs a fallback path proven nowhere if L13 is later narrowed, so L13.1-L13.3 are now the only proof and must not be dropped
F1: Ruling: the bats gate runs SCOPED to backlog.bats and status.bats via `node scripts/run-tests.js bats <file>` rather than the full 386-assertion suite — the runner accepts a file filter, the full suite takes ~69 minutes in the container, and backlog.bats already covers the next-id and status.sh interaction this change could reach; the full suite is CI's verdict, which /add-framework--done reads regardless — costs a regression in an unrelated .bats suite going unseen until CI if wrong
TICKET: 0005B set to doing, work_id=2026-09-23T193550-PLAN--board-pipeline-phase-statuses (ROUTE=worktree, SHA=d686c59, PUSHED=yes)
F1: complete (commits ffd3a24..9d72c18, build.js exit 0 with no warning absent from baseline; scoped bats backlog.bats+status.bats 109/109 ok including L3.2 DEFS_PRESERVED, L3.2b, L3.6, L1.4, L1.4b and L4.4 unmodified)
F4: Ruling: L1.6 (DEFAULT_DEFS names == references/backlog.md) moves into F2 and L1.6b (== lifecycle.md) into F3, instead of both landing in F4 — F4 runs before F2 and F3 in the plan's own execution order, so an F4 carrying them would commit two assertions that cannot be green until two blocks later, and the ledger forbids committing a block whose validation did not pass; each assertion now lands in the block that makes it true — costs the two equality guards arriving one and two blocks later than the rest if wrong, which is visible in the same checkpoint
F4: Ruling: RED was observed against the PRE-F1 tree in a throwaway copy (pre-F1 backlog.sh + the new assertions, run with npx bats), not against the branch — the plan's execution order puts F1 before F4 while its own Validation Matrix demands every level be proven RED before any block lands, and the two contradict; reverting F1 to satisfy the order literally would cost more than reproducing the pre-F1 tree — costs an assertion that was never truly RED if the copy diverged from the real pre-F1 tree, so the copy is taken from `git show ffd3a24:` rather than reconstructed
F4: complete (commits 9d72c18..d2ad96b, RED observed against a pre-F1 copy for all 7 new assertions on their assertion line; backlog.bats 60/60 ok with L3.2, L3.2b, L4.4, L1.4, L1.4b unmodified; build.js exit 0, 3 warnings = baseline)
F2: Ruling: the two passages F2 was told to revise -- references/backlog.md:93 "Never rewritten after that" and :161 "never rewritten once it exists" -- are NOT revised, because they stay TRUE; that scope line was written when the design still injected reserved statuses into an existing file, and dropping the injection is what kept both promises intact; F2 instead states that the SEEDED shape grew while the never-rewritten promise did not, so a reader cannot mistake one for the other -- costs a reader assuming the shipped four are still the seed if wrong, which L3.1d..L3.1h now contradict from the test side
F2: complete (commits d2ad96b..d669306, build.js exit 0 with 3 warnings = baseline; backlog.bats 61/61 ok; L1.4c added and proven by mutation -- renaming one status in the reference turns it red)
F3: complete (commits d669306..c31e3f1, build.js exit 0 with 3 warnings = baseline; backlog.bats 62/62 ok; L1.4d added and proven by mutation -- removing one status from the section turns it red)
CHECKPOINT 1: complete (F1-F4, commits ffd3a24..c31e3f1). Safe boundary per the plan.
F45: Ruling: the three baseline warnings are fixed by ADDING the missing rows to add-ecosystem's script table, not by deleting the three `mention:` declarations the build suggests removing -- the table lists 12 of the 15 declared scripts, so the map is genuinely incomplete, and backlog.sh plus backlog-commit.sh are the whole backlog subsystem this very delivery extends; deleting the declaration would silence the warning while leaving "the source of truth loaded by /add" wrong, which is what the warning text itself calls out -- costs three more rows to keep current if wrong, against a map that omits a subsystem
F45: Ruling: F45 is out-of-plan scope the user added mid-build, tagged [product] for framwork/.codeadd/skills/, and it runs BEFORE checkpoint 2 rather than at the end of its number -- clearing the baseline first means every later block's "no new warning" check runs against zero instead of against a list of three, which is strictly stronger -- costs nothing if wrong; the work is four table rows
F45: complete (commits c31e3f1..6cc79df, build.js exit 0 with ZERO warnings -- the three baseline warnings are gone. Every block from here measures "no new warning" against ZERO, not against a list of three)
F45: Ruling: the F45 commit message was amended once -- two heredocs in one shell command collided and the ledger line was swallowed into the subject line; the file change itself was correct and untouched, the commit was local, unpushed and the branch tip, and a follow-up commit cannot fix a subject line -- costs nothing; no history anything depends on was rewritten
CHECKPOINT 2 ORDERING -- analysis done, no block started. Two counters are pinned on disk and each can only be corrected AFTER the thing it counts exists, so the plan's order (F5 -> pairs -> F16 -> F17) leaves the cli suite red from F5 to F17, thirteen blocks, and the ledger forbids committing a block whose validation did not pass. Verified facts: (1) build-artefact-graph.test.js:1058 pins expect(nodes).toHaveLength(234) with a changelog comment at :1054 -- five fragment nodes make it 239; (2) the 46 literal counts COMMAND markers, not fragment sections -- extractInjectionPoints walks OPEN_MARKER_RE over the command source, so writing a fragment does NOT move it; (3) features.test.js:130 asserts registry-key -> fragment-directory and there is NO reverse check, so fragments may exist with no registry key; (4) build-artefact-graph.test.js:772's toHaveLength(4) is a synthetic fixture, not the real tree, and is unaffected.
CHECKPOINT 2 ORDERING -- proposed: write the five fragments first (F6, F8, F10, F12, F14) with the graph-total bump (F17) FOLDED INTO the last of them; then F5 the registry, which lands green because the directories exist and the commands list matches them 1:1; then the five command edits (F7, F9, F11, F13, F15) with the count-literal bump (F16) FOLDED INTO the last of them; then F18. Two merged blocks, every commit green. The alternative is ten red commits, and merging two blocks is the smaller departure from the plan.
F6: Ruling: each board fragment declares `- skill: add-backlog/references/lifecycle.md` ALONE, not the bare `- skill: add-backlog` the plan's scope line names -- no section's prose names the bare skill, only the reference, and F45 just fixed three warnings of exactly that shape (a declaration whose prose no longer names its target). The stale-acknowledgement check fires on `mention:` rather than `skill:`, so the bare entry would not warn, but declaring a dependency the prose does not have is the defect the move exists to fix -- costs one missing graph edge if some later section names the bare skill, which the same gate would then report
F45: Ruling: F45 was committed RED -- its row named CLAUDE.md and GEMINI.md, which agents-md-only.test.js L3.1 forbids outside an allowlist; F45 closed on build.js alone, which is the documented gate for a skill .md, and that gate never runs the cli suite where this guard lives. Measured, not assumed: the cli suite on ffd3a24 (main) is 1590/1590, and on 6cc79df (the F45 tip) it is 1589/1590 with exactly this failure. Fixed forward in F46 rather than by rewriting F45 -- the ledger never re-executes a block with a complete line -- costs one red commit on the branch history, now named here
BUILD METHOD: from checkpoint 2 on, every block is committed in a clean worktree (C:/tmp/board-base, branch board-cp2) and validated there on its exact committed state, then the delivery branch is fast-forwarded to it. The design of all five fragment/command pairs was rehearsed first in a separate throwaway worktree (C:/tmp/board-dry) against a captured baseline of the five built commands, with a harness applying only the board injection
F46: complete (commits 6cc79df..HEAD on board-cp2, build.js exit 0 zero warnings; cli suite 1590/1590 -- restores the count ffd3a24 had)
CHECKPOINT 2 ORDERING -- REVISED: the approved order (all five fragments first, then the registry, then the commands) rested on a false premise. I had verified that features.test.js carries no reverse check, and concluded none existed; loop-consolidation-0070.test.js L2.6 IS one -- "no orphaned fragment directory exists without a registry key" -- so the first fragment alone turns the suite red. injection-exclusivity's three-way equality (catalog x fragments x sidecar) then couples the registry to the command markers as well. The only order that keeps every commit green is ONE COMMIT PER PAIR: fragment + command + the registry growing by that command + every counter the pair moves. The plan already calls each pair atomic
F6+F7: Ruling: F6 and F7 land in ONE commit, and so does every later pair, with F5 (registry), F16 (count literals) and F17 (graph snapshot) distributed across the pairs instead of standing alone -- see CHECKPOINT 2 ORDERING -- REVISED; every other order leaves the suite red on L2.6 or on the three-way equality -- costs five F-block ids that no longer map one-to-one onto commits, which the commit trailers name explicitly
F6+F7: Ruling: ticket-resolve moved from the middle of its list to the end -- a marker pair inside a tight list leaves a blank line the injection cannot remove, which the existing tdd-pipeline step-list markers in add.plan already show in today's build; the three bullets are independent context inputs, so their order carries nothing, and moving it keeps the list clean with the feature off, which is the default -- costs a byte-identical site becoming a text-identical one, one line later
F6: Ruling: REVERSED -- the earlier ruling that each fragment declares the lifecycle reference alone was wrong. The graph gate reads the `{{skill:add-backlog/references/lifecycle.md}}` prefix as naming the bare add-backlog skill too, and refused the build until it was declared. The plan's scope line was right -- costs nothing now; it was caught by the gate before any commit
F6+F7: complete (commits 592d9ee..c44390d, build.js exit 0 zero warnings; L7.1 harness: frontmatter and intent byte-identical, resolve reordered, report reworded -- 3 hunks, 2 intended changes; cli suite 1590/1592, the 2 failures being inventory currency only, cleared by F47)
F47: Ruling: inventory sync pulled forward from STEP 8 to right after fragments/board/ appeared, as an [internal] block -- the internal-layer skill allows AGENTS.md writes only where the plan says, and the plan has no such block, but add-framework--build's STEP 8 mandates this exact sync on every build; this is that sync, early, so inventory.test.js L2.4 and run-tests.test.js L3.5 are red for one commit instead of for the rest of the delivery -- costs nothing: inventory.js --check is what STEP 8 will run anyway
F47: complete (commits c44390d..HEAD, AGENTS.md one line -- fragments list gains "board"; git status framwork/ empty; build.js zero warnings; cli suite 1592/1592)
F8+F9: complete (commits 089efa1..0f936ce, build.js zero warnings; L7.1: table row and IF block byte-identical, item 3 reworded -- the only hunk; two adjacent sites merged into one section; cli suite 1592/1592)
F10+F11: complete (commits 0f936ce..1be15e0, build.js zero warnings; L7.1: ticket-read byte-identical, done-when reworded -- the only hunk; cli suite 1592/1592)
F12+F13: Ruling: the ticket-doing paragraph sits between items 4 and 5, not after the last item as the plan said -- the last item is "re-run status.sh and continue to STEP 3", so a paragraph after it would read as writing the ticket after moving on, inverting the order the original item 5 had; the plan's actual intent (numbering that does not depend on the flag, cross-references by name) is fully met -- costs one loose list in STEP 2 with the feature off
F12+F13: Ruling: the fragment says "the build's final report" instead of "STEP 18's report" -- product-close-out-parity L4.6 forbids any file other than add.build.md from citing add.build's STEP 17 or 18, and it is right to: a renumber would break a fragment silently. Carry this into checkpoint 3's F27, whose in-review write anchors at STEP 18 and must not SAY so in the fragment text -- costs nothing
F12+F13: Ruling: test-terminal-states-qa-boundary L2.2 failed once on the full run and passed on three consecutive full runs of the identical tree -- it tests add.review (STEP 8, STEP 11, Playwright), which this pair does not touch, and the product-layer skill names load-dependent timeouts in subprocess-driving tests as the suite's remaining flakiness -- costs a real regression going unseen if three runs were all lucky, which CI will re-check
F12+F13: complete (commits 1be15e0..6aebf27, build.js zero warnings; L7.1: all three sites reworded -- the three hunks are exactly those; cli suite 1592/1592 on three consecutive runs)
F14+F15: Ruling: the ticket-report bullet stays in the middle of its list and accepts the one blank line the mechanism leaves, while add.brainstorm's bullet was moved to the end of its list -- the principle is one, not two: move when the order carries nothing, keep when it does; STEP 9's list ends with the next command, which is naturally the report's last line -- costs one loose list in STEP 9 with the feature off
F14+F15: Ruling: delivery-index L2.3 carried a PER-COMMAND pin (add.done has exactly two injection points) that the plan never listed and a grep for the total would never find; updated to five, with the three board sections asserted by name -- costs nothing; noted because the plan's F16 list of count sites was incomplete in the same way its reviewer's was
F14+F15: complete (commits 6aebf27..c3e10b1, build.js zero warnings; totals 60 points / 240 nodes / 946 edges -- identical to the dry-run end state; L7.1 over all five commands: every hunk is an intended one; cli suite 1592/1592)
F18: Ruling: F18 asserts only what no other suite does -- L7.3, the END-STATE MAP by name, board's own enable-order independence, and the absence of shared anchors -- because injection-exclusivity already iterates Object.keys(FEATURES) and so already runs board through exactly-once, byte-identical disable, idempotent re-enable, sibling isolation and reversed order; the plan's L6.1, L6.3 and L6.5 are therefore covered, not duplicated -- costs a reader looking for L6.1 in board-feature.test.js and finding it in injection-exclusivity
F18: Ruling: L6.2 (shared-anchor non-collision) has no subject -- no board marker shares an anchor line with another namespace, measured from the sidecar; F18 pins that fact instead of testing a collision that cannot happen -- costs nothing, and the pin turns red if a later edit makes one
F18: Ruling: L6.4 is scoped to BOARD's position in the enable order, with the other features kept in one fixed order -- reversing the whole list fails on a PRE-EXISTING defect, reproduced with board absent: tdd-pipeline and qa-pipeline anchor their step-list lines on the same line of add.plan, so whichever is enabled second lands first; injection-exclusivity's reversed-order test only checks that blocks land, never that bytes match. Out of scope here, and worth a ticket of its own -- costs that defect staying live until someone takes the ticket
F18: Ruling: L7.2 (the prompt reviewer over the five commands' reworded sites) is deferred to STEP 7's single batched pass instead of running at this checkpoint -- the reviewer ticks all eight items exactly once per artefact per delivery (add-review-discipline), and checkpoint 3 edits these same five commands again, so a pass now would spend each command's one full review on a version that is about to change -- costs the six rewordings going unreviewed by anyone but their author until STEP 7
F18: complete (commits c3e10b1..8eb5ac6, build.js zero warnings; RED proven for L7.3 (eleven offenders in the pre-change build) and for the registry and map (no board key at 6cc79df); L6.4 and the anchor pin are regression guards, never RED; cli suite 1597/1597)
CHECKPOINT 2: complete on board-cp2 -- 6cc79df..8eb5ac6. Safe boundary per the plan.
TICKET 0007B: registered at the user's request through add-framework--backlog -- "Feature enable order changes the bytes of the installed add.plan" (tdd-pipeline and qa-pipeline share one step-list anchor). ROUTE=worktree, PUSHED=yes, SHA=df96b14. Written to main, not to this branch
F48: Ruling: fragment placeholders are fixed IN this delivery, at the user's decision (option a) -- the CLI never resolved {{skill:}}, {{cmd:}} or {{addpath:}} in a fragment, which predates this delivery and already affected tdd-pipeline (default on) and qa-pipeline; moving the ticket instructions into fragments/board/ would have widened it to every board user -- costs a CLI change and two test rewrites inside a delivery that set out to do neither
F48: Ruling: the CLI carries its OWN copy of the build's resolution rule rather than importing it -- scripts/build.js is CommonJS at the repository root and is not in the npm package; fragment-placeholders.test.js P1 holds the copy equal to the build's own resolveResourcePaths for every provider that receives injection, the same equality-by-assertion pattern the repo uses for scripts/graph.js and mcp/ -- costs a second copy that only a test keeps honest
F48: Ruling: resolution uses the provider's SOURCE directory, not its install destination -- the built command a global install receives still names .opencode/skills/..., and an injected block must agree with the file around it -- costs nothing today; it would matter if the build ever started resolving against the destination
F48: Ruling: removal with a provider takes out the resolved block and then the raw one -- an install injected by a CLI from before this fix carries the raw form, and a disable run by the new CLI must still clean it; each pass is a no-op where its block is absent (P3 proves it) -- costs one extra scan per removal
F48: Ruling: injection-exclusivity and test-terminal-states-qa-boundary were rewritten, not worked around -- both counted the RAW fragment text in the installed file, which pinned the bug itself; they now expect the block resolved per provider, and the absence check rejects the raw form too, which the old one could never catch -- costs nothing; the old assertions were wrong
F48: Ruling: the heredoc layer between this session and the shell turns a doubled backslash into a single one. It corrupted cli/tests/test-terminal-states-qa-boundary.test.js ONCE in the worktree -- a grep built with an escaped pattern returned nothing, and a sed with the resulting EMPTY line address rewrote every line of the file; restored from HEAD in the throwaway worktree, never committed. Every heredoc-written file of this session was then re-checked with node --check and by reading the escapes. From here, a backslash is built with String.fromCharCode(92) or read from a file -- costs nothing now; recorded because it is silent and would have shipped a broken test file
F48: complete (commits 8eb5ac6..0cc67b0, build.js zero warnings; RED: P1 x3, the plain-text case, P2 and P2b failed before the fix -- P2 on raw placeholders found on disk in the installed commands; P3 is a regression guard; cli suite 1604/1604)
F19: complete (commits 0cc67b0..ae87753, build.js zero warnings; graph 241 nodes, reference 73; cli suite 1604/1604. RED for the checkpoint was observed up front: the parked contract suite fails 22 of 30 against 0cc67b0, the 8 that pass being regression guards -- five 'no fragment restates a rule', the L4.6 step-number guard, and doing/done already written on both sides)
F20: Ruling: add.brainstorm's shaped write follows the intent file (after 5.3's validation gate), not its report -- the plan's rule puts every exit write at the report step, but this command reports at 5.1, before the approval at 5.2 and the intent file at 5.3, so a report-time write would assert a file that does not exist yet; its failure line joins the 5.4 handoff instead -- costs one exit write that is not at a report step
F20: Ruling: when the phase check skips add.new's refining, the same write still carries feature -- the pointer must reach the board even when the brainstorm already shaped the ticket, and skipping it would bring back the gap the feature field exists to close -- costs nothing
F20: Ruling: lifecycle.md's Status Names keeps the nine names as a one-line list and drops its column table, which moved to phases.md -- a writer needs the exact strings at the moment it writes, and backlog.bats L1.4d holds that list equal to the other two copies -- costs a third copy of nine names, guarded
F20: Ruling: the bats gate for this checkpoint runs in the container from the worktree, with the worktree's root node_modules as a real copy instead of a junction -- the runner packs the tree without following a junction, so bats exited 127 inside the container with nothing in the grep, which reads like a pass; the user stopped a native run as too slow -- costs nothing; noted because no earlier claim depended on it: checkpoint 1's bats ran in the main checkout and checkpoint 2 changed no .sh
F20: complete (commits ae87753..d90757b, build.js zero warnings; phase-check sentinel once, work_id-stop sentinel once, "hook" zero times, no line pairs add.build with STEP 17/18; backlog.bats 62/62 in the container including L1.4d against the rewritten file; cli suite 1604/1604)
F49: Ruling: the container-testing lesson goes into workbench/skills/add-framework-product-layer, not into a CLAUDE.md as the user phrased it -- this repository carries no CLAUDE.md and agents-md-only L3.2 asserts it never will, and AGENTS.md cannot take it either: run-tests.test.js L3.5 allows AGENTS.md to name the runner exactly once and forbids restating its mechanics; AGENTS.md already names this skill as the owner of test mechanics -- costs nothing; the user's intent (write it down where the next session reads it) is met at the file the rules designate
F49: complete (commits d90757b..e9574d2, [internal]: build.js zero warnings, build-workbench exit 0, git status framwork/ empty; cli suite 1604/1604)
F21+F22: complete (commits e9574d2..ce066de, build.js zero warnings; substitutions 62; cli suite 1604/1604)
F23+F24: complete (commits ce066de..ec47519, build.js zero warnings after declaring backlog-commit.sh as a script; substitutions 64; cli suite 1604/1604)
F23+F24: note -- a post-commit grep for a literal backslash-n reported 22 lines; it was the heredoc layer reducing the pattern to a bare n. A node count of real backslashes in the fragment gives 0. Nothing was wrong with the commit
F25+F26: complete (commits ec47519..f06b556, build.js zero warnings; substitutions 65; cli suite 1604/1604)
F27+F28: complete (commits f06b556..b32aad3, build.js zero warnings; substitutions 66; cli suite 1604/1604 including product-close-out-parity L4.6)
F29+F30: Ruling: fix.md gains ticket: as optional frontmatter in the same pair -- the hotfix command now writes it, and the schema listed only id, type, severity, related and tags; mcp/reference.md is NOT touched, because the MCP type registry (mcp/types.mjs) records only required keys and a feature's optional ticket: is absent from it too, so the product-layer rule binding a document-model change to a reference update does not fire -- costs nothing unless the registry starts tracking optional keys
F29+F30: Ruling: the hotfix fragment declares /add.new and /add.done as mentions, not dependencies -- it names add.new only to say a hotfix does not run it, and add.done only as the reader of the ticket: it writes; the add.hotfix command already declares its own handoff to add.done -- costs nothing
F29+F30: complete (commits b32aad3..d59d7e9, build.js zero warnings; substitutions 70, graph 242 nodes / declares 138 / fragment 32; cli suite 1604/1604)
F31: complete (commits d59d7e9..660c59e, [internal]: build.js zero warnings, build-workbench exit 0, git status framwork/ empty; cli suite 1604/1604)
F32: complete (commits 660c59e..b01e694, [internal]: build.js zero warnings, build-workbench ok, framwork/ untouched; cli suite 1604/1604)
F33: complete (commits b01e694..d06e46c, [internal]: build.js zero warnings, build-workbench ok, framwork/ untouched; cli suite 1604/1604)
F34: complete (commits d06e46c..3e8c65d, [internal]: build.js zero warnings, build-workbench ok, framwork/ untouched; cli suite 1604/1604)
F35: Ruling: the new definitions file is generated by backlog.sh seeding a throwaway project and copied, rather than written by hand -- it makes this repository's vocabulary byte-identical to DEFAULT_DEFS, so the two cannot drift at the moment of the edit -- costs nothing
F35: Ruling: confirmed no other consumer before editing, as the plan required -- board/test/server.test.ts and board/e2e/fixture.ts write their own fixture files, build-artefact-graph and states.tsx only name the path in a comment or message, CI reads none, and the live readers are backlog.sh and board/server.mjs -- costs nothing
F35: complete (commits 3e8c65d..13e5d2e, [internal]: 9 statuses and 7 columns; no UNDEFINED_STATUS over the branch's five tickets; build.js zero warnings; framwork/ untouched; cli suite 1604/1604)
F36: Ruling: the plan's F36 (count literals and graph totals) was distributed across the pairs, as in checkpoint 2, and F36 is spent instead on committing the checkpoint's contract suite -- the suite was written and seen RED before F19, parked outside the tree while the blocks landed, and returned at the end -- costs an F-block id whose content differs from the plan's line, which this ruling and the commit name
F36: complete (commits 13e5d2e..ade5d2c, board-phase-writes 31/31 -- 22 of 30 RED against 0cc67b0, the 8 others regression guards; cli suite 1635/1635; build.js zero warnings)
CHECKPOINT 3: complete on board-cp2 -- d59d7e9 is the last product pair, 13e5d2e the last internal block, ade5d2c the contract. Safe boundary per the plan.
CHECKPOINT 4: start on board-cp2 at ade5d2c. Board deps: board/node_modules is a JUNCTION to the main checkout's (same package-lock); npm --prefix board test runs natively -- no container runner covers board/, and the suite takes ~16s. Baseline 40/40.
F37: Ruling: L13's server assertions are committed with F37, and the rest of F43 is distributed the same way (unit tests with the block they test, e2e last) -- board's npm test runs tsc over test/, so a test naming a type that does not exist yet cannot be committed ahead of it, and a test landed with its block is the RED->GREEN pair in one commit -- costs an F43 whose line no longer holds all four files
F37: note -- L13.4 was listed as a regression guard; its statuses half was, its columns half was RED like the rest (the field did not exist)
F37: complete (commits ade5d2c..4a92d7d, RED 7/7 before the change; board suite 47/47; build.js zero warnings)
F38: Ruling: normaliseTicket defaults feature to null -- a ticket line written before the fourteenth field reaches the view without it, and the type says string | null -- costs nothing
F38: complete (commits 4a92d7d..78ed20c, types only -- tsc is the check; board suite 47/47)
F39: Ruling: columnVisible lands here with the grouping instead of in F42 -- it is a pure function over Column, and tickets.ts is where the pure functions and their unit tests live; F42 wires it to the URL -- costs nothing
F39: note -- L14.1 is a regression guard for groupByStatus, which is unchanged (its own L2.1 tests still pass); the L14.1 assertion against groupByColumn was RED because the function did not exist
F39: complete (commits 78ed20c..87da96c, RED 5/5 before the change; board suite 52/52). The view does not read the new grouping yet, so the board is NOT broken here -- F40 is next
F40: Ruling: the column header loses its StatusGlyph and data-status, and the tab bar its glyph -- a column now holds several statuses, so one glyph would name one of them; status-by-shape moves to the card's StatusPill, which carries the glyph unchanged (F18 intact) -- costs the header's colour cue
F40: Ruling: the phone tablist is renamed from Status to Column -- it now switches columns; board.spec L3.6 names it and is updated in F43 -- costs one e2e selector
F40: Ruling: the card carries data-quiet when dimmed -- L15.5 needs a check that does not read Tailwind class names -- costs one attribute
F40: Ruling: a status filter in the URL (?status=) keeps a column when ANY of its statuses matches -- the old behaviour kept the status's own column, and this is its reading once columns hold several -- costs nothing
F40: note -- one run showed server.test L1.3 failing; F40 does not touch the server, and three consecutive full runs then passed 55/55. Recorded as instability in the process-spawning server tests, not as this block's failure
F40: complete (commits 87da96c..a90b9ed, RED 3/3 against the old view and card; board suite 55/55 x3; build.js zero warnings). The board reads columns now -- the F39/F40 no-stop window is closed
F41: complete (commits a90b9ed..0b2d446, RED 1/1 -- four columns before; board suite 56/56)
F42: Ruling: the filter bar renders one Show toggle per hidden column rather than a hard-coded dropped button -- the shipped definitions hide only dropped, so the default UI is exactly the plan's control, and a user who hides another column gets the same control for it -- costs nothing
F42: Ruling: column counts in hasFilters -- it drives the Clear button, which should also reset a column shown on request -- costs NoMatches reading a column param as a filter, which only matters on a board with zero matching tickets
F42: note -- L15.3 passed before F42: F40 already hid hidden columns (columnVisible with no param). It is a regression guard from here
F42: complete (commits 0b2d446..a289f4c, RED 3/3 -- parseBoardSearch column, L15.2, L15.4; board suite 60/60)
F43a: Ruling: server.test.ts's teardown is fixed in this delivery -- it failed intermittently (EPERM removing the temp project on Windows, L1.3 once and L13.2 once, then 1 in 8 after waiting for exit); the cause predates this delivery, but F37 added seven more server-spawning tests and made it frequent, and L17 needs a suite that passes -- the fix waits for exit, retries, and ignores a directory still held -- costs a possible leftover dir in %TEMP% per run
F43a: complete (commits a289f4c..ff2e258, board suite 60/60 on 8 consecutive runs)
F44: Ruling: the sheet's Feature fallback reads 'No feature yet' -- 'Not picked up' is Work's answer to a different question, and the plan asks only that Feature fall back the same way -- costs nothing
F44: complete (commits ff2e258..831fffb, RED 2/2 -- card and sheet; board suite 62/62)
F43: HARD STOP (L16.2) -- measured, not predicted. L16.1 PASSES: noSidewaysScroll holds at 360, 768 and 1080 with six columns (/board) and seven (?column=dropped); the row scrolls inside itself, the page does not. But the palette delivery's own F13 guard, board.spec L4.5 'a full board still fits the desktop viewport', FAILS at 1080: col-review and col-done sit past the right edge (seven: three columns). Control run: the same new code with the old four-status fixture passes L4.5 at 1080 -- the column COUNT is the cause, not F37-F44. Arithmetic at 1080: 1080 - 48 padding - 16px gaps leaves ~158px a column for six and ~193 for five, under the 15rem (240px) floor; four columns get ~246 and fit. Per L16.2 the build does not touch F13's sizing and does not weaken L4.5; the user chooses. Uncommitted in the worktree: e2e/fixture.ts (shipped nine/seven defs) and e2e/board.spec.ts (renamed selectors, L16.1 x2, L15.2-L15.4, L15.6) -- everything else in that suite passes (105 passed, 12 skipped by viewport). Delivery branch fast-forwarded to 831fffb, the last green commit
F43: USER DECISION 2026-09-23 (on the L16.2 stop): 'Aceitar a rolagem' -- ship the six default columns with the ROW scrolling at 1080; board.spec L4.5 is rewritten to assert the page never scrolls sideways and every column comes into view at the end of the row; a layout that fits them is its own delivery, registered as a ticket. F13's 15rem floor and equal-share sizing untouched
F43: Ruling: the e2e fixture writes the shipped definitions as a literal copy of docs/backlog.definitions.json rather than importing it -- the fixture runs from the Playwright config and must not depend on the repository's own board file changing -- costs one more copy of the vocabulary, not guarded by L1.4d
F43: note -- one full e2e run failed L15.6 at 360 (dialog not visible within 5s under full parallel load); 9/9 isolated and two further full runs clean (108 passed, 12 skipped by viewport). Recorded as load timing, not a defect
F43: complete (commits 831fffb..e6f7655, e2e 108/108 x2 across 360/768/1080 -- L16.1 passes at six and seven columns; board unit 62/62; root test:board passes; L16.3: index.css and ui.tsx diff empty since ade5d2c; build.js zero warnings)
CHECKPOINT 4: complete on board-cp2 -- F37-F44 plus F43a (server teardown). Last F-block of the plan.
F43: follow-up ticket 0008B 'Board layout that fits the six default columns on a 1080px desktop' registered on main through backlog-commit.sh (ROUTE=worktree, SHA 72710d5, PUSHED=yes)
GRAPH: product/command/add.brainstorm -- 3 direct dependants (add, add.new, board fragment); none unnamed and unchanged beyond handoffs; history: none dropped
GRAPH: product/command/add.new -- 13 direct dependants; unnamed and unchanged: plugins/gitnexus/fragments/add.new.md (INJECTS_INTO) -- its anchor is held by injection-exclusivity's combined features+plugins count, green at 1635/1635; the rest are HANDS_OFF_TO mentions that cite the command by name, not by step. history: no delivery recorded
GRAPH: product/command/add.plan -- 22 direct dependants; unnamed and unchanged: plugins/gitnexus/fragments/add.plan.md (INJECTS_INTO), same guard as above; tdd-pipeline and qa-pipeline fragments are named (L6.2 and ticket 0007B). Agents and skills are HANDS_OFF_TO by name
GRAPH: product/command/add.build -- 21 direct dependants; unnamed and unchanged: plugins/gitnexus/fragments/add.build.md (INJECTS_INTO), same guard; product-close-out-parity L4.6 (the add.build STEP 17/18 pairing) was re-run green
GRAPH: product/command/add.done -- 11 direct dependants; unnamed and unchanged: plugins/gitnexus/fragments/add.done.md (INJECTS_INTO), same guard; docs-pruning is named. history: live 2026-09-11T014333-PLAN--product-close-out-parity -- not superseded, its four-state cross and merge route table untouched
GRAPH: product/command/add.hotfix -- 9 direct dependants; unnamed and unchanged: plugins/gitnexus/fragments/add.hotfix.md (INJECTS_INTO), same guard; add-investigation is HANDS_OFF_TO by name. history: live 2026-09-19T105204-PLAN--hotfix-diagnosis-handoff-and-review-gate -- its fix.md receipt schema kept, ticket: added as optional beside it
GRAPH: product/skill/add-backlog -- 6 direct dependants, all six board fragments, all this delivery's. history: changed 2026-09-20T222814-PLAN--project-backlog-skill-lifecycle-and-rename -- that delivery's "only add.build and add.done write" is superseded by the seven writes, on purpose (plan umbrella)
GRAPH: product/reference/add-backlog/references/lifecycle.md -- 7 direct dependants (six fragments, add-backlog), all changed here
GRAPH: product/reference/add-backlog/references/phases.md -- 1 direct dependant (add-backlog), changed here; new node
GRAPH: product/reference/add-doc-schemas/references/backlog.md -- 2 (add-backlog, add-doc-schemas); add-doc-schemas unchanged, it only routes to the reference
GRAPH: product/reference/add-doc-schemas/references/fix.md -- 2 (add.hotfix changed, add-doc-schemas routes only)
GRAPH: product/skill/add-ecosystem -- 6 direct dependants (add, add.audit, add.diagnose, add.done, add.hotfix, add.wiki); F46 only removed three stale mention: lines, which no reader consumes
GRAPH: product/skill/add-resource-path-convention -- 1 (add-doc-schemas), unchanged; F45/F46 edit was to its own text
GRAPH: internal/skill/add-plan-authoring -- 4 (the four stages); add-framework--done is unchanged and unnamed as an edit target -- its STEP 8 done write is unchanged, and The Ticket still points it there. history: live 2026-09-22T093959-PLAN--backlog-board-003-internal-ticket-lifecycle, extended not superseded
GRAPH: internal/skill/add-framework--brainstorm -- 0 dependants
GRAPH: internal/skill/add-framework--plan -- 2 (brainstorm, build), both changed
GRAPH: internal/skill/add-framework--build -- 3 (done, plan, building-commands); done and building-commands unchanged -- they cite the build by name, not by step
GRAPH: internal/skill/add-framework-product-layer -- 1 (add-framework--build), changed
GRAPH: board/ (server.mjs, src/, test/, e2e/) -- NOT VERIFIED: the artefact graph does not model board/ source; the side-effects auditor covers its callers by reading
Ruling: STEP 7 -- 3 + 1 dispatched (plan conformance, diff completeness, side effects, and one batched @prompt-review-agent over 24 .md artefacts, all mode=delivery since no F-block cites a ruler item); all four reported
Ruling: review finding ACCEPTED (prompt-review, medium, item 6) -- add-framework-product-layer did not say whether the one-file container bats run or the full test:scripts closes a block; one sentence added, [internal]: build.js zero warnings, build-workbench ok, framwork/ untouched, cli suite 1635/1635 in the container; commit 4f1d4d3
Ruling: review finding REJECTED (prompt-review, low, item 6) -- ticket-resolve sits after Mental inventory in add.brainstorm instead of before it; already ruled at F6+F7: a marker pair mid-list leaves a blank line the injection cannot remove, and the bullets are independent inputs -- costs nothing
Ruling: review finding REJECTED as already-ruled (diff completeness, medium) -- add.hotfix carries a fourth board section, ticket-report, beyond F30's three named anchors; it is the report line every other command's fragment carries under lifecycle.md's degradation rule (L10.1: every write that did not land is one line in the final report), so a hotfix without it would report nothing -- recorded here because F29+F30 did not name it -- costs nothing
Ruling: review finding REJECTED as already-ruled (conformance, low) -- the plan says AGENTS.md is untouched; F47's inventory sync is build STEP 8's mandatory job, not scope
Ruling: review finding REJECTED as already-ruled (conformance, low) -- injection-core.js is on the Does NOT Include list; F48 changed placeholder resolution, not marker handling, on the user's explicit decision (option 3.a)
Ruling: side-effects auditor noted README.md and web/src/pages/docs.astro cite add.plan "STEP 8.1" for design.md while the step is 7.1 -- predates this branch (add.plan numbering untouched here); out of scope, left for /add-framework--sync
REVIEW: complete (6 findings, 1 applied, 5 rejected)
STEP 8: changelog 0992e39; plan status implemented; inventory.js ran -- AGENTS.md already current (F47 carried board)
STEP 9: pushed feat/board-pipeline-phase-statuses and opened PR #91 https://github.com/brabos-ai/code-addiction/pull/91 on the user's yes
STEP 10: ticket 0005B in-review write REFUSED=unknown-status -- backlog-commit.sh writes to main, and main's docs/backlog.definitions.json still carries the four old statuses; the nine arrive with this PR's merge (F35). Not retried with another status, per lifecycle.md; the ticket stays doing (work_id matches) until the close-out writes done
Ruling: the in-review write is lost for this delivery rather than worked around -- writing it would need main's definitions changed ahead of the merge, which is this PR's own content -- costs the ticket reading doing instead of in-review while PR #91 is open
