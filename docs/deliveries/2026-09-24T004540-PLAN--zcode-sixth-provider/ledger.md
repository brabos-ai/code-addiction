# Build ledger — plan: docs/plans/2026-09-24T004540-PLAN--zcode-sixth-provider.md

BASELINE: ADD_GRAPH_WARNINGS=1 node scripts/build.js — exit 0, 0 warnings (743 files generated)
F1: Ruling: committing F1 and F2 together in one commit rather than one commit each — the plan's own L1.1 RED expectation confirmed F1 alone throws (every existing agent has no explicit `providers` list, so it defaults to every provider including the new `zcode`, and build.js dies with "No agent frontmatter dialect for provider zcode" the instant the entry exists) — a red build cannot be committed per the ledger rule, so F1 and F2 share one BASE..HEAD range — costs nothing if wrong, it is a bookkeeping choice, not a scope change
F1: Ruling: the plan said zcode declares no independent "commands"/"skills" pattern, but the build crashed twice more once the agents pattern existed — several real agent bodies contain `{{skill:...}}`/no-`{{cmd:...}}` references, and resolveResourcePaths needs provider.commands/provider.skills to compute those paths for ANY provider carrying an "agents" pattern, regardless of whether that provider builds its own command/skill files — so zcode's entry now declares "commands" and "skills" as the SAME literal strings codex already uses. This does not create a second tree: the resolved `dir` is identical to codex's, so the redundant build pass overwrites the same bytes at the same path (confirmed: `git status --porcelain framwork/.agents/` is empty after the build). The "zero new files" and "one tree, one copy" claims in the plan still hold, just via a different mechanism than described — costs a plan-text correction, nothing functional
F1: Ruling: `framwork/.gitignore` was missing from the plan's Impact table entirely — it lists every generated provider directory to keep out of git (`.agent/`, `.agents/`, `.claude/`, ... `.windsurf/`), and `.zcode/` was absent, so the build's new `framwork/.zcode/agents/` output would have been committed as source. Added `.zcode/` to that file. Costs a stray generated-file commit if missed on a future provider addition, same as this one would have
F3: Ruling: `cli/tests/prompt.test.js` was absent from the plan's Impact table — it hardcodes the provider list `promptProviders()` returns for both project and global scope, and both lists needed `zcode` added (global, because zcode's `globalDest` is `.agents`, the same as codex's). Updated it alongside F3. Costs nothing if wrong — it is test coverage, not shipped behaviour
F1+F2: complete (commits 2d55bcb..04b80a3, ADD_GRAPH_WARNINGS=1 node scripts/build.js exit 0 no new warning; cli tests/build.test.js 128 tests green)
F3: complete (commits 04b80a3..0adbf80, ADD_GRAPH_WARNINGS=1 node scripts/build.js exit 0 no new warning; cli tests/providers.test.js + prompt.test.js green)
F4: complete (commits 0adbf80..96b3935, cli tests/mcp-registration.test.js 26 tests green)
F5: complete (commits 96b3935..452bc38, cli tests/uninstaller.test.js + uninstall-scope.test.js 17 tests green)
L3: Ruling: `cli/tests/install.e2e.test.js` was not named in the plan's Impact table — it is the file that actually proves L3.1/L3.2 (installing zcode alone, and codex+zcode together, write exactly one shared tree). Added two tests there rather than leaving those two Validation Matrix rows unproven by any test file. Costs nothing if wrong — coverage only
L3.3: NOT VERIFIED — no ZCode install is available in this environment. Cannot confirm whether ZCode's skill `name:` field accepts a dot (`add.plan`, the exact name codex's — and now zcode's — commands-as-skills already carry). This was flagged as a risk in the plan and is not gated on; per the plan's own Reviewer Handoff, its outcome is reported here rather than assumed, and ticket 0010B exists for the framework-wide question this could motivate
F6: complete (commits 082c209..7236647, ADD_GRAPH_WARNINGS=1 node scripts/build.js exit 0 no new warning; git status --porcelain framwork/ empty)
F7: complete (commits 7236647..abc98fc, ADD_GRAPH_WARNINGS=1 node scripts/build.js exit 0 no new warning; git status --porcelain framwork/ empty; grep for "5 provider"/"5 MCP-capable" in AGENTS.md returns nothing)
GRAPH: internal/skill/add-framework-product-layer — 1 direct dependant (internal/skill/add-framework--build, the build itself, already accounted for); no superseded/gone history entry conflicts with this delivery
GRAPH: every other touched file (framwork/provider-map.json, scripts/build.js, cli/src/*.js, framwork/.gitignore, AGENTS.md) is not an artefact-graph node — scripts/, cli/ and root config files are outside what the graph models (add-artefact-graph, What the Graph Cannot See)
REVIEW: Ruling: accepted the side-effects auditor's HIGH finding — `.github/workflows/release.yml`'s packaging loop was missing `.zcode`, which would have silently shipped a release with no ZCode agent files (the exact class of bug `cli/tests/release-packaging.test.js` already exists to catch, just for a different subdir). Fixed the loop, and added a generic regression test deriving the required subdir list from every provider's `dir`/`agentsDir` in provider-map.json rather than hardcoding `.zcode` — confirmed RED against the un-fixed workflow, GREEN after. Costs a silently broken release for any future provider added the same way, if missed again — the new test now closes that gap for good
REVIEW: Ruling: rejected the side-effects auditor's MEDIUM finding (README.md and web/ still say "5 providers") — the plan's own "Does NOT Include" explicitly excludes `add-framework--sync` (README/web regeneration), and AGENTS.md's own Web/Documentation section says those files are "Auto-updated by add-framework--sync before releases." Pre-sync staleness here is expected, not a defect. Costs nothing — a normal `/add-framework--sync` run before the next release closes it
REVIEW: Ruling: accepted the plan-conformance auditor's low/cosmetic note as no_change_needed — F7's AGENTS.md edit added one explanatory sentence about the reuse pattern beyond the plan's literal two-occurrence count change; it is accurate and consistent with F6, not a defect
REVIEW: complete (3 findings, 1 applied, 1 rejected as out of plan scope, 1 accepted with no change needed)
