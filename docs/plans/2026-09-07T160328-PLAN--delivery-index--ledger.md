# Build ledger — plan: docs/plans/2026-09-07T160328-PLAN--delivery-index.md

MATRIX: Ruling: the cli matrix file (cli/tests/delivery-index.test.js) is authored and committed RED before F1, and stays red until F14 — the plan's own "every level written and confirmed failing BEFORE any F-block lands" requires it — while each F-block's own gate remains its own level (build.js for artefact blocks, delivered.bats for F3). The delta rule for the rest of the run: the other 776 baseline tests must stay green at every commit; only delivery-index assertions may be red — costs a rewrite of the file into skipped-then-enabled form if a reviewer reads an intermediate red suite as an escaped failure
MATRIX: Ruling: force-added the plan, this ledger, and the four IN-SCOPE design docs (2026-09-07T123919 -01..-04) against the `docs/*` gitignore — the ledger must be tracked by STEP 4.1's own rule ("tracked, not scratch — it carries the rulings"), a tracked ledger whose identity line points at an untracked plan is a dangling pointer, and this plan's stated method is to reference the design set rather than restate it, so an untracked design set makes the tracked plan a pointer into nothing. The internal set (-05..-08) stays local: out of scope here and owned by the companion self-plan — costs six tracked working files the owner wanted local, reversed by `git rm --cached`
MATRIX: complete (commits d7341f3..483f7c4, 37/40 assertions confirmed RED; the 3 green are regression guards)
F4: complete (commits 483f7c4..147c3b5, 53/53 bats tests confirmed RED — zero green)
F1: complete (commits 147c3b5..90121cd, build.js exit 0, ADD_GRAPH_WARNINGS=1 zero, 205 nodes)
F3: complete (commits 90121cd..33da91d, delivered.bats 53/53 green after RED, build.js exit 0, zero warnings)
F2: Ruling: add-doc-schemas/SKILL.md names `delivered.sh` in its new table and declares it `- mention: delivered.sh`, which is a FIFTH graph edge beyond the plan's stated four — the alternative was to not name the script, and distorting prose to keep an edge count is exactly what CLAUDE.md provides `mention:` to avoid. It is semantically free: `impact` and `dependencies` both exclude MENTIONS, so the plan's stated graph semantics are unchanged — costs one edge a reviewer counting literally will query, answered by this line
F2: Ruling: the new "Format References (NOT Doc Schemas)" block is its own H2 rather than a subsection of "Schema Index by Category" — a section whose whole point is that it is NOT in that index reads as a contradiction nested inside it — costs nothing; it also made the matrix assertion expressible as "not a row in the index table" instead of "not anywhere between two named headings"
F2: complete (commits 33da91d..9bc2964, build.js exit 0, zero warnings, 684 edges, T1 matrix assertions green)
F5: Ruling: every command declaring `- script: delivered.sh` must also name the script in RUNNING PROSE, not only inside a bash fence — `proseOf()` (build.js:1029) strips fenced spans before the sniff, so a fence-only mention produces a phantom-edge WARNING, and L2.2b's bar is zero warnings, not "no new ones". Applied here and carried to F9 and F10 — costs one sentence per consumer; the alternative was `(conditional)`, an unproven waiver that would hide the very direction the warning exists to catch
F5: Ruling: `add.done` also declares `- skill: add-doc-schemas/references/delivery-index.md`, an edge beyond the plan's enumerated four, because 6.8 genuinely LOADS that reference and the hard gate at build.js:1117 failed the build without it. The plan's "four new edges" is a floor for the delivered.sh consumers, not a cap on the graph — costs nothing; a reviewer counting edges literally reads this line
F5: complete (commits 9bc2964..e36553f, build.js exit 0, zero warnings, injection 39/onDone 1, L3.1 green)
F6: complete (commits e36553f..9681353, build.js exit 0, zero warnings, injection 39/onDone 1, L3.2 green)
F7: Ruling: the pre-check uses TWO dots (`git diff main branch`), not the three the design and plan both wrote (`git diff main...HEAD`). Three-dot is merge-base-relative, so on the /add.pull-request route it still reports the entire feature diff — that diff IS what the branch added since the base, even though main now carries it — making the check non-empty exactly when it must be empty, and the direct-commit mode dead code. Two dots compare the two TREES, which is the question the design actually asks ("is there anything here main does not already have"). Proven by three new done.bats tests reproducing the squash-merged PR — costs nothing if wrong, because the two tests distinguish the routes and both pass
F7: Ruling: direct mode applies a patch (`git apply --index`) instead of `git checkout <branch> -- <paths>` — checkout cannot carry a DELETION, and with docs-pruning enabled STEP 6 deletes tracked files, which checkout would silently leave on main. A third bats test pins that — costs a slightly more complex branch; the alternative silently loses pruning on the PR route
F7: complete (commits 9681353..ef74d84, done.bats 28/28 green incl. 3 new PR-path tests, build.js exit 0, zero warnings)
F8: complete (commits ef74d84..ca7f252, build.js exit 0, zero warnings, L3.4 green — T2 done)
F9: complete (commits ca7f252..f1707f1, build.js exit 0, zero warnings, 688 edges, L3.5-skill + L3.7 green)
F10: complete (commits f1707f1..37120f1, build.js exit 0, zero warnings, 689 edges, L3.5-hotfix green)
F11: complete (commits 37120f1..6279082, build.js exit 0, zero warnings, 690 edges, L3.6 + L2.2 four-consumer green — T3 done)
T4: Ruling: F13 lands BEFORE F12, reversing the plan's stated T4 order. `cli/tests/features.test.js:125` asserts every registry key has a matching fragment directory and there is NO inverse assertion, so F12-first leaves a real pre-existing test red at that commit while F13-first keeps every intermediate commit green. Same reasoning the plan already accepted for F4-before-F3: "Consumes" is a logical dependency (the directory is named after the key), not a temporal one — costs nothing; the end state is identical and the swap is visible in the commit order
F13: complete (commits 6279082..26e4d00, build.js exit 0, zero warnings, 207 nodes, L4.6-L4.8 green, features.test.js still green)
T4: Ruling: F12 and F14 land in ONE commit, not two. `injection-exclusivity.integration.test.js` asserts a three-way correspondence — registry key <-> fragment <-> sidecar marker — so a commit carrying only two of the three is red whichever two they are, and no ordering fixes it. F13 was separable and did land alone — costs one commit carrying two F-block ids, both named in its trailer
T4: Ruling: bumped the hardcoded substitution counts in injection-exclusivity from 39 to 40 (and fixed a stale "38" in a test title that already disagreed with its own assertion). Updated, never deleted: the literal is the guard that forces a deliberate change whenever the substitution set moves, and computing it would remove the guard — costs a manual bump per feature added, which is the point
F12+F14: complete (commits 26e4d00..d969558, build.js exit 0, zero warnings, injection 40/onDone 2, 156/156 matrix+features+injection green)
