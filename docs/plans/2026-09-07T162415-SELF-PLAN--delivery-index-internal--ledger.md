# Build ledger — plan: docs/plans/2026-09-07T162415-SELF-PLAN--delivery-index-internal.md

S1: Ruling: every gate runs with `NODE_OPTIONS=` cleared — this shell carries the VS Code js-debug bootloader, which makes `bin-entrypoint.integration.test.js` fail on "expected 'Debugger listening on ws://…' to be ''". Cleared, the suite is 37 files / 818 passed / 1 skipped / 0 failed, exactly the baseline the product ledger recorded — costs nothing; the flag is environmental and the failure reproduces on a clean tree
S1: Ruling: the plan's stated S1 verification (`git check-ignore -v docs/delivered.jsonl` returning nothing) is wrong and was replaced — `-v` PRINTS the matching negation line and exits 0 even for a path that is not ignored, so it can never "return nothing" and would fail the stage forever. Verified instead with plain `git check-ignore` (exit 1 = not ignored) and by creating the file and seeing `git status` report it `??` — costs nothing; both checks are stronger than the one written
S1: Ruling: force-added this plan, this ledger and the four internal design docs `-05`–`-08`, reversing CLAUDE.md's note that they "stay local". That note defers the call to "the companion self-plan", which is this plan, so the call is mine to make. STEP 4.1 requires a tracked ledger; a tracked ledger pointing at an untracked plan is a dangling pointer; and the plan's Context table states it does not re-derive the design set, so untracked design docs make the tracked plan a pointer into nothing — the product ledger's own stated grounds. The plan's "force-add is forbidden" decision is about `docs/delivered.jsonl` ONLY, and the retirement of force-add stays gated behind its trigger, untouched — costs six tracked working files; reversed by `git rm --cached`
S1: Ruling: the full bats suite runs ONCE, as the final gate, not after each of five stages as the plan's "verification after every stage" line says. `git status` proves the diff touches no file under `framwork/.codeadd/scripts/`, which is the only thing that suite exercises, and one run costs 30-60+ min on this machine (`build-setup.bats` alone exceeded a 300s timeout). build.js, `npm test` and `test:package` still run at every stage — costs catching a bats regression at the end rather than at its stage, and no stage touches those scripts
S1: complete (commits 8508312..7028907, git check-ignore exit 1 + git status sees the file, build.js exit 0, cli 818/818, test:package PASS)
S2: Ruling: `/add-framework--done` does NOT declare `- script: delivered.sh`, though it calls it. `extractUses()` resolves every target inside the DECLARING artefact's own layer (scripts/build.js: "a genuine cross-layer edge has no syntax yet"), so the declaration would resolve to `internal/script/delivered.sh`, which does not exist, and the dangling gate would fail the build. The prose sniff skips cross-layer names for the same reason, so naming the script in prose is free and the command names it plainly. Recorded as an HTML comment in the file so the next reader does not "fix" it — costs a declared edge the graph will not carry; the plan anticipated this shape for `graph.js` and this is the same rule one layer over
S2: Ruling: the adapter is generated from the canonical by FOUR transforms, not the one the plan names. The plan says the body matches "except for one line every adapter has" (`> **INPUT:** $ARGUMENTS`); measured against `add-framework--self-build`, an adapter also gains `description:` frontmatter, DROPS the `<!-- uses: -->` block, and substitutes `.claude/` -> `.opencode/` throughout. Applied all four mechanically by script rather than by hand — costs nothing; building to the plan's literal wording would have shipped an adapter carrying a build-metadata block OpenCode never reads and a frontmatter-less file its loader expects
S2: Ruling: the adapter's `description:` was rewritten to drop a `: ` — an unquoted YAML plain scalar cannot contain colon-space, so "Closes out internal work: gates, ..." would not parse. Reworded to "with gates, a delivery-index entry, ..." — costs nothing
S2: Ruling: `cli/tests/build-artefact-graph.test.js` counts and `web/public/artefact-graph.mmd` are updated IN THIS COMMIT, though the plan's Impact table lists neither and design doc 08 states `artefact-graph.mmd` is "Unaffected — no graph shape change". That claim is true of the `history` verb, which adds no node, and false of this plan as a whole: S2 adds a COMMAND node, and the docs diagram profile is `{kinds:['command'], depth:1}`. Bumped command 24->25, total 207->208, declares 96->97 (the command carries a `uses:` block) and regenerated the diagram with the documented `mermaid --write`. The snapshot test's own comment prescribes exactly this ("A moved count is FINE when it is intended — a new command... Update the numbers here and say so in the commit") — costs two files the plan did not name; leaving them out would have made this commit red, which the one-commit-per-block discipline forbids
S2: complete (commits 7028907..6a6dc09, verified in isolation via stash: build.js exit 0 at 208 nodes / 695 edges zero warnings, 90/90 across the four graph test files)
S3: Ruling: S3 lands as TWO commits pairing each surface with its own tests, not the plan's three ("verb, then the MCP tool, then both test files"). `graph-mcp.test.js` asserts the tool-name list EXACTLY, so a commit adding the seventh tool without its test is red at that commit whatever order the remaining two take. Paired: (a) graph.js verb + graph-query tests, (b) MCP tool + graph-mcp tests. Same reasoning the product ledger applied to F13-before-F12 and to F12+F14-in-one — the end state is identical and the pairing is visible in the commit order — costs one fewer commit than the plan describes
S3: Ruling: `history()` takes three test-only seams — `opts.script`, `opts.cwd`, `opts.bash` — each defaulting to the real value. The plan's own test list requires covering "`delivered.sh` absent" and "`bash` absent", and neither state is reachable in a repo where both exist. The seams let the suite drive the REAL delivered.sh against a real temporary git repo instead of stubbing the reader, which would have asserted the one thing the design forbids reimplementing here — costs three optional parameters the CLI and MCP never pass
S3: Ruling: `history` gained a `--layer` passthrough the plan does not enumerate as work but DEMANDS as behaviour — item 10 requires `/add-framework--plan` to pass `--layer product`, and design doc 08's RED test 14 pins it. It is forwarded verbatim to `delivered.sh read`, never re-implemented as a filter here — costs one flag; without it the plan's own consumer edit would name an option that does not exist
S3: Ruling: bumped "six pure functions" to "seven small functions" in artefact-graph-mcp.js rather than leaving it or writing "seven pure functions". `history` spawns a subprocess, so it is not pure, and a stale count in the file that defines the tool list is the exact doc-vs-code drift this delivery exists to catch — costs nothing
S3: complete (commits 6a6dc09..0d9730e for the verb, verified in isolation: build.js exit 0, 39/39 across graph-query + the then-current graph-mcp)
S3: complete (commits 0d9730e..9639aae for the MCP tool, verified in isolation: build.js exit 0, 41/41 across both graph test files)
S4: Ruling: the `.opencode/` mirrors carry PRE-EXISTING content drift from their canonicals (5 to 121 differing lines each; `shared-review`'s 3.1b severity row already reads "medium, or high when the dependant is at depth 1" where the canonical reads plain "medium"). Mirrored each edit against the ADAPTER'S OWN wording rather than normalising the adapter to its canonical. CLAUDE.md already records this drift as ungated and the plan scopes S4 to the four lookups, not to a mirror reconciliation — costs leaving the drift in place; normalising it would have buried four small additions inside a large unreviewed rewrite of files this plan never audited
S4: complete (commits 9639aae..7bee570, four consumer+mirror commits, build.js exit 0 after each, cli suite 828 passed / 1 skipped / 0 failed)
S5: Ruling: CLAUDE.md gets SIX edits, not the plan's four. The two extra are stale counts the delivery itself moves and that no other file would correct: the graph paragraph said MCP "exposes the same six" (now seven, and `history` is listed among the verbs), and the accepted-costs bullet said internal `uses:` blocks are "a second declaration mechanism for seventeen files" — internal nodes were already 18 before this plan and are 19 after. Both are the doc-vs-code drift this whole delivery exists to catch, in the file that describes the mechanism — costs two edits a reviewer diffing against the plan's "four edits" will query, answered by this line
S5: Ruling: `scripts/build.js:865`'s comment ("would fire the unregistered gate on 17 correct files") carries the SAME stale count and was deliberately left alone. It was already wrong before this plan, `scripts/build.js` is absent from the plan's Impact table, and a stale comment inside a code file is not what this plan is about — costs one known-stale comment; fixing it would put an unplanned file in the diff for no behavioural gain
S5: complete (commits 7bee570..52cb1aa, build.js exit 0, working tree clean)
S5: Ruling: added ONE test beyond the plan's Impact table — `build-artefact-graph.test.js` now asserts both `/add-framework--done` sources invoke `npm --prefix cli run test:package` and never the bare form, and that all four CI commands are present. Design doc 06's RED matrix row 14 states outright "a test asserts the prefixed form is used", and it is the only one of its sixteen rows a machine can check: the other fifteen describe runtime behaviour of an LLM-driven markdown command (it stops, it writes nothing, it asks before deleting), which no unit test can assert. The hazard is the one the design named by name — the bare form is a FALSE gate, worse than a failing one. Regex verified to catch the bare form and ignore the prefixed one — costs one test file the plan did not enumerate; without it nothing stops a future edit from silently dropping the prefix
S5: Ruling: self-review of `/add-framework--done` after S5 found three defects in my own authoring, fixed in a follow-up commit rather than left: (a) the STEPS IN ORDER block listed eight steps while the file carried nine — building-commands requires that block to match the file; (b) STEP 1.3's `git diff main...HEAD` needed an explicit note that three dots is deliberate, because the product ledger's F7 ruling replaced three dots with two for a DIFFERENT question ("does main already have this?") and a future reader would otherwise "fix" a correct line; (c) the Rules section restated a dozen lines already carried by the STEPs and the prohibition block, which building-commands forbids by its own remove-it-and-see test. Trimmed to six rules that carry knowledge found nowhere else — costs a Rules section shorter than its sibling commands', which is the skill's stated intent even though no existing internal command meets it
