<!-- section:graph-map -->

**Graph available (GitNexus):** beyond grep, use the code knowledge-graph to **map structure** — modules, key call paths, entry points — before forming the questionnaire. Structural/relational questions ("what calls X", "how does this flow") → graph; literal text → grep. Mechanics: load skill `add-gitnexus`.

<!-- /section:graph-map -->
