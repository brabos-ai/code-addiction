<!-- section:graph -->

**GitNexus graph available:** before designing against an existing system, query the code knowledge-graph to ground your understanding of how it actually fits together — entry points, key flows, integration seams — so the design extends reality rather than an assumed shape. Structural/relational questions → graph; literal text → grep. Mechanics: load skill `add-gitnexus` (routes to `gitnexus-exploring`).

<!-- /section:graph -->
