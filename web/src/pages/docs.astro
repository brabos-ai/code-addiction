---
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";
---

<Layout title="Docs — Code Addiction">
  <!-- Nav -->
  <nav class="fixed top-0 w-full z-50 backdrop-blur-md bg-surface/80 border-b border-white/5">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 h-14 flex items-center justify-between">
      <div class="flex items-center gap-3">
        <button id="docs-sidebar-toggle" class="lg:hidden p-1 -ml-1 text-muted-light hover:text-foreground" aria-label="Toggle sidebar">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
        </button>
        <a href="/" class="font-display font-bold text-foreground tracking-tight text-lg">
          <span class="text-accent">+</span> code addiction
        </a>
      </div>
      <div class="hidden md:flex items-center gap-6">
        <a href="/" class="text-sm text-muted-light hover:text-accent transition-colors">Home</a>
        <a href="#commands" class="text-sm text-muted-light hover:text-accent transition-colors">Commands</a>
        <a href="#flows" class="text-sm text-muted-light hover:text-accent transition-colors">Flows</a>
        <a href="https://github.com/brabos-ai/code-addiction" target="_blank" rel="noopener" class="text-sm text-surface bg-accent hover:bg-accent-dim font-mono font-medium px-4 py-1.5 rounded-md transition-colors">
          GitHub
        </a>
      </div>
      <div class="flex md:hidden items-center">
        <a href="https://github.com/brabos-ai/code-addiction" target="_blank" rel="noopener" class="text-sm text-surface bg-accent hover:bg-accent-dim font-mono font-medium px-3 py-1.5 rounded-md transition-colors">
          GitHub
        </a>
      </div>
    </div>
  </nav>

  <!-- Mobile Backdrop -->
  <div id="docs-backdrop" class="fixed inset-0 bg-black/60 z-40 hidden lg:hidden backdrop-blur-sm transition-opacity"></div>

  <div class="flex pt-14">
    <!-- Sidebar -->
    <aside id="docs-sidebar" class="fixed top-14 left-0 w-64 h-[calc(100vh-3.5rem)] overflow-y-auto border-r border-white/5 bg-surface z-50 px-6 py-8 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 ease-in-out">
      <nav class="space-y-6 text-sm">
        <div class="lg:hidden pb-2 mb-2">
          <a href="/" class="flex items-center gap-2 text-accent hover:text-accent-dim transition-colors font-medium">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
            Back to Home
          </a>
        </div>
        <div>
          <p class="font-mono text-accent text-xs tracking-widest uppercase mb-3">Getting Started</p>
          <ul class="space-y-2">
            <li><a href="#about" class="text-muted-light hover:text-foreground transition-colors">About</a></li>
            <li><a href="#install" class="text-muted-light hover:text-foreground transition-colors">Installation</a></li>
            <li><a href="#quickstart" class="text-muted-light hover:text-foreground transition-colors">Quickstart</a></li>
          </ul>
        </div>
        <div>
          <p class="font-mono text-accent text-xs tracking-widest uppercase mb-3">Reference</p>
          <ul class="space-y-2">
            <li><a href="#commands" class="text-muted-light hover:text-foreground transition-colors">Commands</a></li>
            <li><a href="#flows" class="text-muted-light hover:text-foreground transition-colors">Flows</a></li>
            <li><a href="#scenarios" class="text-muted-light hover:text-foreground transition-colors">Scenarios</a></li>
          </ul>
        </div>
        <div>
          <p class="font-mono text-accent text-xs tracking-widest uppercase mb-3">Deep Dive</p>
          <ul class="space-y-2">
            <li><a href="#ecosystem" class="text-muted-light hover:text-foreground transition-colors">Ecosystem Map</a></li>
            <li><a href="#skills" class="text-muted-light hover:text-foreground transition-colors">Skills</a></li>
            <li><a href="#features" class="text-muted-light hover:text-foreground transition-colors">Optional Features</a></li>
            <li><a href="#plugins" class="text-muted-light hover:text-foreground transition-colors">Plugins</a></li>
            <li><a href="#providers" class="text-muted-light hover:text-foreground transition-colors">Providers</a></li>
            <li><a href="#structure" class="text-muted-light hover:text-foreground transition-colors">Project Structure</a></li>
          </ul>
        </div>
      </nav>
    </aside>

    <!-- Main content -->
    <main class="flex-1 lg:ml-64 px-6 sm:px-12 py-12 max-w-4xl">

      <!-- ABOUT -->
      <section id="about" class="mb-20">
        <p class="font-mono text-accent text-xs tracking-widest uppercase mb-4">Getting Started</p>
        <h1 class="font-display font-extrabold text-4xl sm:text-5xl mb-6">
          What is Code<br/><span class="text-accent glow">Addiction</span>?
        </h1>
        <p class="text-muted-light text-lg leading-relaxed mb-6">
          Code Addiction is a spec-driven framework for AI coding assistants.
          It standardizes how features go from idea to production through structured,
          repeatable workflows — installed in any project with a single command.
        </p>
        <div class="grid sm:grid-cols-3 gap-4">
          <div class="p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <div class="text-accent font-mono text-sm mb-2">.codeadd/</div>
            <p class="text-muted-light text-sm">Shared core: commands, scripts, skills, templates</p>
          </div>
          <div class="p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <div class="text-accent font-mono text-sm mb-2">Provider adapters</div>
            <p class="text-muted-light text-sm">Claude, Codex, Antigravity, KiloCode, OpenCode</p>
          </div>
          <div class="p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <div class="text-accent font-mono text-sm mb-2">codeadd CLI</div>
            <p class="text-muted-light text-sm">install, update, uninstall, doctor, validate, features, plugins, config</p>
          </div>
        </div>
      </section>

      <!-- INSTALL -->
      <section id="install" class="mb-20">
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-6">Installation</h2>
        <p class="text-muted-light mb-6">Requires Node.js 18+. Works on Windows, macOS, and Linux.</p>

        <div class="bg-white/[0.03] border border-white/10 rounded-xl p-6 font-mono text-sm space-y-3">
          <div><span class="text-muted select-none"># install in your project</span></div>
          <div><span class="text-accent select-none">$ </span><span class="text-foreground">npx codeadd install</span></div>
          <div class="pt-2"><span class="text-muted select-none"># install specific version</span></div>
          <div><span class="text-accent select-none">$ </span><span class="text-foreground">npx codeadd install --version v2.0.1</span></div>
          <div class="pt-2"><span class="text-muted select-none"># install from a specific branch</span></div>
          <div><span class="text-accent select-none">$ </span><span class="text-foreground">npx codeadd install --branch feature-xyz</span></div>
        </div>

        <h3 class="font-display font-semibold text-lg mt-8 mb-4">What gets installed</h3>
        <div class="bg-white/[0.03] border border-white/10 rounded-xl p-6 font-mono text-sm leading-relaxed">
          <span class="text-muted">your-project/</span><br/>
          <span class="text-accent">├── .codeadd/</span><span class="text-muted ml-6">← core (always)</span><br/>
          <span class="text-foreground">├── .claude/</span><span class="text-muted ml-8">← Claude Code</span><br/>
          <span class="text-foreground">├── .agents/</span><span class="text-muted ml-7">← Codex (OpenAI)</span><br/>
          <span class="text-foreground">├── .agent/</span><span class="text-muted ml-8">← Antigravity</span><br/>
          <span class="text-foreground">├── .kilocode/</span><span class="text-muted ml-4">← KiloCode</span><br/>
          <span class="text-foreground">└── .opencode/</span><span class="text-muted ml-4">← OpenCode</span>
        </div>
      </section>

      <!-- QUICKSTART -->
      <section id="quickstart" class="mb-20">
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-6">Quickstart</h2>
        <p class="text-muted-light mb-6">After installing, use these CLI commands to manage your setup:</p>

        <div class="space-y-3">
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd doctor</code>
            <span class="text-muted-light text-sm">Check environment health — Node, Git, installation status</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd validate</code>
            <span class="text-muted-light text-sm">Validate file integrity via SHA-256 hashes</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd validate --repair</code>
            <span class="text-muted-light text-sm">Restore missing or modified files from the release</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd update</code>
            <span class="text-muted-light text-sm">Update to latest release, or re-pull current branch</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd uninstall</code>
            <span class="text-muted-light text-sm">Clean removal of all Code Addiction files (user files are kept)</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd uninstall --force</code>
            <span class="text-muted-light text-sm">Skip confirmation prompt during uninstall</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd features</code>
            <span class="text-muted-light text-sm">List, enable, or disable optional features (e.g. TDD, Startup Test)</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd plugins</code>
            <span class="text-muted-light text-sm">List, enable, or disable MCP plugins (e.g. GitNexus, Playwright)</span>
          </div>
          <div class="flex items-start gap-4 p-4 rounded-lg border border-white/5 bg-white/[0.02]">
            <code class="text-accent font-mono text-sm whitespace-nowrap">npx codeadd config show</code>
            <span class="text-muted-light text-sm">Display installation configuration. Add <code class="text-foreground">--verbose</code> to check for updates</span>
          </div>
        </div>
      </section>

      <!-- Divider -->
      <div class="border-t border-white/5 my-16"></div>

      <!-- COMMANDS -->
      <section id="commands" class="mb-20">
        <p class="font-mono text-accent text-xs tracking-widest uppercase mb-4">Reference</p>
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-4">Commands</h2>
        <p class="text-muted-light mb-8">
          Every command is a slash command you type in your AI coding assistant.
          They orchestrate discovery, planning, implementation, and delivery.
        </p>

        <!-- Commands Map -->
        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4 mb-12">
          <!-- Discovery -->
          <div class="rounded-xl border border-white/5 bg-white/[0.015] p-5">
            <h3 class="font-display font-bold text-xs tracking-widest uppercase text-foreground mb-4">Discovery</h3>
            <div class="space-y-2.5">
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.brainstorm</code>
                <p class="text-muted text-xs mt-0.5">Explore ideas (read-only)</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.new</code>
                <p class="text-muted text-xs mt-0.5">AI-guided feature discovery → <code class="text-foreground">about.md</code></p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.init</code>
                <p class="text-muted text-xs mt-0.5">Project onboarding → <code class="text-foreground">owner.md</code></p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.xray</code>
                <p class="text-muted text-xs mt-0.5">Map architecture, classify apps</p>
              </div>
            </div>
          </div>

          <!-- Planning -->
          <div class="rounded-xl border border-white/5 bg-white/[0.015] p-5">
            <h3 class="font-display font-bold text-xs tracking-widest uppercase text-foreground mb-4">Planning</h3>
            <div class="space-y-2.5">
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.design</code>
                <p class="text-muted text-xs mt-0.5">UX spec, mobile-first. Coordinates subagents</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.plan</code>
                <p class="text-muted text-xs mt-0.5">Technical Planning Orchestrator</p>
              </div>
            </div>
          </div>

          <!-- Implementation -->
          <div class="rounded-xl border border-white/5 bg-white/[0.015] p-5">
            <h3 class="font-display font-bold text-xs tracking-widest uppercase text-foreground mb-4">Implementation</h3>
            <div class="space-y-2.5">
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.build</code>
                <p class="text-muted text-xs mt-0.5">Development Execution Specialist</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.autopilot</code>
                <p class="text-muted text-xs mt-0.5">Autonomous Feature Coordinator</p>
              </div>
            </div>
          </div>

          <!-- Quality -->
          <div class="rounded-xl border border-white/5 bg-white/[0.015] p-5">
            <h3 class="font-display font-bold text-xs tracking-widest uppercase text-foreground mb-4">Quality</h3>
            <div class="space-y-2.5">
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.review</code>
                <p class="text-muted text-xs mt-0.5">Feature Code Review Specialist</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.test</code>
                <p class="text-muted text-xs mt-0.5">Generate tests — coverage reported as signal, not enforced target</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.audit</code>
                <p class="text-muted text-xs mt-0.5">Full project health analysis</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.diagnose</code>
                <p class="text-muted text-xs mt-0.5">Pre-decision triage for ambiguous symptoms (read-only)</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.qa</code>
                <p class="text-muted text-xs mt-0.5">Agent-judged QA — judges the persisted qa.spec run across UX, functional, responsiveness, and a11y (audit, not a gate). playwright plugin adds live driving</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.qa-setup</code>
                <p class="text-muted text-xs mt-0.5">QA prerequisites + config bootstrap — installs the @playwright/test runner + chromium, generates a qa-project skill, scaffolds QA config</p>
              </div>
            </div>
          </div>

          <!-- Delivery -->
          <div class="rounded-xl border border-white/5 bg-white/[0.015] p-5">
            <h3 class="font-display font-bold text-xs tracking-widest uppercase text-foreground mb-4">Delivery</h3>
            <div class="space-y-2.5">
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.done</code>
                <p class="text-muted text-xs mt-0.5">Changelog, docs, finalize. Routes to PR or merge</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.pull-request</code>
                <p class="text-muted text-xs mt-0.5">Idempotent PR — creates new PR or appends update section if one is already open. Generates feature changelog on feature branches</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm" style="color:#ff4444">/add.hotfix</code>
                <p class="text-muted text-xs mt-0.5">Emergency fix with global ID (H[NNNN])</p>
              </div>
            </div>
          </div>

          <!-- Utility -->
          <div class="rounded-xl border border-white/5 bg-white/[0.015] p-5">
            <h3 class="font-display font-bold text-xs tracking-widest uppercase text-foreground mb-4">Utilities</h3>
            <div class="space-y-2.5">
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add</code>
                <p class="text-muted text-xs mt-0.5">Smart gateway — answers questions, guides flow</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-white/[0.03] px-4 py-2.5 hover:border-accent/20 transition-colors">
                <code class="text-accent font-mono text-sm">/add.ux</code>
                <p class="text-muted text-xs mt-0.5">Quick UX — loads ux-design for free context</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Divider -->
      <div class="border-t border-white/5 my-16"></div>

      <!-- FLOWS -->
      <section id="flows" class="mb-20">
        <p class="font-mono text-accent text-xs tracking-widest uppercase mb-4">Reference</p>
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-4">Flows</h2>
        <p class="text-muted-light mb-8">
          Pick the shortest path that fits. Less ceremony, same quality.
        </p>

        <!-- Flows HTML -->
        <div class="space-y-10">

          <!-- Complete -->
          <div>
            <h3 class="font-display font-bold text-sm tracking-widest uppercase mb-1">Complete Flow</h3>
            <p class="text-muted text-xs mb-4">Complex features with UI — full discovery, design, planning, implementation, review</p>
            <div class="flex flex-wrap items-center gap-2">
              <span class="flow-node">Brainstorm<br/><span class="flow-cmd">/add.brainstorm</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">New<br/><span class="flow-cmd">/add.new</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Design<br/><span class="flow-cmd">/add.design</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Plan<br/><span class="flow-cmd">/add.plan</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Code<br/><span class="flow-cmd">/add.build</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Check<br/><span class="flow-cmd">/add.review</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Done<br/><span class="flow-cmd">/add.done</span></span>
            </div>
          </div>

          <!-- Standard -->
          <div>
            <h3 class="font-display font-bold text-sm tracking-widest uppercase mb-1">Standard Flow</h3>
            <p class="text-muted text-xs mb-4">Features without complex UI — skip brainstorm and design</p>
            <div class="flex flex-wrap items-center gap-2">
              <span class="flow-node">New<br/><span class="flow-cmd">/add.new</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Plan<br/><span class="flow-cmd">/add.plan</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Code<br/><span class="flow-cmd">/add.build</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Done<br/><span class="flow-cmd">/add.done</span></span>
            </div>
          </div>

          <!-- Lean -->
          <div>
            <h3 class="font-display font-bold text-sm tracking-widest uppercase mb-1">Lean Flow</h3>
            <p class="text-muted text-xs mb-4">Small changes, quick tasks — minimal ceremony</p>
            <div class="flex flex-wrap items-center gap-2">
              <span class="flow-node">New<br/><span class="flow-cmd">/add.new</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Code<br/><span class="flow-cmd">/add.build</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">Done<br/><span class="flow-cmd">/add.done</span></span>
            </div>
          </div>

          <!-- Autonomous -->
          <div>
            <h3 class="font-display font-bold text-sm tracking-widest uppercase mb-1" style="color:#ffaa00">Autonomous Flow</h3>
            <p class="text-muted text-xs mb-4">Full AI implementation, no human interaction needed</p>
            <div class="flex flex-wrap items-center gap-2">
              <span class="flow-node">New<br/><span class="flow-cmd">/add.new</span></span>
              <span class="flow-arrow" style="color:#ffaa00">→</span>
              <span class="flow-node" style="border-color:#ffaa00;background:rgba(255,170,0,0.05)">Autopilot<br/><span class="flow-cmd" style="color:#ffaa00">/add.autopilot</span></span>
              <span class="flow-arrow" style="color:#ffaa00">→</span>
              <span class="flow-node">Done<br/><span class="flow-cmd">/add.done</span></span>
            </div>
          </div>

          <!-- Emergency -->
          <div>
            <h3 class="font-display font-bold text-sm tracking-widest uppercase mb-1" style="color:#ff4444">Emergency Flow</h3>
            <p class="text-muted text-xs mb-4">Critical bug in production — fast track to fix</p>
            <div class="flex flex-wrap items-center gap-2">
              <span class="flow-node" style="border-color:#ff4444;background:rgba(255,68,68,0.05)">Hotfix<br/><span class="flow-cmd" style="color:#ff4444">/add.hotfix</span></span>
              <span class="flow-arrow" style="color:#ff4444">→</span>
              <span class="flow-node">Done<br/><span class="flow-cmd">/add.done</span></span>
            </div>
          </div>

          <!-- Exploration -->
          <div>
            <h3 class="font-display font-bold text-sm tracking-widest uppercase mb-1">Exploration Flow</h3>
            <p class="text-muted text-xs mb-4">Don't know where to start? Brainstorm first, then pick any flow above</p>
            <div class="flex flex-wrap items-center gap-2">
              <span class="flow-node">Brainstorm<br/><span class="flow-cmd">/add.brainstorm</span></span>
              <span class="flow-arrow">→</span>
              <span class="flow-node">New<br/><span class="flow-cmd">/add.new</span></span>
              <span class="flow-arrow">→</span>
              <span class="text-muted text-sm italic">...pick your flow</span>
            </div>
          </div>

        </div>
      </section>

      <!-- Divider -->
      <div class="border-t border-white/5 my-16"></div>

      <!-- SCENARIOS -->
      <section id="scenarios" class="mb-20">
        <p class="font-mono text-accent text-xs tracking-widest uppercase mb-4">Reference</p>
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-6">Scenarios</h2>
        <p class="text-muted-light mb-8">Real-world examples of when to use each flow.</p>

        <div class="space-y-6">
          <!-- Scenario 1 -->
          <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-3">
              <span class="text-accent font-mono text-xs px-2 py-0.5 rounded bg-accent/10 border border-accent/20">COMPLETE</span>
              <h3 class="font-display font-semibold">"Build a user dashboard with charts and filters"</h3>
            </div>
            <p class="text-muted-light text-sm mb-4">Complex feature with UI, multiple user flows, needs discovery and design.</p>
            <div class="font-mono text-sm text-muted-light">
              <span class="text-accent">/add.brainstorm</span> → explore scope and requirements<br/>
              <span class="text-accent">/add.new</span> → define requirements, acceptance criteria<br/>
              <span class="text-accent">/add.design</span> → UX spec, mobile-first layouts<br/>
              <span class="text-accent">/add.plan</span> → technical architecture, task breakdown<br/>
              <span class="text-accent">/add.build</span> → subagent-driven implementation<br/>
              <span class="text-accent">/add.review</span> → automated code review<br/>
              <span class="text-accent">/add.done</span> → changelog, docs, merge
            </div>
          </div>

          <!-- Scenario 2 -->
          <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-3">
              <span class="text-accent font-mono text-xs px-2 py-0.5 rounded bg-accent/10 border border-accent/20">STANDARD</span>
              <h3 class="font-display font-semibold">"Add email notifications for order status"</h3>
            </div>
            <p class="text-muted-light text-sm mb-4">Backend feature, no complex UI. Needs planning but no design phase.</p>
            <div class="font-mono text-sm text-muted-light">
              <span class="text-accent">/add.new</span> → define notification triggers, templates<br/>
              <span class="text-accent">/add.plan</span> → service architecture, queue strategy<br/>
              <span class="text-accent">/add.build</span> → implement<br/>
              <span class="text-accent">/add.done</span> → finalize
            </div>
          </div>

          <!-- Scenario 3 -->
          <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-3">
              <span class="text-accent font-mono text-xs px-2 py-0.5 rounded bg-accent/10 border border-accent/20">LEAN</span>
              <h3 class="font-display font-semibold">"Add a loading spinner to the submit button"</h3>
            </div>
            <p class="text-muted-light text-sm mb-4">Small, well-defined change. Minimal ceremony.</p>
            <div class="font-mono text-sm text-muted-light">
              <span class="text-accent">/add.new</span> → quick spec<br/>
              <span class="text-accent">/add.build</span> → implement<br/>
              <span class="text-accent">/add.done</span> → finalize & merge
            </div>
          </div>

          <!-- Scenario 4 -->
          <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-3">
              <span class="font-mono text-xs px-2 py-0.5 rounded border" style="color:#ffaa00;background:rgba(255,170,0,0.1);border-color:rgba(255,170,0,0.2)">AUTONOMOUS</span>
              <h3 class="font-display font-semibold">"Implement the 5 API endpoints from the spec"</h3>
            </div>
            <p class="text-muted-light text-sm mb-4">Well-specified work. Let the AI handle it end-to-end with no interaction.</p>
            <div class="font-mono text-sm text-muted-light">
              <span style="color:#ffaa00">/add.new</span> → define scope<br/>
              <span style="color:#ffaa00">/add.autopilot</span> → AI implements everything autonomously<br/>
              <span class="text-accent">/add.done</span> → finalize
            </div>
          </div>

          <!-- Scenario 5 -->
          <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-3">
              <span class="font-mono text-xs px-2 py-0.5 rounded border" style="color:#ff4444;background:rgba(255,68,68,0.1);border-color:rgba(255,68,68,0.2)">EMERGENCY</span>
              <h3 class="font-display font-semibold">"Users can't login — auth token validation is broken"</h3>
            </div>
            <p class="text-muted-light text-sm mb-4">Critical production bug. Fast-track fix with tracking.</p>
            <div class="font-mono text-sm text-muted-light">
              <span style="color:#ff4444">/add.hotfix</span> → diagnose, fix, document with global ID<br/>
              <span class="text-accent">/add.done</span> → deploy
            </div>
          </div>

          <!-- Scenario 6 -->
          <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-3">
              <span class="text-accent font-mono text-xs px-2 py-0.5 rounded bg-accent/10 border border-accent/20">EXPLORATION</span>
              <h3 class="font-display font-semibold">"I want to add AI to the product but don't know where to start"</h3>
            </div>
            <p class="text-muted-light text-sm mb-4">Unclear scope. Start with brainstorming, then pick a flow.</p>
            <div class="font-mono text-sm text-muted-light">
              <span class="text-accent">/add.brainstorm</span> → explore possibilities, no commitment<br/>
              <span class="text-accent">/add.new</span> → lock in what you'll build<br/>
              <span class="text-muted">→ ...pick Complete, Standard, or Lean from here</span>
            </div>
          </div>
        </div>
      </section>

      <!-- Divider -->
      <div class="border-t border-white/5 my-16"></div>

      <!-- SKILLS -->
      <section id="skills" class="mb-20">
        <p class="font-mono text-accent text-xs tracking-widest uppercase mb-4">Deep Dive</p>
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-4">Skills</h2>
        <p class="text-muted-light mb-8">
          Skills are specialized knowledge modules loaded by commands.
          They provide domain expertise for backend, frontend, database, UX, security, and more.
        </p>

        <div class="grid sm:grid-cols-2 gap-3">
          {[
            { name: "backend-development", desc: "SOLID, Clean Arch, DTOs, Services, Repository — stack-agnostic" },
            { name: "frontend-development", desc: "State, data fetching, components, forms, routing — stack-agnostic" },
            { name: "database-development", desc: "Entities, repositories, migrations, naming — stack-agnostic" },
            { name: "ux-design", desc: "Components, mobile-first, SaaS patterns, shadcn, Tailwind" },
            { name: "security-audit", desc: "OWASP checklist, RLS, secrets, multi-tenancy" },
            { name: "code-review", desc: "Code review: IoC, RESTful, Contracts, Security (OWASP), Clean Architecture, SOLID" },
            { name: "feature-discovery", desc: "Discovery process, codebase analysis" },
            { name: "feature-specification", desc: "Structured about.md with RFs, RNs, criteria" },
            { name: "architecture-discovery", desc: "Map architecture, detect patterns, stack-context.md" },
            { name: "delivery-validation", desc: "Product validation: Requirements 100% implemented, prerequisites exist, acceptance criteria pass" },
            { name: "subagent-driven-development", desc: "Coordinate subagents with quality gates" },
            { name: "add-tasks-checklist", desc: "Use when generating, reading, or ticking tasks.md — defines the canonical 5-section schema, tick rules per section, failure marker semantics, 'non-trivial change' rule, and the architect subagent prompt template" },
            { name: "stripe", desc: "Stripe integration, price versioning, grandfathering" },
            { name: "token-efficiency", desc: "Compression, compact JSON, minimal tokens" },
            { name: "commit", desc: "Smart commit with auto-generated Conventional Commits messages" },
            { name: "planning", desc: "Technical planning — creates/updates plan.md with tasks and file mappings" },
            { name: "product-discovery", desc: "Founder + product blueprint via guided questionnaire → owner.md + product.md" },
            { name: "project-scaffolding", desc: "Scaffold Node.js projects (Express, Fastify, NestJS, Bun) — monolith or monorepo" },
            { name: "health-check", desc: "Tech health check suite: docs, security, architecture, data analysis" },
            { name: "plan-based-features", desc: "Subscription plan gating — 3-layer PlanFeatures pattern and IPlanService" },
            { name: "dev-environment-setup", desc: "Detect OS, install missing tools, configure VS Code terminal" },
            { name: "optimizing-git-workflow", desc: "Complete Git config: colors, aliases, smart defaults, performance" },
            { name: "backend-architecture", desc: "Backend architecture patterns and workspace conventions" },
            { name: "backend-architecture-workspace", desc: "Workspace-level backend architecture scaffolding" },
            { name: "frontend-architecture", desc: "Frontend architecture patterns and workspace conventions" },
            { name: "frontend-architecture-workspace", desc: "Workspace-level frontend architecture scaffolding" },
            { name: "ecosystem", desc: "ADD ecosystem map and framework overview" },
            { name: "investigation", desc: "Deep diagnostic investigation for ambiguous findings" },
            { name: "resource-path-convention", desc: "Consistent resource path conventions across the project" },
            { name: "id-convention", desc: "Canonical [NNNN][L] ID and branch naming format expected by next-id.sh, get-branch-metadata.sh, done.sh" },
            { name: "skill-creator", desc: "Create and structure new ADD skills" },
            { name: "claude-md-style", desc: "Use when generating or updating CLAUDE.md files — defines what belongs vs. what stays in skills/docs, format rules (JSON for data, markdown for rules/instructions), and line budget" },
            { name: "add-tdd", desc: "RED-GREEN-REFACTOR execution discipline for AI agents. Use when implementing any feature or bugfix that has (or should have) tests — forces a failing test confirmed for the right reason before any production code. Loaded by add.build and add.test." },
            { name: "add-test-specification", desc: "Generate contract test cases from feature requirements (RFs/RNs) and technical contracts. Use when add.plan needs to produce plan-test-spec.md mapping each requirement to testable cases before implementation." },
            { name: "add-qa-spec", desc: "Generate a code-free QA/E2E specification (reachability intent, UX acceptance criteria, functional E2E scenarios, target viewports, a11y expectations) from about.md + design.md + plan-*.md. Loaded by add.plan's qa-pipeline QA-Spec step." },
            { name: "add-qa", desc: "Use when running agent-judged QA validation (read-PNG by default; the playwright plugin adds live driving) — the Level C judge rubric, severity taxonomy, dual-axis (UX + functional) method, report schema/template, and the config.json/screens.json formats. Consumed by /add.qa and the qa-agent." },
          ].map(s => (
            <div class="flex items-start gap-3 p-3 rounded-lg border border-white/5 bg-white/[0.02]">
              <code class="text-accent font-mono text-xs whitespace-nowrap mt-0.5">{s.name}</code>
              <span class="text-muted-light text-xs">{s.desc}</span>
            </div>
          ))}
        </div>
      </section>

      <!-- OPTIONAL FEATURES -->
      <section id="features" class="mb-20">
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-4">Optional Features</h2>
        <p class="text-muted-light mb-6">
          Features are toggleable behaviors injected into commands via fragment files.
          Enable or disable them with <code class="text-accent font-mono text-xs">npx codeadd features enable|disable &lt;name&gt;</code>.
        </p>

        <div class="space-y-3">
          <div class="border border-white/5 rounded-lg p-4 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-2">
              <code class="text-accent font-mono text-sm">tdd</code>
              <span class="text-xs text-surface bg-accent/80 px-2 py-0.5 rounded font-mono">enabled by default</span>
            </div>
            <p class="text-muted-light text-sm mb-1">TDD Pipeline (test-first development)</p>
            <p class="text-muted text-xs">Injected into: <code class="font-mono">add.plan</code>, <code class="font-mono">add.build</code></p>
          </div>
          <div class="border border-white/5 rounded-lg p-4 bg-white/[0.02]">
            <div class="flex items-center gap-3 mb-2">
              <code class="text-accent font-mono text-sm">qa-pipeline</code>
              <span class="text-xs text-muted-light bg-white/10 px-2 py-0.5 rounded font-mono">disabled by default</span>
            </div>
            <p class="text-muted-light text-sm mb-1">QA pipeline (E2E authoring + agent QA validation)</p>
            <p class="text-muted text-xs">Injected into: <code class="font-mono">add.plan</code>, <code class="font-mono">add.test</code>, <code class="font-mono">add.build</code></p>
          </div>
        </div>
      </section>

      <!-- PLUGINS -->
      <section id="plugins" class="mb-20">
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-4">Plugins</h2>
        <p class="text-muted-light mb-6">
          Plugins integrate external MCP tools into ADD commands. Code Addiction handles utilization —
          it validates the tool is present, injects guidance into commands and agents, and activates
          plugin-bound skills. Plugins are <strong>disabled by default</strong> and require the
          external tool to be installed first.
        </p>

        <!-- gitnexus -->
        <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02] space-y-6">

          <!-- Header -->
          <div>
            <div class="flex flex-wrap items-center gap-3 mb-2">
              <code class="text-accent font-mono text-lg font-bold">gitnexus</code>
              <span class="text-xs text-muted border border-white/10 px-2 py-0.5 rounded font-mono">disabled by default</span>
              <span class="text-xs text-muted border border-white/10 px-2 py-0.5 rounded font-mono">mcp</span>
            </div>
            <p class="text-muted-light text-sm mb-1">Code knowledge-graph navigation (call graph, refs, blast-radius) via MCP. Enables structural code navigation across discovery, planning, diagnosis, and hotfix commands — going beyond grep to understand actual call chains and impact.</p>
            <p class="text-muted text-xs mb-1">Injects into: <code class="font-mono">add.new</code>, <code class="font-mono">add.plan</code>, <code class="font-mono">add.diagnose</code>, <code class="font-mono">add.hotfix</code>, <code class="font-mono">add.done</code>, <code class="font-mono">add.xray</code></p>
            <p class="text-muted text-xs">Activates skill: <code class="font-mono">add-gitnexus</code> — structural/relational code navigation (call graph, references, blast-radius, trace flows, safe refactors)</p>
            <p class="text-muted text-xs mt-1"><a href="https://github.com/abhigyanpatwari/GitNexus" target="_blank" rel="noopener" class="text-accent hover:underline">github.com/abhigyanpatwari/GitNexus</a></p>
          </div>

          <!-- Step 1: Install prerequisite -->
          <div>
            <p class="text-foreground text-sm font-semibold mb-2">1 — Install GitNexus</p>
            <p class="text-muted-light text-xs mb-2">Required before enabling the plugin. Code Addiction validates this with <code class="font-mono">gitnexus --version</code>.</p>
            <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
              <span class="text-accent select-none">$</span>
              <span class="flex-1 text-foreground" id="cmd-gn-install">npm install -g gitnexus</span>
              <button onclick="copyCmd('cmd-gn-install', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
            </div>
          </div>

          <!-- Step 2: Enable -->
          <div>
            <p class="text-foreground text-sm font-semibold mb-2">2 — Enable the plugin</p>
            <div class="space-y-2">
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-gn-enable">npx codeadd plugins enable gitnexus</span>
                <button onclick="copyCmd('cmd-gn-enable', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-muted select-none"># to disable later</span>
              </div>
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-gn-disable">npx codeadd plugins disable gitnexus</span>
                <button onclick="copyCmd('cmd-gn-disable', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
            </div>
          </div>

          <!-- Step 3: Post-enable setup -->
          <div>
            <p class="text-foreground text-sm font-semibold mb-2">3 — Wire and index (once per repo)</p>
            <p class="text-muted-light text-xs mb-2">Run these after enabling. Re-run <code class="font-mono">analyze</code> after large changes.</p>
            <div class="space-y-2">
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-gn-setup">gitnexus setup</span>
                <button onclick="copyCmd('cmd-gn-setup', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-gn-analyze">gitnexus analyze</span>
                <button onclick="copyCmd('cmd-gn-analyze', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
            </div>
            <p class="text-muted text-xs mt-2">Verify: ask Claude to run <code class="font-mono">gitnexus list_repos</code> (or <code class="font-mono">/mcp</code>) — this repo must appear indexed. If it does not, commands silently fall back to grep.</p>
          </div>

        </div>

        <!-- playwright -->
        <div class="border border-white/5 rounded-xl p-6 bg-white/[0.02] space-y-6 mt-6">

          <!-- Header -->
          <div>
            <div class="flex flex-wrap items-center gap-3 mb-2">
              <code class="text-accent font-mono text-lg font-bold">playwright</code>
              <span class="text-xs text-muted border border-white/10 px-2 py-0.5 rounded font-mono">disabled by default</span>
              <span class="text-xs text-muted border border-white/10 px-2 py-0.5 rounded font-mono">mcp</span>
            </div>
            <p class="text-muted-light text-sm mb-1">Adds live browser driving (screenshots + console/network) to the already-present agent-judged QA validation via Playwright MCP. QA works out of the box from persisted run evidence (screenshots + assertion/axe results) — this plugin lets qa-agent additionally drive the app live for richer evidence.</p>
            <p class="text-muted text-xs mb-1">Injects into: <code class="font-mono">add.qa</code></p>
            <p class="text-muted text-xs">Injects into agent: <code class="font-mono">qa-agent</code></p>
            <p class="text-muted text-xs mt-1"><a href="https://github.com/microsoft/playwright-mcp" target="_blank" rel="noopener" class="text-accent hover:underline">github.com/microsoft/playwright-mcp</a></p>
          </div>

          <!-- Step 1: Install prerequisite -->
          <div>
            <p class="text-foreground text-sm font-semibold mb-2">1 — Install Playwright MCP</p>
            <p class="text-muted-light text-xs mb-2">Required before enabling the plugin. Code Addiction validates this with <code class="font-mono">npx --yes @playwright/mcp@latest --version</code>. Run <code class="font-mono">/add.qa-setup</code> for a guided, per-OS install of chromium + the Playwright MCP server.</p>
            <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
              <span class="text-accent select-none">$</span>
              <span class="flex-1 text-foreground" id="cmd-pw-install">npx --yes @playwright/mcp@latest --version</span>
              <button onclick="copyCmd('cmd-pw-install', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
            </div>
          </div>

          <!-- Step 2: Enable -->
          <div>
            <p class="text-foreground text-sm font-semibold mb-2">2 — Enable the plugin</p>
            <div class="space-y-2">
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-pw-enable">npx codeadd plugins enable playwright</span>
                <button onclick="copyCmd('cmd-pw-enable', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-muted select-none"># to disable later</span>
              </div>
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-pw-disable">npx codeadd plugins disable playwright</span>
                <button onclick="copyCmd('cmd-pw-disable', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
            </div>
          </div>

          <!-- Step 3: Set up and run -->
          <div>
            <p class="text-foreground text-sm font-semibold mb-2">3 — Set up and run QA (once per project)</p>
            <p class="text-muted-light text-xs mb-2">Scaffold QA config after enabling, then run the audit per feature.</p>
            <div class="space-y-2">
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-pw-setup">/add.qa-setup</span>
                <button onclick="copyCmd('cmd-pw-setup', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
              <div class="flex items-center gap-2 bg-white/[0.03] border border-white/10 rounded-lg px-4 py-3 font-mono text-sm">
                <span class="text-accent select-none">$</span>
                <span class="flex-1 text-foreground" id="cmd-pw-run">/add.qa &lt;feature-id&gt;</span>
                <button onclick="copyCmd('cmd-pw-run', this)" class="plugin-copy-btn text-muted hover:text-accent transition-colors text-xs font-sans ml-2 shrink-0">Copy</button>
              </div>
            </div>
            <p class="text-muted text-xs mt-2">Verify: the Playwright MCP server must be connected (e.g. <code class="font-mono">/mcp</code>) — if it does not appear, <code class="font-mono">/add.qa</code> cannot drive the browser.</p>
          </div>

        </div>

        <script is:inline>
          function copyCmd(id, btn) {
            var text = document.getElementById(id).textContent;
            navigator.clipboard.writeText(text).then(function() {
              var orig = btn.textContent;
              btn.textContent = '✓';
              setTimeout(function() { btn.textContent = orig; }, 1500);
            });
          }
        </script>
      </section>

      <!-- PROVIDERS -->
      <section id="providers" class="mb-20">
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-6">Providers</h2>
        <p class="text-muted-light mb-8">
          Code Addiction generates provider-specific files so the same commands work across AI assistants.
        </p>

        <div class="overflow-x-auto">
          <table class="w-full text-sm">
            <thead>
              <tr class="border-b border-white/10">
                <th class="text-left py-3 pr-4 font-display font-semibold text-foreground">Provider</th>
                <th class="text-left py-3 pr-4 font-display font-semibold text-foreground">Directory</th>
                <th class="text-left py-3 font-display font-semibold text-foreground">Format</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-white/5">
              <tr><td class="py-3 pr-4 text-muted-light">Claude Code</td><td class="py-3 pr-4"><code class="text-accent font-mono text-xs">.claude/</code></td><td class="py-3 text-muted-light">commands/</td></tr>
              <tr><td class="py-3 pr-4 text-muted-light">Codex (OpenAI)</td><td class="py-3 pr-4"><code class="text-accent font-mono text-xs">.agents/</code></td><td class="py-3 text-muted-light">skills/</td></tr>
              <tr><td class="py-3 pr-4 text-muted-light">Google Antigravity</td><td class="py-3 pr-4"><code class="text-accent font-mono text-xs">.agent/</code></td><td class="py-3 text-muted-light">skills/</td></tr>
              <tr><td class="py-3 pr-4 text-muted-light">KiloCode</td><td class="py-3 pr-4"><code class="text-accent font-mono text-xs">.kilocode/</code></td><td class="py-3 text-muted-light">workflows/</td></tr>
              <tr><td class="py-3 pr-4 text-muted-light">OpenCode</td><td class="py-3 pr-4"><code class="text-accent font-mono text-xs">.opencode/</code></td><td class="py-3 text-muted-light">commands/</td></tr>
            </tbody>
          </table>
        </div>
      </section>

      <!-- STRUCTURE -->
      <section id="structure" class="mb-20">
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-6">Project Structure</h2>
        <p class="text-muted-light mb-6">
          The Code Addiction repository is organized as follows:
        </p>

        <div class="bg-white/[0.03] border border-white/10 rounded-xl p-6 font-mono text-sm leading-loose">
          <span class="text-accent">code-addiction/</span><br/>
          <span class="text-foreground">├── cli/</span><span class="text-muted ml-12">← installer CLI (npm: codeadd)</span><br/>
          <span class="text-foreground">├── framwork/</span><span class="text-muted ml-6">← framework payload</span><br/>
          <span class="text-foreground">│   └── .codeadd/</span><span class="text-muted ml-2">← core: commands, skills, scripts</span><br/>
          <span class="text-foreground">├── web/</span><span class="text-muted ml-14">← this website</span><br/>
          <span class="text-foreground">├── scripts/</span><span class="text-muted ml-8">← build and release scripts</span><br/>
          <span class="text-foreground">└── docs/</span><span class="text-muted ml-12">← project documentation</span>
        </div>
      </section>

      <!-- ECOSYSTEM MAP -->
      <section id="ecosystem" class="mb-20">
        <p class="font-mono text-accent text-xs tracking-widest uppercase mb-4">Deep Dive</p>
        <h2 class="font-display font-bold text-2xl sm:text-3xl mb-4">Ecosystem Map</h2>
        <p class="text-muted-light mb-6">
          Interactive map of commands, skills, and agents. Hover a node to highlight its connections. Click to inspect.
        </p>

        <!-- Legend + filters -->
        <div class="flex flex-wrap items-center gap-4 mb-4">
          <div class="flex items-center gap-4 text-xs text-muted-light">
            <span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full inline-block" style="background:#a78bfa"></span>Commands</span>
            <span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full inline-block" style="background:#22d3ee"></span>Skills</span>
            <span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full inline-block" style="background:#34d399"></span>Agents</span>
          </div>
          <div class="flex items-center gap-2 ml-auto flex-wrap">
            <input id="eco-search" type="text" placeholder="Search..." class="bg-white/[0.04] border border-white/10 rounded-md px-2.5 py-1 text-xs text-foreground placeholder-muted focus:outline-none focus:border-accent/40 font-mono w-32"/>
            <button data-filter="all" class="eco-btn text-xs px-3 py-1 rounded-md border border-accent bg-accent text-surface font-mono">All</button>
            <button data-filter="command" class="eco-btn text-xs px-3 py-1 rounded-md border border-white/10 text-muted-light hover:text-foreground font-mono">Commands</button>
            <button data-filter="skill" class="eco-btn text-xs px-3 py-1 rounded-md border border-white/10 text-muted-light hover:text-foreground font-mono">Skills</button>
            <button data-filter="agent" class="eco-btn text-xs px-3 py-1 rounded-md border border-white/10 text-muted-light hover:text-foreground font-mono">Agents</button>
          </div>
        </div>

        <!-- Graph + browse sidebar -->
        <div class="flex gap-4 items-start">
          <div id="eco-graph-wrap" class="flex-1 relative min-w-0">
            <div id="eco-cy" class="rounded-xl border border-white/10 bg-[#0a0a0f]" style="height:720px"></div>
            <div class="absolute top-3 right-3 flex flex-col gap-1 bg-[#0a0a0f]/80 border border-white/10 rounded-lg p-1 backdrop-blur-sm">
              <button id="eco-zoom-in" title="Zoom in" class="eco-ctrl"><svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 6v12m6-6H6"/></svg></button>
              <button id="eco-zoom-out" title="Zoom out" class="eco-ctrl"><svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M18 12H6"/></svg></button>
              <button id="eco-fit" title="Fit to canvas" class="eco-ctrl"><svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 8V4h4M16 4h4v4M20 16v4h-4M8 20H4v-4"/></svg></button>
              <div class="h-px bg-white/10 my-0.5"></div>
              <button id="eco-fullscreen" title="Fullscreen" class="eco-ctrl"><svg id="eco-fs-icon" class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 8V4h4M16 4h4v4M4 16v4h4M16 20h4v-4"/></svg></button>
            </div>
            <div class="absolute bottom-3 left-3 text-[10px] text-muted-light/70 font-mono pointer-events-none select-none bg-[#0a0a0f]/70 px-2 py-1 rounded">
              scroll to zoom · drag to pan · click to inspect
            </div>
          </div>
          <aside id="eco-sidebar" class="hidden lg:flex w-60 shrink-0 rounded-xl border border-white/10 bg-white/[0.02] flex-col" style="height:720px">
            <div id="eco-inspect" class="hidden border-b border-white/10 p-4 shrink-0">
              <div class="flex items-start justify-between gap-2">
                <div class="min-w-0">
                  <p id="eco-panel-name" class="font-mono text-foreground font-bold text-sm break-all leading-tight"></p>
                  <p id="eco-panel-type" class="font-mono text-[10px] uppercase tracking-widest mt-0.5"></p>
                </div>
                <button id="eco-panel-close" class="text-muted hover:text-foreground text-xs leading-none shrink-0">✕</button>
              </div>
              <div id="eco-panel-body" class="text-xs text-muted-light space-y-2 mt-3 max-h-48 overflow-y-auto"></div>
            </div>
            <div id="eco-browse" class="flex-1 overflow-y-auto p-3 space-y-3 min-h-0"></div>
          </aside>
        </div>
      </section>

      <!-- Footer -->
      <footer class="border-t border-white/5 pt-8 mt-16 flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-muted">
        <div class="flex items-center gap-2">
          <span class="font-display font-bold text-foreground"><span class="text-accent">+</span> code addiction</span>
          <span class="ml-2">by brabos.ai</span>
        </div>
        <div class="flex items-center gap-4">
          <a href="https://github.com/brabos-ai/code-addiction" target="_blank" rel="noopener" class="hover:text-accent transition-colors">GitHub</a>
          <a href="https://www.linkedin.com/in/maiconmatsubara/" target="_blank" rel="noopener" class="hover:text-accent transition-colors">LinkedIn</a>
          <a href="https://www.npmjs.com/package/codeadd" target="_blank" rel="noopener" class="hover:text-accent transition-colors">npm</a>
        </div>
      </footer>
    </main>
  </div>

  <script is:inline>
    const sidebarToggle = document.getElementById('docs-sidebar-toggle');
    const sidebar = document.getElementById('docs-sidebar');
    const backdrop = document.getElementById('docs-backdrop');
    const sidebarLinks = sidebar.querySelectorAll('a');

    function toggleSidebar() {
      const isClosed = sidebar.classList.contains('-translate-x-full');
      if (isClosed) {
        sidebar.classList.remove('-translate-x-full');
        backdrop.classList.remove('hidden');
        document.body.style.overflow = 'hidden';
      } else {
        sidebar.classList.add('-translate-x-full');
        backdrop.classList.add('hidden');
        document.body.style.overflow = '';
      }
    }

    if (sidebarToggle && sidebar && backdrop) {
      sidebarToggle.addEventListener('click', toggleSidebar);
      backdrop.addEventListener('click', toggleSidebar);

      sidebarLinks.forEach(link => {
        link.addEventListener('click', () => {
          if (window.innerWidth < 1024) {
            toggleSidebar();
          }
        });
      });
    }
  </script>

  <style is:global>
    .eco-list-item--active { background: rgba(255,255,255,0.06); }
    .eco-list-item--active span:last-child { color: #f1f5f9; }
    .eco-ctrl {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 28px;
      height: 28px;
      border-radius: 4px;
      color: #94a3b8;
      transition: background-color 120ms, color 120ms;
    }
    .eco-ctrl:hover { background: rgba(255,255,255,0.06); color: #f1f5f9; }
    .eco-ctrl:active { background: rgba(255,255,255,0.1); }
    #eco-graph-wrap.is-fullscreen {
      position: fixed;
      inset: 0;
      z-index: 100;
      padding: 16px;
      background: #0a0a0f;
    }
    #eco-graph-wrap.is-fullscreen #eco-cy {
      height: 100% !important;
      border-radius: 8px;
    }
  </style>

  <script is:inline>
  (function () {
    var CYTOSCAPE_CDN = 'https://cdnjs.cloudflare.com/ajax/libs/cytoscape/3.30.2/cytoscape.min.js';
    var LAYOUT_BASE_CDN = 'https://cdn.jsdelivr.net/npm/layout-base@2.0.1/layout-base.min.js';
    var COSE_BASE_CDN = 'https://cdn.jsdelivr.net/npm/cose-base@2.2.0/cose-base.min.js';
    var COSE_BILKENT_CDN = 'https://cdn.jsdelivr.net/npm/cytoscape-cose-bilkent@4.1.0/cytoscape-cose-bilkent.min.js';

    function loadScript(src) {
      return new Promise(function(res, rej) {
        var s = document.createElement('script');
        s.src = src;
        s.onload = res;
        s.onerror = rej;
        document.head.appendChild(s);
      });
    }

    function initEcosystem() {
      var nodes = [
        // Commands
        {data:{id:'add',label:'add',type:'command'}},
        {data:{id:'add.audit',label:'add.audit',type:'command'}},
        {data:{id:'add.autopilot',label:'add.autopilot',type:'command'}},
        {data:{id:'add.brainstorm',label:'add.brainstorm',type:'command'}},
        {data:{id:'add.build',label:'add.build',type:'command'}},
        {data:{id:'add.design',label:'add.design',type:'command'}},
        {data:{id:'add.diagnose',label:'add.diagnose',type:'command'}},
        {data:{id:'add.done',label:'add.done',type:'command'}},
        {data:{id:'add.hotfix',label:'add.hotfix',type:'command'}},
        {data:{id:'add.init',label:'add.init',type:'command'}},
        {data:{id:'add.new',label:'add.new',type:'command'}},
        {data:{id:'add.plan',label:'add.plan',type:'command'}},
        {data:{id:'add.pull-request',label:'add.pull-request',type:'command'}},
        {data:{id:'add.qa',label:'add.qa',type:'command'}},
        {data:{id:'add.qa-setup',label:'add.qa-setup',type:'command'}},
        {data:{id:'add.review',label:'add.review',type:'command'}},
        {data:{id:'add.test',label:'add.test',type:'command'}},
        {data:{id:'add.ux',label:'add.ux',type:'command'}},
        {data:{id:'add.xray',label:'add.xray',type:'command'}},
        // Agents
        {data:{id:'architecture-agent',label:'architecture',type:'agent'}},
        {data:{id:'backend-agent',label:'backend',type:'agent'}},
        {data:{id:'database-agent',label:'database',type:'agent'}},
        {data:{id:'discovery-agent',label:'discovery',type:'agent'}},
        {data:{id:'doc-reviewer-agent',label:'doc-reviewer',type:'agent'}},
        {data:{id:'feature-history-agent',label:'feature-history',type:'agent'}},
        {data:{id:'frontend-agent',label:'frontend',type:'agent'}},
        {data:{id:'git-history-agent',label:'git-history',type:'agent'}},
        {data:{id:'qa-agent',label:'qa',type:'agent'}},
        {data:{id:'e2e-agent',label:'e2e',type:'agent'}},
        {data:{id:'reviewer-agent',label:'reviewer',type:'agent'}},
        {data:{id:'system-design-agent',label:'system-design',type:'agent'}},
        {data:{id:'ux-agent',label:'ux',type:'agent'}},
        // Skills
        {data:{id:'add-doc-schemas',label:'doc-schemas',type:'skill'}},
        {data:{id:'add-ecosystem',label:'ecosystem',type:'skill'}},
        {data:{id:'add-ux-design',label:'ux-design',type:'skill'}},
        {data:{id:'add-id-convention',label:'id-convention',type:'skill'}},
        {data:{id:'add-backend-development',label:'backend-dev',type:'skill'}},
        {data:{id:'add-frontend-development',label:'frontend-dev',type:'skill'}},
        {data:{id:'add-tasks-checklist',label:'tasks-checklist',type:'skill'}},
        {data:{id:'add-database-development',label:'database-dev',type:'skill'}},
        {data:{id:'add-investigation',label:'investigation',type:'skill'}},
        {data:{id:'add-code-review',label:'code-review',type:'skill'}},
        {data:{id:'add-security-audit',label:'security-audit',type:'skill'}},
        {data:{id:'add-architecture-discovery',label:'arch-discovery',type:'skill'}},
        {data:{id:'add-feature-discovery',label:'feature-discovery',type:'skill'}},
        {data:{id:'add-delivery-validation',label:'delivery-validation',type:'skill'}},
        {data:{id:'add-health-check',label:'health-check',type:'skill'}},
        {data:{id:'add-product-discovery',label:'product-discovery',type:'skill'}},
        {data:{id:'add-doc-reviewer',label:'doc-reviewer',type:'skill'}},
        {data:{id:'add-claude-md-style',label:'claude-md-style',type:'skill'}},
        {data:{id:'add-dev-environment-setup',label:'dev-env-setup',type:'skill'}},
        {data:{id:'add-commit',label:'commit',type:'skill'}},
        {data:{id:'add-feature-specification',label:'feature-spec',type:'skill'}},
        {data:{id:'add-backend-architecture',label:'backend-arch',type:'skill'}},
        {data:{id:'add-frontend-architecture',label:'frontend-arch',type:'skill'}},
        {data:{id:'add-tdd',label:'tdd',type:'skill'}},
        {data:{id:'add-test-specification',label:'test-specification',type:'skill'}},
        {data:{id:'add-qa',label:'qa',type:'skill'}},
        {data:{id:'add-qa-spec',label:'qa-spec',type:'skill'}},
      ];

      var RAW = [
        // command → skill (loads)
        ['add','add-ecosystem','loads'],
        ['add','add-dev-environment-setup','loads'],
        ['add.audit','add-doc-schemas','loads'],
        ['add.audit','add-health-check','loads'],
        ['add.audit','add-investigation','loads'],
        ['add.autopilot','add-doc-schemas','loads'],
        ['add.autopilot','add-id-convention','loads'],
        ['add.autopilot','add-tasks-checklist','loads'],
        ['add.autopilot','add-ecosystem','loads'],
        ['add.autopilot','add-ux-design','loads'],
        ['add.autopilot','add-database-development','loads'],
        ['add.autopilot','add-backend-development','loads'],
        ['add.autopilot','add-frontend-development','loads'],
        ['add.autopilot','add-code-review','loads'],
        ['add.brainstorm','add-doc-schemas','loads'],
        ['add.brainstorm','add-ecosystem','loads'],
        ['add.build','add-doc-schemas','loads'],
        ['add.build','add-id-convention','loads'],
        ['add.build','add-tasks-checklist','loads'],
        ['add.build','add-ux-design','loads'],
        ['add.build','add-database-development','loads'],
        ['add.build','add-backend-development','loads'],
        ['add.build','add-frontend-development','loads'],
        ['add.design','add-doc-schemas','loads'],
        ['add.design','add-id-convention','loads'],
        ['add.design','add-ux-design','loads'],
        ['add.diagnose','add-doc-schemas','loads'],
        ['add.diagnose','add-ecosystem','loads'],
        ['add.diagnose','add-investigation','loads'],
        ['add.done','add-doc-schemas','loads'],
        ['add.done','add-id-convention','loads'],
        ['add.done','add-ecosystem','loads'],
        ['add.hotfix','add-doc-schemas','loads'],
        ['add.hotfix','add-id-convention','loads'],
        ['add.hotfix','add-investigation','loads'],
        ['add.hotfix','add-ux-design','loads'],
        ['add.hotfix','add-ecosystem','loads'],
        ['add.init','add-doc-schemas','loads'],
        ['add.init','add-product-discovery','loads'],
        ['add.new','add-doc-schemas','loads'],
        ['add.new','add-id-convention','loads'],
        ['add.new','add-feature-discovery','loads'],
        ['add.new','add-feature-specification','loads'],
        ['add.new','add-doc-reviewer','loads'],
        ['add.plan','add-doc-schemas','loads'],
        ['add.plan','add-id-convention','loads'],
        ['add.plan','add-backend-development','loads'],
        ['add.plan','add-database-development','loads'],
        ['add.plan','add-frontend-development','loads'],
        ['add.plan','add-ux-design','loads'],
        ['add.plan','add-tasks-checklist','loads'],
        ['add.review','add-code-review','loads'],
        ['add.review','add-frontend-development','loads'],
        ['add.review','add-ux-design','loads'],
        ['add.review','add-backend-development','loads'],
        ['add.review','add-database-development','loads'],
        ['add.review','add-security-audit','loads'],
        ['add.review','add-delivery-validation','loads'],
        ['add.review','add-tasks-checklist','loads'],
        ['add.review','add-investigation','loads'],
        ['add.test','add-backend-development','loads'],
        ['add.test','add-frontend-development','loads'],
        ['add.test','add-ecosystem','loads'],
        ['add.ux','add-ux-design','loads'],
        ['add.xray','add-doc-schemas','loads'],
        ['add.xray','add-architecture-discovery','loads'],
        ['add.xray','add-claude-md-style','loads'],
        ['add.qa','add-doc-schemas','loads'],
        ['add.qa','add-qa','loads'],
        ['add.qa-setup','add-doc-schemas','loads'],
        ['add.qa-setup','add-dev-environment-setup','loads'],
        ['add.plan','add-qa-spec','loads'],
        // command → agent (dispatches)
        ['add.autopilot','backend-agent','dispatches'],
        ['add.autopilot','frontend-agent','dispatches'],
        ['add.autopilot','database-agent','dispatches'],
        ['add.autopilot','reviewer-agent','dispatches'],
        ['add.build','backend-agent','dispatches'],
        ['add.build','frontend-agent','dispatches'],
        ['add.build','database-agent','dispatches'],
        ['add.build','reviewer-agent','dispatches'],
        ['add.design','ux-agent','dispatches'],
        ['add.new','discovery-agent','dispatches'],
        ['add.new','doc-reviewer-agent','dispatches'],
        ['add.xray','architecture-agent','dispatches'],
        ['add.qa','qa-agent','dispatches'],
        ['add.test','e2e-agent','dispatches'],
        ['add.diagnose','discovery-agent','dispatches'],
        ['add.diagnose','architecture-agent','dispatches'],
        ['add.diagnose','feature-history-agent','dispatches'],
        ['add.diagnose','git-history-agent','dispatches'],
        ['add.hotfix','feature-history-agent','dispatches'],
        ['add.hotfix','git-history-agent','dispatches'],
        // agent → skill (uses)
        ['backend-agent','add-backend-development','uses'],
        ['backend-agent','add-database-development','uses'],
        ['frontend-agent','add-frontend-development','uses'],
        ['database-agent','add-database-development','uses'],
        ['reviewer-agent','add-code-review','uses'],
        ['reviewer-agent','add-security-audit','uses'],
        ['ux-agent','add-ux-design','uses'],
        ['discovery-agent','add-feature-discovery','uses'],
        ['discovery-agent','add-feature-specification','uses'],
        ['doc-reviewer-agent','add-doc-reviewer','uses'],
        ['doc-reviewer-agent','add-doc-schemas','uses'],
        ['architecture-agent','add-architecture-discovery','uses'],
        ['architecture-agent','add-backend-architecture','uses'],
        ['architecture-agent','add-frontend-architecture','uses'],
        ['system-design-agent','add-architecture-discovery','uses'],
        ['qa-agent','add-qa','uses'],
      ];

      var edges = RAW.map(function(r, i) {
        return {data:{id:'e'+i, source:r[0], target:r[1], type:r[2]}};
      });

      // Compute degree (connection count) per node for size scaling
      var degreeMap = {};
      edges.forEach(function(e) {
        degreeMap[e.data.source] = (degreeMap[e.data.source] || 0) + 1;
        degreeMap[e.data.target] = (degreeMap[e.data.target] || 0) + 1;
      });
      nodes.forEach(function(n) {
        n.data.degree = degreeMap[n.data.id] || 1;
      });

      var cy = window.__ecoCy = window.cytoscape({
        container: document.getElementById('eco-cy'),
        elements: { nodes: nodes, edges: edges },
        wheelSensitivity: 0.2,
        style: [
          {
            selector: 'node',
            style: {
              'label': '',
              'font-size': 10,
              'font-family': 'ui-sans-serif, system-ui, sans-serif',
              'text-valign': 'bottom',
              'text-halign': 'center',
              'text-margin-y': 6,
              'color': '#cbd5e1',
              'text-wrap': 'wrap',
              'text-max-width': 140,
              'text-outline-color': '#0a0a0f',
              'text-outline-width': 2,
              'shape': 'ellipse',
              'width': 'mapData(degree, 1, 12, 10, 38)',
              'height': 'mapData(degree, 1, 12, 10, 38)',
              'border-width': 0,
              'transition-property': 'background-color, border-width, width, height',
              'transition-duration': '120ms',
            }
          },
          {
            selector: 'node.show-label',
            style: { 'label': 'data(label)' }
          },
          {
            selector: 'node[type="command"]',
            style: {
              'background-color': '#a78bfa',
            }
          },
          {
            selector: 'node[type="skill"]',
            style: {
              'background-color': '#22d3ee',
            }
          },
          {
            selector: 'node[type="agent"]',
            style: {
              'background-color': '#34d399',
            }
          },
          {
            selector: 'node.highlighted',
            style: {
              'border-width': 2,
              'border-color': '#ffffff',
              'border-opacity': 0.85,
              'color': '#f1f5f9',
              'font-size': 11,
              'z-index': 999,
            }
          },
          {
            selector: 'edge',
            style: {
              'width': 0.8,
              'line-color': '#334155',
              'curve-style': 'bezier',
              'opacity': 0.55,
              'target-arrow-shape': 'none',
            }
          },
          {
            selector: 'edge.highlighted',
            style: {
              'width': 1.4,
              'line-color': '#cbd5e1',
              'opacity': 1,
              'z-index': 998,
            }
          },
          {
            selector: '.faded',
            style: {'opacity': 0.08}
          },
          {
            selector: '.hidden-node',
            style: {'display': 'none'}
          },
        ],
        layout: {
          name: 'cose-bilkent',
          quality: 'proof',
          nodeDimensionsIncludeLabels: false,
          fit: true,
          padding: 60,
          randomize: true,
          nodeRepulsion: 6500,
          idealEdgeLength: 85,
          edgeElasticity: 0.45,
          nestingFactor: 0.1,
          gravity: 0.9,
          numIter: 3500,
          tile: true,
          tilingPaddingVertical: 6,
          tilingPaddingHorizontal: 6,
          gravityRangeCompound: 1.5,
          gravityCompound: 1.0,
          gravityRange: 1.2,
          initialEnergyOnIncremental: 0.5,
          animate: false,
        }
      });

      // Pull disconnected components close to the main cluster centroid.
      // cose-bilkent tiles orphans on the side; we re-center them so they
      // don't visually drift away from the graph.
      cy.ready(function() {
        var components = cy.elements().components();
        if (components.length <= 1) return;
        // Largest component is treated as the "main" cluster
        var main = components.reduce(function(a, b) { return a.length >= b.length ? a : b; });
        var bb = main.boundingBox();
        var cx = bb.x1 + bb.w / 2;
        var cy1 = bb.y1 + bb.h / 2;
        components.forEach(function(comp) {
          if (comp === main) return;
          var cb = comp.boundingBox();
          var ccx = cb.x1 + cb.w / 2;
          var ccy = cb.y1 + cb.h / 2;
          // Pick the closest edge of the main bbox + a small gap
          var gap = 80;
          var targetX, targetY;
          if (Math.abs(ccx - cx) > Math.abs(ccy - cy1)) {
            targetX = ccx < cx ? bb.x1 - gap : bb.x2 + gap;
            targetY = cy1;
          } else {
            targetX = cx;
            targetY = ccy < cy1 ? bb.y1 - gap : bb.y2 + gap;
          }
          var dx = targetX - ccx;
          var dy = targetY - ccy;
          comp.nodes().forEach(function(n) {
            var p = n.position();
            n.position({ x: p.x + dx, y: p.y + dy });
          });
        });
        cy.fit(cy.elements(), 50);
      });

      // ----- Helpers shared between graph + list -----
      function highlightNode(id) {
        var n = cy.getElementById(id);
        if (!n || n.empty()) return;
        // Always reset state first so successive highlights don't accumulate
        cy.elements().removeClass('faded').removeClass('highlighted').removeClass('show-label');
        var hood = n.closedNeighborhood();
        cy.elements().not(hood).addClass('faded');
        hood.addClass('highlighted');
        hood.nodes().addClass('show-label');
        document.querySelectorAll('.eco-list-item').forEach(function(el) {
          el.classList.toggle('eco-list-item--active', el.dataset.id === id);
        });
      }
      function clearHighlight() {
        cy.elements().removeClass('faded').removeClass('highlighted');
        if (cy.zoom() < 1.1) cy.nodes().removeClass('show-label');
        document.querySelectorAll('.eco-list-item--active').forEach(function(el) {
          el.classList.remove('eco-list-item--active');
        });
      }
      function focusNode(id) {
        var n = cy.getElementById(id);
        if (!n || n.empty()) return;
        cy.stop(true, true);
        cy.animate({ center: { eles: n }, zoom: Math.max(cy.zoom(), 1.4) }, { duration: 350 });
        showInspect(n);
        highlightNode(id);
      }
      function showInspect(n) {
        var inspect = document.getElementById('eco-inspect');
        document.getElementById('eco-panel-name').textContent = n.data('id');
        var typeEl = document.getElementById('eco-panel-type');
        typeEl.textContent = n.data('type');
        typeEl.style.color = TYPE_COLOR[n.data('type')] || '#cbd5e1';
        var out = n.outgoers('node');
        var inc = n.incomers('node');
        var html = '';
        if (out.length) {
          html += '<div><span class="text-foreground font-mono text-[10px] uppercase tracking-wide">Loads / Dispatches</span><ul class="mt-1 space-y-0.5">';
          out.each(function(x) { html += '<li class="font-mono text-[10px] truncate hover:text-foreground cursor-pointer eco-link" data-id="' + x.id() + '">' + x.id() + '</li>'; });
          html += '</ul></div>';
        }
        if (inc.length) {
          html += '<div><span class="text-foreground font-mono text-[10px] uppercase tracking-wide">Used by</span><ul class="mt-1 space-y-0.5">';
          inc.each(function(x) { html += '<li class="font-mono text-[10px] truncate hover:text-foreground cursor-pointer eco-link" data-id="' + x.id() + '">' + x.id() + '</li>'; });
          html += '</ul></div>';
        }
        document.getElementById('eco-panel-body').innerHTML = html;
        inspect.classList.remove('hidden');
        // Wire link clicks inside inspect to focus
        inspect.querySelectorAll('.eco-link').forEach(function(li) {
          li.addEventListener('click', function() { focusNode(li.dataset.id); });
        });
      }
      var TYPE_COLOR = { command: '#a78bfa', skill: '#22d3ee', agent: '#34d399' };

      // Hover: highlight neighborhood + reveal labels (Obsidian-style)
      cy.on('mouseover', 'node', function(e) { highlightNode(e.target.id()); });
      cy.on('mouseout', 'node', function() { clearHighlight(); });

      // ----- Browse sidebar: render lists -----
      var browseEl = document.getElementById('eco-browse');
      var GROUPS = [
        { type: 'command', label: 'Commands', color: TYPE_COLOR.command, collapsed: false },
        { type: 'skill', label: 'Skills', color: TYPE_COLOR.skill, collapsed: true },
        { type: 'agent', label: 'Agents', color: TYPE_COLOR.agent, collapsed: true },
      ];
      browseEl.innerHTML = GROUPS.map(function(g) {
        var items = nodes.filter(function(n) { return n.data.type === g.type; })
          .sort(function(a, b) { return a.data.id.localeCompare(b.data.id); });
        var lis = items.map(function(n) {
          return '<li class="eco-list-item flex items-center gap-2 px-2 py-1 rounded cursor-pointer hover:bg-white/[0.04] transition-colors" data-id="' + n.data.id + '">' +
            '<span class="w-1.5 h-1.5 rounded-full shrink-0" style="background:' + g.color + '"></span>' +
            '<span class="font-mono text-[11px] text-muted-light truncate">' + n.data.id + '</span></li>';
        }).join('');
        var collapsedAttr = g.collapsed ? 'true' : 'false';
        var listStyle = g.collapsed ? ' style="display:none"' : '';
        var chevStyle = g.collapsed ? ' style="transform:rotate(-90deg)"' : '';
        return '<div data-type="' + g.type + '" data-collapsed="' + collapsedAttr + '">' +
          '<button type="button" class="eco-group-toggle font-mono text-[10px] uppercase tracking-widest mb-1.5 px-2 w-full flex items-center justify-between hover:bg-white/[0.03] rounded py-0.5 transition-colors" style="color:' + g.color + '">' +
            '<span class="flex items-center gap-1.5">' +
              '<svg class="eco-chevron w-2.5 h-2.5 transition-transform"' + chevStyle + ' fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M19 9l-7 7-7-7"/></svg>' +
              g.label +
            '</span><span class="text-muted" data-count>' + items.length + '</span>' +
          '</button>' +
          '<ul class="eco-group-list space-y-px"' + listStyle + '>' + lis + '</ul></div>';
      }).join('');

      // List item interactions — click only (hover does nothing, per UX feedback)
      browseEl.addEventListener('click', function(e) {
        var toggle = e.target.closest('.eco-group-toggle');
        if (toggle) {
          var group = toggle.parentElement;
          var collapsed = group.dataset.collapsed === 'true';
          group.dataset.collapsed = collapsed ? 'false' : 'true';
          var list = group.querySelector('.eco-group-list');
          var chev = group.querySelector('.eco-chevron');
          if (list) list.style.display = collapsed ? '' : 'none';
          if (chev) chev.style.transform = collapsed ? '' : 'rotate(-90deg)';
          return;
        }
        var li = e.target.closest('.eco-list-item');
        if (li) focusNode(li.dataset.id);
      });

      // Zoom: progressively reveal labels
      cy.on('zoom', function() {
        var z = cy.zoom();
        if (z >= 1.1) {
          cy.nodes().addClass('show-label');
        } else {
          cy.nodes().removeClass('show-label');
        }
      });

      // Click node: open inspect
      cy.on('tap', 'node', function(e) { showInspect(e.target); });

      document.getElementById('eco-panel-close').addEventListener('click', function() {
        document.getElementById('eco-inspect').classList.add('hidden');
      });

      // ----- Sidebar list filter (type + search) -----
      var listState = { filter: 'all', query: '' };
      function applyListFilter() {
        var q = listState.query;
        var f = listState.filter;
        document.querySelectorAll('#eco-browse > div').forEach(function(group) {
          var groupType = group.dataset.type;
          var groupVisible = (f === 'all' || f === groupType);
          var visibleCount = 0;
          group.querySelectorAll('.eco-list-item').forEach(function(li) {
            var match = groupVisible && (!q || li.dataset.id.toLowerCase().indexOf(q) !== -1);
            li.style.display = match ? '' : 'none';
            if (match) visibleCount++;
          });
          group.style.display = (groupVisible && visibleCount > 0) ? '' : 'none';
          var countEl = group.querySelector('[data-count]');
          if (countEl) countEl.textContent = visibleCount;
        });
      }

      document.querySelectorAll('.eco-btn').forEach(function(btn) {
        btn.addEventListener('click', function() {
          document.querySelectorAll('.eco-btn').forEach(function(b) {
            b.className = 'eco-btn text-xs px-3 py-1 rounded-md border border-white/10 text-muted-light hover:text-foreground font-mono';
          });
          this.className = 'eco-btn text-xs px-3 py-1 rounded-md border border-accent bg-accent text-surface font-mono';
          listState.filter = this.dataset.filter;
          applyListFilter();
        });
      });

      document.getElementById('eco-search').addEventListener('input', function() {
        listState.query = this.value.trim().toLowerCase();
        applyListFilter();
      });

      // ----- Graph controls -----
      function zoomBy(factor) {
        var z = cy.zoom() * factor;
        cy.animate({ zoom: { level: z, position: { x: cy.width()/2, y: cy.height()/2 } } }, { duration: 180 });
      }
      document.getElementById('eco-zoom-in').addEventListener('click', function() { zoomBy(1.3); });
      document.getElementById('eco-zoom-out').addEventListener('click', function() { zoomBy(1/1.3); });
      document.getElementById('eco-fit').addEventListener('click', function() {
        cy.stop(true, true);
        cy.elements().removeClass('faded').removeClass('highlighted').removeClass('show-label');
        document.querySelectorAll('.eco-list-item--active').forEach(function(el) { el.classList.remove('eco-list-item--active'); });
        document.getElementById('eco-inspect').classList.add('hidden');
        cy.animate({ fit: { eles: cy.elements(), padding: 50 } }, { duration: 350 });
      });

      var wrap = document.getElementById('eco-graph-wrap');
      var fsIcon = document.getElementById('eco-fs-icon');
      var ICON_FS = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 8V4h4M16 4h4v4M4 16v4h4M16 20h4v-4"/>';
      var ICON_EXIT = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 9V4M9 9H4M15 9V4M15 9h5M9 15v5M9 15H4M15 15v5M15 15h5"/>';
      function toggleFullscreen() {
        var entering = !wrap.classList.contains('is-fullscreen');
        wrap.classList.toggle('is-fullscreen', entering);
        fsIcon.innerHTML = entering ? ICON_EXIT : ICON_FS;
        document.body.style.overflow = entering ? 'hidden' : '';
        // Resize cy to new container, then refit
        setTimeout(function() {
          cy.resize();
          cy.fit(cy.elements(), 50);
        }, 60);
      }
      document.getElementById('eco-fullscreen').addEventListener('click', toggleFullscreen);
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && wrap.classList.contains('is-fullscreen')) toggleFullscreen();
      });
    }

    // Lazy-load Cytoscape when section scrolls into view
    var section = document.getElementById('ecosystem');
    if (!section) return;
    var observer = new IntersectionObserver(function(entries) {
      if (entries[0].isIntersecting) {
        observer.disconnect();
        var chain = window.cytoscape
          ? Promise.resolve()
          : loadScript(CYTOSCAPE_CDN);
        chain
          .then(function() { return loadScript(LAYOUT_BASE_CDN); })
          .then(function() { return loadScript(COSE_BASE_CDN); })
          .then(function() { return loadScript(COSE_BILKENT_CDN); })
          .then(initEcosystem)
          .catch(function(err) { console.error('Ecosystem load failed:', err); });
      }
    }, { rootMargin: '300px' });
    observer.observe(section);
  })();
  </script>
</Layout>